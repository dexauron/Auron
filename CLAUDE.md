# Auron Finance — память проекта

## Что это
Универсальная финансовая платформа — от личного бюджета до сети магазинов.
Модульная архитектура: ядро (счета, транзакции, аналитика) + подключаемые модули под каждый тип бизнеса.
**v1.0 = Модуль розницы** на примере Way Market.
Владелец — не программист. Общение с ним — на русском, простым языком, без жаргона.

## ⚠️ Главное правило
Проект проходит редизайн «с нуля» по этапам. **Не добавляй фичи и не пиши код,
пока соответствующий документ в `docs/` не утверждён владельцем.**

## Память проекта — в документах
Вся история решений живёт в `docs/`, а не в чатах. В начале сессии прочитай:

| Файл | Содержание | Статус |
|------|-----------|--------|
| `docs/00-VISION.md` | Что делает продукт, для кого, чего НЕ делает | ✅ утверждён |
| `docs/01-USER.md` | Портрет пользователя, его день | ✅ утверждён |
| `docs/02-FLOWS.md` | Ключевые сценарии по шагам | ✅ утверждён |
| `docs/03-SCREENS.md` | Описание каждого экрана словами | ✅ утверждён |
| `docs/04-DESIGN-SYSTEM.md` | Цвета, кнопки, отступы — правила | ✅ утверждён |
| `docs/05-ARCHITECTURE.md` | Данные, синхронизация, офлайн | ✅ утверждён |
| `docs/06-ROADMAP.md` | Дорожная карта компании по шагам | ✅ утверждён |
| `docs/07-IDEAS.md` | Банк одобренных идей на будущее | 🔄 пополняется |
| `docs/08-CATALOG.md` | Каталог товаров для сотрудников (отдельное приложение) | ✅ утверждён (решения делегированы) |
| `docs/DECISIONS.md` | Журнал решений с причинами | 🔄 ведётся |

**Текущий этап: Разговор 7 — Написание кода. Все документы утверждены.**
Сделано: `auth.js` (телефон+SMS, мультимагазинность, лестница PIN), `api.js` (+ домены Розницы),
`state.js` и `router.js` (шина событий, права, реестр модулей), миграция `005_retail_domains.sql`.
Дальше: модули экранов в `app/js/modules/` и переписывание `app/index.html`.

## Как работать
1. Прочитай документы → пойми, на каком этапе проект
2. Обсуждай с владельцем ТОЛЬКО текущий этап, не прыгай вперёд
3. Каждое принятое решение — сразу в соответствующий документ + строка в `DECISIONS.md`
4. Обновляй статусы и «Текущий этап» в этой таблице
5. Коммить документы после каждого обсуждения (ветка `claude/optimistic-einstein-Afzmv`, мерж в `main` — только по просьбе владельца)
6. Документ больше 2 страниц = решения недостаточно чёткие, сжимай

## Технический контекст (текущее состояние кода)
- `app/` — SPA, деплой на GitHub Pages через `.github/workflows/deploy.yml` → https://dexauron.github.io/Auron/. Файлы: `index.html` (каркас, переписывается), `js/state.js` (шина событий, права, модули), `js/router.js` (навигация по hash), `js/api.js`, `js/auth.js`, `js/offline-queue.js`
- Backend: **self-hosted Supabase на Timeweb Cloud** (Москва, 201.51.6.170). Studio: `http://201.51.6.170:8000`. Стек: PostgreSQL + Auth + PostgREST + Realtime + Storage
- Схема БД — `infra/migrations/`, применять по порядку номеров. Рабочая схема одна; папка `supabase/` со второй схемой удалена
- Роли: **Владелец / Бухгалтер / Администратор / Сотрудник зала**. Роли «Кассир» нет. Права — набором ключей в `state.js`, не жёстко по экранам
- Безопасность: PIN 4 цифры, автоблокировка 1 мин (настраивается). Защита от перебора — лестница из `03-SCREENS.md`: 1–5 без пауз · 6–10 пауза 30 сек · 11–15 пауза 5 мин · с 16-й блокировка
- Тесты — `tests/*.test.mjs`, запуск `node tests/имя.test.mjs`. Сети не требуют
- PWA: `app/sw.js`, версию кэша (`auron-vNN`) бампать при каждом изменении фронта
- ⚠️ Бэкенд работает по `http://`, сайт по `https://` — браузер блокирует такие запросы. В проде приложение не свяжется с базой, пока не включён HTTPS: скрипт готов в `setup/enable-https.sh`
- ⚠️ Вход по телефону написан, но требует SMS-провайдера в Supabase Auth. Пока рабочий вход — email и пароль
- `prototype/index.html` — эталон SVG-иконок для дизайн-системы, не удалять
- `catalog/` — **отдельное приложение** «Каталог товаров для сотрудников» (см. `docs/08-CATALOG.md`): своя база (отдельный проект Supabase Cloud, ключи в `catalog/js/config.js`), живёт на `…/Auron/catalog/`. С кодом и базой Auron НЕ смешивать
- **Разделение веток и деплоя:** приложение Auron разрабатывается в ветке `claude/optimistic-einstein-Afzmv`, каталог — в ветке `claude/store-product-catalog-60wer4`. Деплой (`.github/workflows/deploy.yml`, одинаковый в обеих ветках) всегда собирает сайт из ДВУХ источников: `app/` из ветки Auron + `catalog/` из ветки каталога — пуш в одну ветку не затрагивает второе приложение. Меняешь workflow — синхронизируй его в обе ветки
