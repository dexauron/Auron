# Журнал решений

> Формат: дата — решение — почему. Только принятые решения, не обсуждения.

| Дата | Решение | Почему |
|------|---------|--------|
| 2026-06-10 | Редизайн «с нуля» через этапы проектирования, код не трогаем до утверждения документов | Приложение разрослось в «кашу»: слишком много фич, запутанная навигация, разнобой в дизайне |
| 2026-06-10 | Память проекта — в документах `docs/`, а не в истории чатов | Длинные чаты теряют контекст; короткие документы-выжимки переживают любую сессию |
| 2026-06-10 | Все обсуждения — в Claude Code (не в обычном чате) | Прямой доступ к репозиторию: решения сразу фиксируются в документах |
| 2026-06-11 | Продукт специализированный для розницы с кредитными поставщиками — не универсальный финансовый трекер | Бизнес имеет специфику: две смены, 20+ поставщиков с долгами, ежедневная сверка кассы |
| 2026-06-11 | Три роли: Бухгалтер (главный), Администратор (заказы/записи), Владелец (только просмотр) | Реальная структура команды магазина |
| 2026-06-11 | Не заменяем 1С — остатки, ТСД, списания остаются там | 1С уже работает, дублирование создаст путаницу |
| 2026-06-11 | 10 сценариев зафиксированы как ядро продукта | На основе реального Excel-workflow бухгалтера Way Market |
| 2026-06-11 | Продукт становится SaaS-платформой для любых розничных магазинов | Владелец хочет продавать другим магазинам, Way Market — первый клиент и эталон |
| 2026-06-11 | Freemium: бесплатно = лучше Excel, платно = то чего Excel не умеет (ИИ, прогнозы, уведомления, аналитика) | Бесплатный план должен уже быть лучше Excel чтобы пользователи почувствовали ценность |
| 2026-06-11 | Каждый магазин настраивает логику под себя (отделы, расходы, поставщики, сотрудники) | Универсальность: разные магазины = разные конфигурации |
| 2026-06-11 | Сотрудники: можно уволить (статус «уволен», история сохраняется) и добавить нового | Реальная потребность — текучка кадров в рознице |
| 2026-06-11 | «25%» = рентабельность: `(Выручка − Себестоимость) / Выручка × 100%`. Каждый магазин задаёт свой целевой % | Владелец отслеживает эффективность торговли, а не только обороты |
| 2026-06-11 | Два режима учёта товаров: «1С» (импорт Excel) и «Ручной» (встроенный 1С-лайт, до ~50 позиций) — выбирается в настройках | Ручной режим = та же логика что у 1С (приход, продажи, рентабельность), только упрощённая и встроенная в приложение — для магазинов без 1С |
| 2026-06-11 | Приложение не продаёт товары — только аналитика, учёт и отчётность для бухгалтера/администратора/владельца | Чёткая граница: операционная торговля остаётся в 1С и на кассе |
| 2026-06-11 | Стиль: минимализм без урезания функционала. Принцип — прятать сложность, не убирать её. Главное видно сразу, детали — по тапу. Эталон: Monzo, Revolut, Zenmoney | Комфорт и интуитивность важнее визуальной насыщенности; пользователь должен понимать где что лежит без объяснений |
| 2026-06-11 | Роли гибкие: один человек может иметь несколько ролей (например, Бухгалтер + Администратор). Видит объединение всех своих прав | Реальная ситуация — один человек часто работает за двоих |
| 2026-06-11 | Только Владелец управляет аккаунтами и ролями сотрудников. Владелец может назначать, менять и отзывать доступ в любое время | Безопасность: права выдаются сверху вниз, не самостоятельно |
| 2026-06-11 | Несколько человек могут иметь одну роль (2 бухгалтера, 2 администратора и т.д.) — каждый со своим аккаунтом | Реальная ситуация: большой магазин делит работу между несколькими сотрудниками одной должности |
| 2026-06-11 | Мультимагазинность: один аккаунт владельца → несколько магазинов. Сотрудники каждого магазина видят только свои данные. Владелец видит все магазины из одного места | Реальная потребность сетевого владельца: контролировать все точки без переключения аккаунтов |
| 2026-06-11 | Владелец может выдать «сквозной» доступ ко всем магазинам другому аккаунту (управляющий, доверенный бухгалтер сети) | Делегирование контроля без передачи владения |
| 2026-06-11 | Табель: у каждого сотрудника — график (5/2, 2/2, 3/3, свой) и временные события (отпуск, больничный с датами). Приложение авто-заполняет статус каждого дня, бухгалтер проверяет и подтверждает | Исключает ежедневный ручной ввод для 10+ сотрудников — только проверка и «Подтвердить» |
| 2026-06-11 | Статусы табеля: Вышел / Полдня (50%) / Выходной / Больничный / Прогул / Отпуск. Для нестандартных случаев — скрытое поле коэффициента (0.75, 0.3 и т.д.) | Покрывает все реальные ситуации без усложнения обычного дня |
| 2026-06-11 | Два типа уведомлений: плановые (что запланировано на сегодня) + напоминания о незакрытых задачах (Z-отчёт, табель, несостоявшиеся выплаты). Время напоминаний настраивается в настройках | Приложение проактивно следит за задачами — пользователь не должен держать это в голове |
| 2026-06-11 | Дневной отчёт: автогенерация из данных дня → отправка текстом в WhatsApp/Telegram или PDF. Кнопка на Главной после закрытия Z-отчёта. Живая ссылка — платный тариф | Бухгалтер не набирает отчёт вручную — одна кнопка и владелец получает сводку |
| 2026-06-11 | Главный бухгалтер = роль Бухгалтер + сквозной доступ ко всем магазинам (только финансовые данные, не управление аккаунтами) | Иерархия: главный видит всё, бухгалтер магазина — только свой |
| 2026-06-11 | ❌ Встроенный мессенджер — не делаем | Отдельный продукт по сложности; сотрудники уже используют WhatsApp-группы; реальные потребности в общении закрыты уведомлениями и заметками |
| 2026-06-11 | Задачи/заметки от главного бухгалтера — делаем. Простые: написал → бухгалтер магазина видит при входе | Заменяет голосовое общение, не мессенджер — только рабочие инструкции |
| 2026-06-11 | При входе с несколькими магазинами: показывается сводная картина по всем сразу. Переход в конкретный магазин — по выбору | Не тратить время на выбор при каждом входе; общая картина важнее детали по умолчанию |
| 2026-06-11 | Четвёртая роль: **Сотрудник зала** — видит только товары, историю поставок и заказы. Финансовые данные (касса, долги, зарплаты) не видит | Продавцу нужно знать где товар и от кого пришёл — деньги магазина его не касаются |
| 2026-06-11 | ИИ-стратегия: старт на Claude API (₽2 000/мес), свой сервер — только если продукт докажет востребованность (300+ платных магазинов) | Не тратить ресурсы на инфраструктуру пока нет подтверждённого спроса |
| 2026-06-11 | ИИ для пользователей MVP: кнопка «Спросить ИИ» в приложении + вердикт в дневном отчёте + аномалии в уведомлениях. Всё остальное (прогнозы, проактивные советы) — позже | Дать пользователю ощутить ценность ИИ с первого дня без сложной инфраструктуры |
| 2026-06-11 | ИИ для разработчика: с первого дня собираем аналитику поведения + in-app опросы раз в 2 недели. Позже — ИИ-анализ паттернов и автопредложения улучшений | Данные копятся пассивно с нулевой стоимостью — основа для умного продукта в будущем |
| 2026-06-11 | Философия продукта: самосовершенствующийся продукт под надзором разработчика. ИИ наблюдает за пользователями → находит проблемы → предлагает инсайт + дизайн + код → разработчик одобряет → деплой → ИИ наблюдает результат. Цикл бесконечный | ИИ — это двигатель эволюции продукта. Разработчик — фильтр качества, не источник идей |
| 2026-06-11 | Персонализация per-user: каждый аккаунт получает свою версию интерфейса. Три слоя: визуал (тема, шрифт) → рабочий процесс (порядок элементов, быстрые действия) → функционал (скрыть/показать разделы). Изменения через панель разработчика с одобрением | Лего-конструктор: старт одинаковый для всех, со временем — каждый получает своё |
| 2026-06-11 | Два обучающихся цикла: (1) пользователь → ИИ собирает паттерны → отчёт разработчику раз в 2-3 недели → одобрение → изменение аккаунта; (2) разработчик пишет код → ИИ наблюдает паттерн решения → учится → предлагает аналогичное сам в будущем | ИИ учится у разработчика чтобы в будущем делать похожие изменения самостоятельно |
| 2026-06-11 | Для персонализации заложить с первого дня: feature flags per-user (включать/выключать любой элемент для конкретного аккаунта), трекинг каждого события с привязкой к пользователю | Без per-user флагов персонализация технически невозможна |
| 2026-06-11 | Для самосовершенствования заложить в архитектуру с первого дня: трекинг каждого события, флаги фич (включать/выключать части UI без деплоя), модульная структура кода, тесты | Без этой основы ИИ не сможет безопасно предлагать и проверять изменения |
| 2026-06-11 | Финансовый пульс: один индикатор (🟢/🟡/🔴) в шапке показывает здоровье бизнеса. Считается из выручки + долгов + накоплений + расхождений. Платный тариф | Владелец понимает состояние бизнеса за секунду без чтения цифр |
| 2026-06-11 | Календарь денег: визуальный календарь на месяц вперёд — зелёные/красные/жёлтые дни по плановым притокам и выплатам. Видно когда будет туго заранее. Платный тариф | Ни одно малое бизнес-приложение не даёт этого визуально — сильное отличие от конкурентов |
| 2026-06-11 | Предсказание кассового разрыва: ИИ считает наперёд — когда и на сколько не хватит денег, и сразу предлагает решение (перенести выплату или докладывать X в день). Платный тариф | Бухгалтер сейчас держит это в голове — одна из главных болей |
| 2026-06-11 | Оптимальный порядок выплат: когда денег меньше чем нужно отдать, ИИ предлагает кому платить первым с обоснованием. Платный тариф | Стратегия вместо списка — делает бухгалтера увереннее |
| 2026-06-11 | WhatsApp-first отчёт: приложение само отправляет итог дня владельцу в WhatsApp каждый вечер в настраиваемое время — без кнопки, без открытия приложения. Платный тариф | Нулевое трение для владельца — просто читает в ленте как обычное сообщение |
| 2026-06-11 | Фото накладной → автозаполнение: фотографируешь накладную → ИИ читает сумму/поставщика/дату → форма заполняется автоматически. Платный тариф | Убирает ручной ввод — одна из самых частых ежедневных операций |
| 2026-06-11 | Надёжность поставщика: накапливается рейтинг каждого поставщика — точность сумм, пунктуальность доставок, явка на плановые выплаты. Видно в карточке поставщика | Помогает владельцу принимать решения с кем работать на основе данных, а не ощущений |
| 2026-06-11 | «Что если» симулятор: сценарное планирование — «что если аренда вырастет на ₽10К», «что если выручка упадёт на 20%» → мгновенный пересчёт влияния на прибыль и остаток. Платный тариф | Никто из конкурентов не даёт этого для малого ритейла |
| 2026-06-11 | API-first архитектура: все данные доступны через REST API, вебхуки на ключевые события, API-ключи per-organisation. Интеграции: WhatsApp/Telegram (1-я очередь), банки/кассы/1С (2-я), маркетплейсы/ФНС (3-я) | Подготовить с первого дня — переделывать архитектуру под интеграции потом дорого |
| 2026-06-11 | Дополнительные каналы интеграции: Zapier/Make (no-code автоматизация), Email-to-action (накладная на почту → автозапись), глубокие ссылки (auron://...), CSV/JSON импорт-экспорт, SMS-уведомления (запасной канал), QR-коды для объектов | Каждый канал решает свою задачу: Zapier для нетехнических пользователей, email для безбарьерного приёма накладных, QR/deep links для быстрого доступа |
| 2026-06-11 | Мессенджеры — три уровня: (1) Web Share API — браузерный шейр без настройки, работает сразу; (2) wa.me/t.me ссылки — открывают нужный чат с готовым текстом; (3) API — Telegram Bot (бесплатный официальный), WhatsApp Business API (официальный), Green API (неофициальный шлюз, популярен в РФ). Базовый уровень включён по умолчанию | Web Share API = нулевой порог входа, пользователь делится с первого дня. Полная автоматика — апгрейд в настройках |
| 2026-06-11 | Auron — платформа, не приложение. Пять фундаментов: (1) модульная архитектура — каждая функция модуль on/off per-store; (2) шина событий — модули общаются через события, не прямые связи; (3) конфиг вместо кода — экраны/поля/отчёты в конфиге; (4) кастомные поля везде (JSONB); (5) движок прав — роли как наборы permissions, не фиксированные экраны | Это фундамент бесконечного роста: новые модули добавляются без перестройки старых. Цель: Auron Marketplace где сторонние разработчики создают модули для разных типов магазинов |
| 2026-06-11 | С первого дня обязательно заложить: feature flags per-user, event bus, кастомные поля (JSONB), реестр модулей, движок permissions, трекинг каждого события | Эти шесть основ нельзя добавить потом без переписывания половины кода |
| 2026-06-11 | Продукт переименован в **Auron Finance** и расширен до универсальной финансовой платформы — от личного бюджета до сети магазинов. Дух продукта: Zenmoney + бизнес-инструменты в одном | Один продукт для любого кто управляет деньгами — физлицо, фрилансер, малый бизнес, ритейл |
| 2026-06-11 | Архитектура продукта: универсальное ядро (счета, транзакции, категории, бюджеты, цели, аналитика) + подключаемые модули под каждый тип бизнеса/задачи. Way Market = первая реализация «Модуля розницы» (v1.0) | Ядро одинаково для всех, модули делают продукт специализированным под конкретную нишу |
| 2026-06-11 | Бизнес-модель: тарифы + пакеты, как у мобильных операторов. Бесплатное ядро навсегда. Один тариф под тип пользователя (Личный ₽0 / ИП ₽299 / Бизнес ₽590 / Ритейл ₽890 / Сеть ₽690×магазин) — включает нужные модули. Дополнительные пакеты докупаются сверху: ИИ ₽149–290, Мультиорг ₽390 | Простота выбора как у операторов — выбрал тариф под себя, докупил что нужно сверху |
| 2026-06-11 | Позиционирование: «Auron Finance делает работу бухгалтера за 30 минут вместо 4 часов и даёт аналитику которую раньше нужно было заказывать у аналитика». Для малого бизнеса = штатный бухгалтер-аналитик за ₽890/мес | Малый бизнес не может позволить отдельного аналитика — Auron Finance даёт его по цене кофе в день |
| 2026-06-11 | Навигация собирается из активных модулей. Ядро даёт 4 вкладки (Главная/Операции/Аналитика/Настройки). Каждый модуль добавляет свои вкладки. Модуль Розница добавляет: Касса/Поставщики/Персонал/Товары. Новый модуль = новые вкладки, старые не трогаются | Универсальная навигация без модулей + специализированная с ними — один принцип для всех типов пользователей |
| 2026-06-11 | Онбординг v1.0 — только для модуля Розница (Way Market). Онбординг для личного пользователя и ИП — следующие версии | Way Market = первый клиент, его онбординг в приоритете |
| 2026-06-11 | Синхронизация с банком — три уровня по образцу Zenmoney: (1) SMS-парсинг — бесплатно, работает на Android с любым банком; (2) CSV-импорт — бесплатно, работает везде; (3) Прямое API банка — Пакет ИИ, приоритет: Т-Банк → Сбер → Альфа. Дубликаты объединяются автоматически | SMS-парсинг = нулевой порог входа без соглашений с банками; прямой API = надёжность и real-time, стоит апгрейда |
| 2026-06-11 | Универсальное ядро — 5 базовых сценариев по образцу Zenmoney: быстрый ввод транзакции (FAB + нумпад), управление счетами, бюджет по категориям, цели/накопления, синхронизация с банком | Личный пользователь получает полноценный Zenmoney-like опыт на бесплатном тарифе |
| 2026-06-12 | Тихий старт: первые 14 дней — никаких советов и предупреждений кроме критических. Приложение наблюдает и учится. С 15-го дня — лёгкие подсказки (макс. 2/день). С 2-го месяца — полноценные инсайты | Новый пользователь не должен чувствовать себя под допросом. Советы имеют ценность только когда приложение уже понимает паттерны конкретного человека |
| 2026-06-12 | Правило трёх: одно и то же предупреждение закрыто 3 раза → приложение предлагает его отключить навсегда | Пользователь голосует действием — если закрывает, значит это ему не нужно |
| 2026-06-12 | Три уровня сложности интерфейса: Простой (авто, дни 1–14) → Стандартный (авто, с 3-й недели) → Расширенный (по выбору). ИИ сам предлагает переход на следующий уровень | Новичок не пугается сложностью, опытный пользователь получает всю мощь |
| 2026-06-12 | Полный контроль через настройки: каждое уведомление, предупреждение и ИИ-инсайт отдельно включается/выключается. Порог срабатывания предупреждений настраивается | Пользователь сам решает что ему показывать — приложение не навязывает |
| 2026-06-12 | Адаптивная Главная: карточки переставляются по частоте использования. Функция не используемая 30+ дней уходит вниз. ИИ раз в месяц предлагает скрыть неиспользуемое и вынести частое вперёд | Интерфейс отражает реальную работу конкретного человека, а не усреднённый шаблон |
| 2026-06-12 | Надёжность: автосохранение черновиков (30 сек), корзина 30 дней, история версий с диффом, полный офлайн-режим с очередью синхронизации, проверка целостности данных раз в день, чеклист закрытия месяца | Данные не теряются никогда. Ошибки перехватываются до того как попали в историю |
| 2026-06-12 | Вход: телефон → SMS-OTP (один раз при регистрации) → PIN 4 цифры → Face ID/Touch ID. Ежедневный вход = биометрия (1 касание). SMS используется только при регистрации и восстановлении | 86% пользователей ожидают биометрию. SMS только при первом входе — минимальный риск |
| 2026-06-12 | Финансовый пульс 🟢🟡🔴 закреплён в шапке постоянно на всех экранах | Пользователь видит состояние бизнеса без переходов — в любой момент |
| 2026-06-12 | Контекстная подсказка «обычно сейчас вы делаете X» появляется только после 20+ повторений одного действия в одно время. До этого — тишина | Советовать можно только когда точно знаешь паттерн пользователя |
| 2026-06-12 | Умные уведомления: пакетирование (макс. 3 в день), обучение по времени чтения, действие прямо из уведомления без открытия приложения | Уведомления которые раздражают — отключают. Умные уведомления — читают |
| 2026-06-12 | Доверие и прозрачность: тап на любое число → формула расчёта. ИИ показывает уровень уверенности в прогнозах | Пользователь доверяет числам когда понимает откуда они берутся |
| 2026-06-12 | Доступность: поддержка крупного текста без поломки интерфейса, дублирование цвета формами для дальтоников, упрощённый режим для нетехнических пользователей | Охватываем максимум пользователей, включая 8% мужчин с дальтонизмом |
| 2026-06-12 | Auron Wrapped: годовой итог 31 декабря — ключевые цифры года, лучший месяц, Auron Score. Можно поделиться | Момент гордости и эмоциональной привязки к продукту |
| 2026-06-12 | Планшет/ПК: два столбца (список + детали одновременно), горячие клавиши (N/Z/P//), режим печати для отчётов | Бухгалтер на компьютере — другой контекст работы, нужен другой интерфейс |
| 2026-06-12 | Краш-тест логики: бесплатный тариф = только ядро (счета, транзакции, категории, базовая аналитика). Касса/поставщики/персонал — тариф Ритейл ₽890 с триалом 14 дней. Экран Подписки приведён к Видению | Старое решение «бесплатно = лучше Excel» противоречило операторской модели — операторская победила |
| 2026-06-12 | Тариф Ритейл = 1 магазин. 2 магазина = Ритейл + Мультиорг ₽390. От 3 магазинов = тариф Сеть ₽690/магазин | «До 10 магазинов» за ₽890 делало тариф Сеть бессмысленным |
| 2026-06-12 | Цели ставит и меняет Владелец (единственное что он создаёт). Бухгалтер видит и работает на цель | Это его бизнес и его планы — таблица ролей исправлена |
| 2026-06-12 | Формула расхождения: `(Факт + Выплаты) − Z-отчёт`. Недостача с минусом 🔴, излишек с плюсом 🟡, ноль ✓. Иман — тип выплаты, в формуле не дублируется | Старая формула давала недостачу с плюсом и считала Иман дважды |
| 2026-06-12 | Навигация на телефоне: максимум 5 вкладок. Розница: Главная · Касса · Поставщики · Финансы · Ещё (Операции/Персонал/Товары/Настройки). Настройки также через ⚙️ | 7 вкладок физически не влезают в нижнюю панель |
| 2026-06-12 | SMS-парсинг убран из v1 — PWA технически не может читать SMS. Банковская синхронизация v1: CSV-импорт (бесплатно) + прямое API банка (Пакет ИИ). SMS-парсинг вернётся с нативным Android-приложением | Честность к себе: не обещаем невозможное |
| 2026-06-12 | Главная зависит от роли: Бухгалтер — полная, Администратор — без сумм кассы, Сотрудник зала — только заказы и поставки, Владелец — режим просмотра | Администратор не имеет права видеть кассу — Главная не должна её показывать |
| 2026-06-12 | Создан банк идей `07-IDEAS.md` — все одобренные идеи в одном месте, реализованное переносится в основные документы | ~50 одобренных идей жили только в чате — потерялись бы |
| 2026-06-12 | Цвета прогресс-баров зависят от смысла: накопления — больше=зеленее, бюджет расходов — больше=краснее | Потратить 90% бюджета — плохо, а показывалось зелёным |
| 2026-06-12 | Накопления: каждый магазин сам выбирает метод — виртуальный резерв (деньги остаются в кассе, помечаются) или физический счёт (реальный перевод в банк). Оба варианта одинаково отражаются в аналитике | Разные магазины — разный cash-flow; универсальный выбор лучше навязывания одного способа |
| 2026-06-12 | Карта/СБП выручка зачисляется на банковский счёт в приложении (отдельно от Наличных). Комиссия банка фиксируется как статья расходов | Наличка и безнал — разные счета в реальном магазине; смешивать их было бы ошибкой |
| 2026-06-12 | Ночная смена и дата Z-отчёта: настраивается на магазин — привязывать к дате начала смены или к дате окончания. По умолчанию: дата начала | Разные магазины работают с разной логикой; правильного ответа нет — нужен выбор |
| 2026-06-12 | Иман в аналитике = категория расходов. Пользователь называет категорию сам при первой настройке (по умолчанию «Хозрасходы владельца») | Иман — выплата с кассы, должна отражаться в расходах как любая другая статья |
| 2026-06-12 | Два бухгалтера: оба получают одно напоминание. Один выполнил задачу → исчезает у обоих | Дублирования работы нет; принцип «первый выполнил — закрыто для всех» |
| 2026-06-12 | Сотрудник в нескольких магазинах сети: один телефон = доступ в несколько магазинов. При входе выбирает магазин из списка | Распространённая ситуация в сети; один аккаунт удобнее чем несколько |
| 2026-06-12 | Согласие с условиями на регистрации: текст-ссылки на Условия использования и Политику конфиденциальности. Без принятия — регистрация невозможна | Юридическое требование; минимально ненавязчивый способ |
| 2026-06-12 | Возврат поставщику: кнопка [↩ Возврат] в карточке поставщика рядом с Выплатить/Накладная. Уменьшает долг (или создаёт переплату) | Кнопки не было — бухгалтер не знал как оформить возврат |
| 2026-06-12 | Списания (продукт, расходники): вводятся через нумпад FAB с предустановленной категорией «Списание». Физический товар остаётся в 1С, в приложении — только финансовая сторона | Один инструмент для всего — нумпад уже знают, отдельный экран не нужен |
| 2026-06-12 | Смена номера телефона: в Настройках → Аккаунт. Требует SMS-верификацию нового номера + повторный вход. Старый номер аннулируется | Кейс при смене SIM-карты; без верификации нового номера — риск потери доступа |
| 2026-06-12 | Удаление аккаунта: только Владелец. Обязательный экспорт перед удалением (не блокирует, но предлагается). Все данные удаляются через 30 дней льготного периода | Право пользователя на удаление; 30 дней — страховка от случайного удаления |
| 2026-06-12 | Вход с компьютера: биометрия недоступна → PIN-код. При поддержке браузером WebAuthn/passkeys — используется. По умолчанию: поле ввода PIN 4 цифры | Компьютер не имеет Face ID; отдельное решение для веба |
| 2026-06-12 | Онбординг: пресеты категорий расходов для магазина (Закупка/ЗП/Аренда/Коммуналка/Налоги/Хозрасходы/Реклама/Прочее) предустановлены автоматически на шаге 2, можно настроить | Новый пользователь не должен создавать категории с нуля — стандартный набор подходит 90% магазинов |
| 2026-06-12 | v1 — только рубли. Мультивалютность добавлена в банк идей (`07-IDEAS.md`) | Сложность мультивалюты не оправдана для v1: 99% пользователей Way Market работают в рублях |
| 2026-06-12 | Именование: «Доступно» (ядро) = баланс минус бюджеты/цели; «Свободно» (Розница) = наличка в кассе минус выплаты минус резервы накоплений | Разные формулы — разные термины; смешивать нельзя |
| 2026-06-12 | Офлайн-режим: только просмотр. Ввод операций при потере интернета заблокирован. Service Worker кэширует последние данные, показывает время последнего обновления | Избегаем конфликтов синхронизации: если офлайн нельзя писать — конфликтов нет |
| 2026-06-12 | Реальное время для 3+ одновременных пользователей: Supabase Realtime (WebSocket) транслирует изменения всем устройствам организации мгновенно | В одном магазине одновременно работают кассир, бухгалтер, владелец — данные должны быть синхронны без ручного обновления |
| 2026-06-12 | История операций хранится бессрочно, никогда не удаляется автоматически. Soft delete: записи получают `deleted_at`, баланс корректируется, данные остаются | Бухгалтерские данные — юридически значимые документы. Потеря данных недопустима |
| 2026-06-12 | Схема БД — профессиональная: двойная запись (journal_entries/journal_lines), неизменяемый реестр (исправление только сторно-проводкой), закрытые фискальные периоды, роли как таблицы (не enum), полиморфные файлы и комментарии. 69 таблиц в 15 доменах | Стандарт банков и бухучёта: баланс сходится математически, аудит безупречен, налоговая через 3 года видит те же данные |
| 2026-06-12 | Человеческий фактор в схеме: запросы на исправление закрытых периодов (с одобрением Владельца), споры по записям, временные замещения с автоистечением, срочный доступ при потере телефона | Реальная жизнь магазина: люди болеют, ошибаются, спорят, теряют телефоны — система должна иметь процесс для каждого случая |
| 2026-06-12 | Антифрод с первого дня: настраиваемые правила (всплески сумм, ночные операции, дубликаты) + сигналы с уровнем серьёзности | Внутреннее воровство — реальная проблема розницы; ИИ-инсайты слишком медленные для этого |
| 2026-06-12 | Форс-мажоры в схеме: инциденты (ограбление/потоп/отключение), периоды-исключения для аналитики (ремонт/локдаун не ломают сравнения), закрытия магазина с уведомлением поставщиков | Через год должно быть понятно почему в ту среду не было выручки; аномальные месяцы не должны искажать прогнозы |
| 2026-06-12 | Краш-тест документации (3 параллельные проверки: сценарии/экраны/решения vs схема) → исправлено: PIN и настройки в users, тип выплаты + Иман в shift_withdrawals, механизм отмены Z-отчёта, авансы в расчёте ЗП, статьи накоплений = goals c type=reserve_item, +4 таблицы (user_notification_preferences, generated_reports, pulse_snapshots, custom_field_definitions). Итог: 75 таблиц | Схема проверена против каждого сценария и экрана до написания кода — дешевле чем после |
| 2026-06-12 | Разрешено противоречие soft delete vs неизменяемый реестр: финансовый реестр (journal) — неизменяемый, только сторно; бизнес-объекты (transactions, debt_entries) — deleted_at + корзина 30 дней + автосторно в реестре; документы (shifts, orders) — status=cancelled | Пользователь получает привычную корзину, бухгалтерия получает безупречный аудит — оба мира совместимы |
| 2026-06-12 | Формула Финансового пульса: выручка vs средняя (30%) + долги vs лимит (30%) + накопления vs план (20%) + расхождения за неделю (20%). Пересчёт при закрытии смены, история в pulse_snapshots | Пульс был концепцией без формулы — теперь вычислим и тестируем |
| 2026-06-12 | Файлы физически хранятся в Supabase Storage с RLS-политиками на бакеты | Тот же проект что и БД: одна инфраструктура, одна модель доступа |
| 2026-06-12 | Позиционирование для «старой школы»: Auron = управленческий учёт (электронная тетрадка), не фискальный сервис. Никуда ничего не передаём; интеграция с ФНС только по явному желанию. Функции для сокрытия оборотов от налоговой — не делаем | Честность: облачный сервис не может обещать невидимость от государства; зато приватность от всех остальных — даём по максимуму |
| 2026-06-12 | Облако пользователя, 3 уровня: (1) v1 — автобэкап зашифрованного архива в любое облако (Яндекс.Диск, VK Облако, СберДиск, Google Drive, WebDAV), шифрование паролем пользователя ДО отправки; (2) v1.5 — файлы чеков/накладных в облаке пользователя; (3) v2+ — своя база self-hosted на российском облаке (тариф Сеть/Enterprise). Таблица cloud_backup_connections в схеме | «Ваша копия лежит у вас» — главный аргумент для недоверчивых к облакам; российские облака в приоритете + помогает с 152-ФЗ |
| 2026-06-12 | `05-ARCHITECTURE.md` утверждён владельцем (76 таблиц, 15 доменов, прошёл краш-тест). Следующий этап: завершение `04-DESIGN-SYSTEM.md` — последний документ перед началом кода | Все вопросы данных, синхронизации, офлайна и структуры кода закрыты |
| 2026-06-12 | Код не пишем, пока всё досконально не изучено и не обсуждено — этапы проектирования до конца | Подтверждение владельцем главного правила проекта |
| 2026-06-12 | Два стиля оформления на выбор пользователя: «Градиент» (по умолчанию — плитки со свечением, как в прототипе) и «Классика» (плоский iOS-стиль). Одни токены, один CSS-класс на body. Хранится в users.preferences | Слой «визуал» персонализации: современный стиль большинству, строгий — «старой школе» |
| 2026-06-12 | Тёмная тема v1: авто по системе + ручной выбор (Авто/Светлая/Тёмная) в настройках | Дёшево реализовать, часто просят |
| 2026-06-12 | Shake to Undo убран из v1 в банк идей; вместо него — кнопка «Отменить» в Toast (5 сек) | На iOS требует разрешения на датчики, в браузере нестабилен; Toast надёжнее |
| 2026-06-12 | Дизайн-система согласована с архитектурой: офлайн-раздел исправлен на «только просмотр + локальные черновики», иконки категорий = SVG-спрайт из прототипа (не эмодзи) | Краш-тест нашёл противоречия 04 ↔ 05; архитектура главнее |
| 2026-06-12 | Звуков нет — только хаптика. Язык: русский v1, но все тексты в словаре ru.json (i18n-ready с нуля). Плотность: переключатель Просторно/Компактно в персонализации, по умолчанию просторно | Звуки неуместны в торговом зале; словарь бесплатен сейчас и спасает от переписывания потом; компактность — для бухгалтера с 50 заходами в день |
| 2026-06-12 | Краш-тест окон (2 проверки: сценарии↔экраны, полнота состояний) → в 03-SCREENS добавлено: глобальные правила состояний (загрузка/пусто/офлайн/ошибка для каждого экрана), матрица экран↔роль, экран Корзины, закрытие месяца с чеклистом, запрос исправления закрытого периода, продажи и импорт 1С в Товарах, ИИ-порядок выплат, Резервные копии, развёрнутая Персонализация (стиль/тема/плотность), методы накоплений и дата ночной смены в Настройках. Удалён дубль блока «Уведомления» | Сценарии и архитектура описывали функции, для которых не было окон — найдено 8 критичных пробелов до написания кода |
| 2026-06-13 | Роль «Кассир» — не нужна. Финальный список ролей: **Бухгалтер / Администратор / Владелец / Сотрудник зала** | Auron Finance — инструмент управления и учёта, не кассовая программа. Кассиры работают на онлайн-кассе (1С, АТОЛ) — в Auron они не нужны |
| 2026-06-13 | **Supabase Cloud заменяется на российский backend**: PostgreSQL + PostgREST + Realtime + Storage, всё self-hosted на российском облаке. Приоритет: Яндекс Cloud / Timeweb Cloud / VK Cloud. Конкретное облако выбирается перед началом кода | Данные российских магазинов должны храниться на российских серверах (152-ФЗ). Supabase Cloud — американская компания. Self-hosted = сохраняем весь стек (Auth, Realtime, Storage, PostgREST), меняем только где это работает |
| 2026-06-13 | PIN-код: **4 цифры** | Стандарт смартфонов, знакомый каждому пользователю |
| 2026-06-13 | Автоблокировка: **1 минута по умолчанию**, настраивается в Настройки → Безопасность по аналогии со смартфоном: 30 сек / 1 мин / 2 мин / 5 мин / Никогда | UX который пользователи уже знают — не нужно объяснять |
| 2026-06-13 | **5 неверных PIN → аккаунт блокируется**. Разблокировка только через ссылку на email | Защита от подбора. Email-ссылка — простой recovery без поддержки |
| 2026-06-13 | **`04-DESIGN-SYSTEM.md` утверждён владельцем**. Ключевые решения: два стиля (Градиент/Классика), акценты (#30D158/#FF453A/#FF9F0A/#5E5CE6), нумпад со свайпом и калькулятором, 5 вкладок (Главная·Касса·Поставщики·Финансы·Ещё), переключатель плотности. Следующий этап: код | Все документы утверждены — можно писать код |
| 2026-06-13 | **Облако: Timeweb Cloud** (Москва). Сервер 201.51.6.170 — self-hosted Supabase работает. Студия: `http://201.51.6.170:8000` (login: supabase). Порт 5432 закрыт файрволом | Российское облако (152-ФЗ), цена адекватная, настройка прошла успешно. Яндекс Cloud и VK Cloud как резерв |
| 2026-07-10 | **Каталог товаров для сотрудников** — отдельная страница по ссылке без регистрации: умный поиск (опечатки, часть слова, по коду), группы товаров, карточка с фото (несколько), кодом кассы, артикулом, штрихкодом. Редактирует только владелец через один админ-аккаунт. Цены и остатки не показываются. Детали — `docs/08-CATALOG.md` | У товаров без штрихкода (бургеры, хот-доги, выпечка) кассиры и подменяющие их сотрудники путают коды — каталог убирает путаницу. Бесплатно: GitHub Pages |
| 2026-07-10 | Каталог — **полностью отдельное приложение с отдельной базой данных**: свой репозиторий, своя ссылка, свой вход админа. С Auron Finance не смешивается ни кодом, ни базой | Требование владельца: каталог живёт независимо — переписывание и обновления Auron его не затрагивают |
| 2026-07-10 | Каталог: админ может **изменять и удалять всё сохранённое** (название, коды, группу, фото — добавить/удалить/заменить, товар целиком) одинаково с телефона и компьютера. Без входа в админ-аккаунт — только просмотр | Владелец заполняет и правит каталог сам с любого устройства; сотрудники изменить ничего не могут |
| 2026-07-10 | Владелец делегировал решения по логике, дизайну и архитектуре каталога. Принято: база — бесплатный отдельный проект Supabase Cloud; название «Way Market · Каталог»; 8 стартовых групп (правятся в приложении); поле «Отдел» необязательное; поиск с опечатками работает на телефоне мгновенно + офлайн-кэш последней версии каталога | «Продумай сам всё за меня» — решения зафиксированы в `docs/08-CATALOG.md` (✅ утверждён) |
| 2026-07-10 | Код каталога написан: `catalog/` (index.html, styles.css, js/app.js, js/config.js) + `catalog/setup/` (schema.sql — создание базы одним запуском, НАСТРОЙКА.md — инструкция владельцу на 10 минут). Деплой добавлен в workflow → `…/Auron/catalog/` | Отдельное приложение готово; для запуска владельцу осталось создать бесплатный проект Supabase и прислать 2 ключа |
| 2026-07-10 | Каталог, дополнения владельца: (1) поиск рус↔англ через транслитерацию («сникерс» ↔ Snickers); (2) весовые товары — отметка «⚖ Весовой» + отдельный фильтр; (3) сканер штрихкода доступен ВСЕМ сотрудникам (кнопка камеры у поиска → карточка товара), работает на Android и iPhone; (4) дизайн: крупный текст и большие кнопки — для пользователей любого возраста | Кассиры вводят как привыкли (рус/англ); у весовых товаров свои коды; сканер — самый быстрый способ найти товар с полки |
| 2026-07-10 | Каталог: **поставщики**. У товара — поле «Поставщик» (ведёт админ, как группы). Сотрудник находит товары поставщика тремя путями: поиск по имени, чип «🚚 Поставщики» со списком, кнопка «все товары поставщика» в карточке товара | Просьба владельца: сотрудники зала должны видеть, от какого поставщика пришёл товар, и весь его ассортимент |
| 2026-07-10 | Каталог: **импорт из 1С** встроен в приложение (меню админа). Источник — отчёт «Текущие цены поставщиков» (+ опционально «Штрихкоды номенклатуры» для всех штрихкодов). У товара теперь несколько штрихкодов и несколько поставщиков (миграция `setup/ОБНОВЛЕНИЕ-1.sql`); весовые определяются по ед. «кг»; повторный импорт обновляет по коду, фото сохраняются. Производительность на 16,5 тыс. товаров: индекс поиска, страницы по 80 карточек, группы топ-12 + список с поиском | Владелец прислал реальную выгрузку: 16 537 товаров, 196 групп, 621 поставщик — каталог заполняется за минуты, а не месяцы вручную |
| 2026-07-11 | Каталог, предложение (🔄 ждёт «да» владельца): **заказы поставщикам и сравнение цен**. Вход сотрудника по общему паролю → видны цены поставщиков (из импорта 1С, с датой), лучшая подсвечена «✓ выгоднее»; контакты поставщика (телефон, WhatsApp); заказ собирается кнопкой «＋ В заказ», группируется по поставщикам, отправляется в WhatsApp или звонком; черновик автосохраняется в базу — переживает выход из приложения и смену телефона. Дизайн — по исследованиям удобства (Нильсен, Baymard, законы Фиттса/Хика): нижняя панель из 3 вкладок, крупные кнопки. Детали — `docs/08-CATALOG.md` | Просьба владельца: сотрудники должны сами правильно заказывать у выгодного поставщика; цены нельзя показывать всем — ссылка публичная, поэтому вход |
| 2026-07-11 | Каталог: владелец сократил предложение — **заказы внутри приложения и автосохранение черновиков заказа НЕ делаем**. Остаются: вход сотрудника по общему паролю, сравнение цен поставщиков (из импорта 1С, лучшая подсвечена, с датой), контакты поставщиков (телефон, WhatsApp, контактное лицо) | Решение владельца: сотрудникам достаточно видеть, у кого дешевле, и связаться с поставщиком напрямую — сам заказ оформляется как привыкли |
| 2026-07-11 | Каталог, владелец утвердил («Делай 2, 8») и код написан: **сравнение цен с историей + единица продажи**. Вход сотрудника — только пароль магазина (служебный аккаунт staff@waymarket.ru); цены поставщиков из импорта 1С копятся с датами (стрелки ↑/↓ % и история по тапу, лучшая цена «✓ выгоднее»); контакты поставщиков (телефон → звонок/WhatsApp) — отдельная таблица, видна после входа; единица продажи (шт/кг/упак) из колонки «Ед.»; права: менять каталог может только админ из списка catalog_admins. Миграция `catalog/setup/ОБНОВЛЕНИЕ-2.sql` + инструкция в НАСТРОЙКА.md | Из банка идей владелец выбрал №2 (единица продажи) и №8 (история цен); заказы и черновики отклонил ранее. История цен показывает, когда поставщик поднял цену — пора сравнивать |
| 2026-07-11 | Каталог, просьбы владельца, код написан: (1) **продажи и «Ходовые товары»** — импорт отчёта «Продажи» из 1С (по дням из файла или дата задаётся вручную), экран топа для всех вошедших: сегодня / 7 дней / 30 дней / свой период, топ считает база (RPC), не телефон; (2) **вход запоминается** на устройстве и продлевается сам до нажатия «Выйти»; (3) **50 сотрудников через один аккаунт** staff@waymarket.ru — у каждого устройства свой сеанс, в НАСТРОЙКА.md предупреждение не включать «Single session per user». Миграция `catalog/setup/ОБНОВЛЕНИЕ-3.sql` | Владелец хочет видеть ходовые товары по периодам и дать каталог всей смене без возни с аккаунтами; продажи закрыты входом — ссылка публичная |
| 2026-07-11 | Каталог, решение владельца: **кнопка «Я администратор» убрана — форма входа одна для всех**, права программа определяет по аккаунту (список catalog_admins). Сотрудник вводит только пароль магазина; админ — свой пароль, email спрашивается один раз и запоминается на устройстве, дальше подставляется сам | «Программа должна знать, у кого какие права» — различие ролей по аккаунту уже было в базе, теперь и вход не заставляет выбирать, кто ты |
| 2026-07-11 | Каталог, владелец выбрал из предложенных доработок (отклонил: бэкап-кнопку, «снят с продажи», плашку устаревших данных, маржу, второго админа): (1) **экономная история цен** — функция базы `catalog_save_prices` пишет цену только при изменении, иначе ежедневный импорт дал бы ~6 млн строк/год (проверено на локальном Postgres); (2) **смена пароля магазина из приложения** — меняет пароль staff-аккаунта и выкидывает все устройства сотрудников (для увольнений), плюс кнопка «только выгнать устройства»; (3) **PWA** — манифест, иконки, офлайн-скрипт «сначала сеть»: установка на главный экран, работа без связи; (4) **кэш цен и контактов на телефоне** — цены открытых товаров видны офлайн, стираются при выходе. Миграция `catalog/setup/ОБНОВЛЕНИЕ-4.sql` | База не должна расти зря; общий аккаунт безопасен только если при увольнении можно мгновенно отрезать старые входы; сотрудникам удобнее «приложение», чем закладка |
| 2026-07-11 | Каталог, уточнения владельца: (1) **иконка и логотип — фирменный знак Way Market** (тележка WAY с фруктами, зелёный круг, линии скорости) — владелец прислал картинку; из неё сделаны иконки приложения 192/512/apple и круглый логотип в шапке вместо эмодзи; (2) **дата у цены = дата последнего поступления** товара у поставщика с этой ценой (колонка даты из отчёта 1С): та же цена + свежее поступление → обновляется только дата; новая цена → новая строка истории. Проверено на локальном Postgres | Колонка даты в отчёте «Цены поставщиков» — это дата последнего прихода товара; владельцу важно видеть, когда реально возили товар по этой цене |
| 2026-07-12 | Каталог: **прогон дизайна по всем экранам** на реальных данных (16,5 тыс. товаров, Playwright-скриншоты, светлая+тёмная тема, телефон и компьютер). Исправлено: карточки без фото — компактные (пока фото нет ни у одного товара, сетка вмещает вдвое больше); кнопка входа — «Войти» с текстом вместо безликого человечка (после входа сжимается в значок); логотип на экране входа; пустое офлайн-состояние с подсказкой вместо белого экрана; отклик кнопок на нажатие; ограничение ширины на компьютере; курсор-«рука» только у строк с историей цен | Просьба владельца «доработай экраны и дизайн»: смотрели на живое приложение, а не на код — все правки от увиденных проблем |
| 2026-07-12 | Каталог, просьба владельца: **автопоиск фото по штрихкоду**. Источник — открытая всемирная база Open Food Facts (+ Open Beauty Facts для химии): проверка на 40 реальных штрихкодах магазина дала ~20% попаданий (известные бренды). Два инструмента админа: массовый «🖼 Фото по штрихкодам» (прогресс, пауза/продолжение в любой день, проверенные штрихкоды запоминаются) и «Найти фото в интернете» в карточке товара. Найденное фото скачивается, сжимается и сохраняется в НАШЕ хранилище — работает офлайн и не зависит от чужого сайта. Весь путь проверен e2e-тестом в браузере | Фото нет ни у одного из 16,5 тыс. товаров — вручную фотографировать месяцы; автопоиск бесплатно закроет ~2–3 тыс. позиций известных брендов |
| 2026-07-12 | Каталог, просьбы владельца — три доработки: (1) **минимализм в группах**: 202 группы 1С автоматически раскладываются по ~13 понятным категориям (Напитки, Молочное, Сладости, Дом и химия…) по названию группы; верхний ряд — категории, второй ряд — подгруппы выбранной категории. Классификатор проверен на всех 202 группах. (2) **Фото из Excel по ссылкам**: админ грузит Excel (ссылка на фото + штрихкод/код/название), товары находятся по штрихкоду→коду→точному названию, фото показывается по ссылке (не перезаливается). (3) **Контакты поставщика в карточке товара**: после входа под поставщиком — имя, «📞 позвонить», «💬 WhatsApp», примечание + «показать все товары». Логика проверена тестами и скриншотами | Владелец: «минимализм в группах», «через эксель… по штрихкоду… показывал фотографию», «когда нажимаю на поставщика показывал его данные — позвонить или ватсап» |
| 2026-07-12 | Каталог, «умный каталог для анализа продаж»: в карточке товара после входа — блок **«Продажи»** в штуках (не деньгах): за 7 дней · за 30 дней · среднее в день. Считается из загруженных продаж (catalog_sales), кэшируется на телефоне (видно офлайн, стирается при выходе), не продавался за месяц — блок скрыт. Прямо помогает решить, сколько заказывать | Владелец: «умный каталог где можно анализировать продажи товара не денег… для заказа, администратора и сотрудников зала» |
| 2026-07-12 | Каталог, **скорость и свежесть данных** (жалоба: пустой экран и долгая загрузка каждый раз): найдена корневая причина — каталог 16,5 тыс. товаров весит ~8 МБ, а localStorage вмещает ~5 МБ, поэтому кэш ТИХО не сохранялся и каждый заход грузил все 17 страниц заново. Исправлено: (1) кэш перенесён в **IndexedDB** (сотни МБ) — реально сохраняется, заход мгновенный, работает офлайн; (2) **докачка** (delta-sync) — при заходе тянутся только изменившиеся товары (по updated_at) + сверка количества для удалений; обычно 2 запроса вместо 17; (3) **прогрессивная загрузка** — первая страница показывается сразу (~0,3 с), остальное догружается в фоне; загрузка страницами по id (индекс PK) вместо сортировки по имени на сервере; (4) библиотека Supabase лежит локально (не с CDN) — быстрее старт и офлайн. Индекс `idx_products_updated` (ОБНОВЛЕНИЕ-5.sql). Проверено в браузере: 1-й заход — первая карточка 0,3 с; повторный — 2 запроса вместо 17 | Владелец: «когда захожу каталог пустой и долго грузится… чтобы не зависало, данные актуальные и быстро» |
| 2026-07-12 | Каталог, просьба владельца: **единый список поставщиков и цен в карточке товара + кликабельные поставщики**. В карточке — раздел «Поставщики и цены»: ВСЕ поставщики товара (объединение supplier_ids и тех, у кого есть цена), у каждого — цена и дата последнего поступления по этой цене, самая низкая помечена «✓ выгоднее», стрелка тренда; поставщик без цены — «цена не указана». Строка кликабельна → карточка поставщика: контактное лицо, две крупные кнопки «📞 Позвонить» (tel:) и «💬 WhatsApp» (wa.me), телефон, примечание, цена+дата, история цены, «показать все товары», у админа — «изменить контакты». Прежние два раздельных блока (цены + контакты всплошную) объединены в один. Безопасность прежняя: цены и контакты только после входа (RLS). Найден и исправлен баг — коллизия имён (новая функция называлась как существующая renderSupplierList → переименована в renderCardSuppliers). Логика покрыта unit-тестами, экраны проверены e2e-скриншотами | Владелец: «показывать все цены от всех поставщиков и дату последнего поступления, поставщики кликабельны → контакты и две кнопки позвонить/ватсап» |
| 2026-07-12 | Каталог, жалоба владельца «жесты не работают, кнопки назад нет»: добавлены мобильные жесты для окон (шторок). (1) **Кнопка «назад» телефона** и жест назад закрывают открытое окно, а не выходят из приложения — через History API (стек окон, pushState при открытии, popstate закрывает верхнее). (2) **Смахивание окна вниз** закрывает его (drag по шторке, порог 90px; не мешает прокрутке контента, кнопкам, листанию фото). (3) **Стрелка «‹ назад»** в левом верхнем углу каждого окна (вставляется автоматически во все шторки). Проверено Playwright: назад-телефон/стрелка/смахивание закрывают, прокрутка страницы восстанавливается | Владелец на телефоне ждал привычных жестов; их не было — окна закрывались только кнопкой «Закрыть» |
| 2026-07-12 | Каталог, **баг: повторный импорт двоил товары** — товары без кода кассы каждый импорт вставлялись заново (было 16 537 → стало 33 230). Исправлено: при импорте товары без кода находятся по штрихкоду/названию в существующих и ОБНОВЛЯЮТСЯ (upsert по id), а не вставляются; код не затирается. Для чистки уже появившихся дублей — `setup/УБРАТЬ-ДУБЛИ.sql` (оставляет один товар на имя+штрихкоды, приоритет строке с кодом; разные товары с одинаковым именем но разными штрихкодами не трогает). SQL проверен на локальном Postgres | Владелец: «каталог при загрузке снова эксель начал двоиться» |
| 2026-07-12 | Каталог, просьба «программа сама искала фото новых товаров без меня»: **автопоиск фото** — приложение само в фоне ищет фото товаров без картинок (Open Food Facts по штрихкоду) и сохраняет их. Запускается автоматически при входе владельца, после каждого импорта, при возврате на вкладку и появлении сети; продолжается между заходами (проверенные штрихкоды запоминаются), тихо приостанавливается когда вкладка скрыта/нет сети. Только для аккаунта владельца (не общий staff), чтобы телефоны сотрудников не дёргали внешнюю базу. Кнопка ручного поиска сохранена | Владелец хочет, чтобы фото подтягивались сами, без нажатий и без моего участия |
| 2026-07-12 | Каталог, реальный файл продаж из 1С: отчёт «Продажи» **агрегирован за период** (в шапке «Период: 01.06–12.07»), без разбивки по дням, товары — по названию (кода/штрихкода нет). Переделал модель продаж на **периоды**: catalog_sales = товар × период (period_from/to) × количество × сумма; RPC catalog_top_products(period) и catalog_sales_periods(). Парсер берёт период из шапки, колонку «Сумма продажи» (не «приходную»), пропускает строки «Итого». Экран «Ходовые» — чипы загруженных периодов, показывает N шт и ≈/день. Карточка товара — продажи за последний период. Проверено на реальном файле (6788 позиций, период 01.06–12.07, топ: яйца/вода). Обновлены ОБНОВЛЕНИЕ-3.sql, schema.sql, ВСЕ-ОБНОВЛЕНИЯ.sql | Владелец прислал реальный отчёт продаж — приложение должно его понимать |
| 2026-07-12 | Каталог, «чтобы чистка дублей шла сама»: дедуп товаров теперь **автоматический** — тихо после каждого импорта и при входе владельца (без вопросов), плюс ручная кнопка «🧹 Убрать дубли» в меню. Оставляет один товар на имя+штрихкоды (приоритет строке с кодом), удаляет остальные пачками через приложение (права владельца). Только для аккаунта владельца | Владелец не хочет запускать чистку вручную каждый раз |
| 2026-07-13 | Каталог, просьбы владельца: (1) **три роли доступа**: главный админ (загрузка и правка каталога, все цены, аналитика) · аналитик/зал (закупочные и розничные цены, контакты, аналитика; без загрузки) · кассир (только товар и розничная цена, без закупочных цен и контактов). Роль хранится в БД (`catalog_roles`), определяется после входа (`catalog_my_role()`); закупочные цены/контакты закрыты правами (`catalog_can_purchase()` = admin+manager), аналитика «Ходовые» — тоже admin+manager. Служебные аккаунты (кассир, аналитик) подбираются по паролю при входе (config `SERVICE_EMAILS`). (2) **Розничная цена** (цена на полке) — из колонки файла импорта («Розничная»/«Цена продажи»/«Продажная»), хранится в `catalog_products.retail_price`, видна ВСЕМ (крупно в карточке и на плитке товара). Отдельно от закупочных цен поставщиков. ОБНОВЛЕНИЕ-7.sql; schema.sql обновлён; роли и права проверены на локальном Postgres, все три роли — Playwright-тестами (кассир не видит закупки/аналитику, аналитик и админ — видят; розница видна всем) | Владелец: «три аккаунта — главный админ, аналитик/зал, кассир»; «показывать и приходные (закупочные) и розничные цены — розничная по которой продаём в магазине» |
| 2026-07-13 | Каталог, просьба владельца — **«разведка цен» (сравнение с другими магазинами)**: сотрудник в чужом магазине сканирует штрихкод (или находит товар вручную/в каталоге), выбирает магазин-конкурент или создаёт новый и вписывает их розничную цену. В карточке товара — блок «Цены в других магазинах»: наша розничная цена и цены конкурентов с датой внесения и пометкой «у них дешевле/дороже на N ₽». Вести разведку может любой вошедший сотрудник (в т.ч. кассир). Таблицы `catalog_competitors` и `catalog_competitor_prices` (одна актуальная цена на товар × магазин, права: читать/вносить — все вошедшие, удалять — админ). ОБНОВЛЕНИЕ-8.sql; schema.sql обновлён; весь путь (открыть карточку → создать магазин → внести цену → сравнение) проверен Playwright-тестом, SQL — на локальном Postgres | Владелец: «зайти в другой магазин, сканировать штрихкод, проверить есть ли у нас, сравнить их розничную цену с нашей; каталог магазинов с их ценами и датой» |
| 2026-07-13 | Каталог, просьбы владельца — цены и математика: (1) **устаревшие цены не «выгодные»**: цена поставщика старше 30 дней (нового поступления не было) больше не участвует в выборе «✓ выгоднее» и помечается «⚠ цена от ДД.ММ · поступления не было» (порог — константа STALE_PRICE_DAYS). Причина: без нового прихода цена у поставщика могла измениться, показывать её как выгодную нельзя. (2) **Продажи**: загрузка сохраняет через upsert по (товар, период) — новый период добавляется, тот же период обновляется, дубли не плодятся (проверено на Postgres). (3) **Фикс математики «Ходовых»**: топ считался по всем ВЛОЖЕННЫМ периодам → при пересекающихся периодах продажи двоились; теперь считается РОВНО по выбранному периоду (ОБНОВЛЕНИЕ-9.sql). Проверено: 01.06–12.07 = 450 (а не 750). Дни/в-день, тренды %, разница с конкурентом — перепроверены, верны | Владелец: «старые цены (месяц-два) не давать как выгодные — поступления не было»; «продажи сохранялись, новые добавлялись, старые обновлялись; проверь математику и логику» |
| 2026-07-13 | Каталог, просьбы владельца: (1) **импорт контактов поставщиков из 1С** (справочник «Контрагенты»: название + телефон) — меню админа «📇 Импорт контактов»: поставщик по названию (нет — создаётся), телефон upsert по supplier_id (накапливаются/обновляются). Парсер проверен на реальных файлах (636 контрагентов, 483 телефона). (2) **Фото товара может добавить любой вошедший сотрудник** (не только админ): кнопка «📷 Добавить фото» в карточке (камера на телефоне) → загрузка в Storage (право insert открыто вошедшим) → дозапись в photos через `catalog_add_photo` (security definer, сам товар правит только админ). ОБНОВЛЕНИЕ-10.sql. Несколько фото накапливаются. Проверено Playwright (кассир добавил фото) и на Postgres (RPC дописывает массив) | Владелец: «пусть я могу добавить контакты поставщиков файлом»; «все аккаунты / сотрудники могут добавить фото товара» |
| 2026-07-18 | Каталог, три партии улучшений покупателю: (1) **карточка товара** — цена весовых как «₽/кг», кнопка «Поделиться товаром», блок «Похожие товары», прямая ссылка на товар (…/#p=id) открывает карточку и при заходе по ссылке, и при переходе внутри каталога. (2) **Избранное (♥)** и **«Недавно смотрели»** — хранятся только на устройстве (localStorage), в базу не уходят; чип «♥ Избранное» со счётчиком. (3) **«Популярное» — анонимный учёт**: считаем на сервере, сколько раз открывали товар и что искали, БЕЗ сохранения личности (обезличенные счётчики; ОБНОВЛЕНИЕ-16.sql). Из этого — лента «🔥 Популярное» на главной, сортировка «Популярные», подсказки частых запросов. Стабильный анонимный номер устройства как «якорь» памяти. Всё покрыто Playwright-тестами (share, fav, popular) | Владелец: «поделиться, избранное, похожие, ссылка на товар»; «чтобы программа запоминала устройство без регистрации и было видно, что чаще смотрят/ищут — популярные товары». Честно объяснено: 100%-е распознавание через VPN/скрытый режим невозможно и незаконно (152-ФЗ); выбран надёжный и легальный вариант — анонимные счётчики + память на устройстве |
| 2026-07-19 | Каталог, жалоба владельца «дизайн и архитектура „Ходовых товаров“ и как она их вычисляет — сделай профессионально»: **переделан расчёт и вид «Ходовых»**. Расчёт: ранжирование по умолчанию **по выручке (₽)**, а не по смешанному количеству — деньги сравнимы для весовых (кг) и штучных (шт), поэтому дорогие позиции (Арбуз 49% оборота, Адреналин) больше не «прячутся» внизу; переключатель «По выручке / По количеству»; сортировку делает база (новый параметр p_order у catalog_top_products) — истинный топ не обрезается; доля товара в обороте (%) считается от итога периода (новая функция catalog_period_total). Вид: строки с полосой относительного объёма, крупным главным числом и второй метрикой + доля/в-день, топ-3 выделены акцентом (без «детских» медалей), сводка периода с оборотом. ОБНОВЛЕНИЕ-17.sql (+ в мастер-файл и сводные). Клиент переживает непринятую миграцию (fallback на старую функцию). Проверено: Playwright (по выручке №1 — Арбуз, по количеству №1 — яйца, доля, полоса, топ-3) + SQL на локальном Postgres (два прогона идемпотентно) | Владелец: «мне не нравится дизайн и архитектура про ходовые товары и то как она их вычисляет — сделай профессионально» |
| 2026-07-19 | Каталог, баг из скриншота владельца: **в лентах «Популярное»/«Недавно смотрели» карточки разной высоты** — портретное фото распирало квадрат (не было overflow:hidden), одна карточка становилась огромной, соседние тянулись под неё с пустыми полями (у одной пропадала цена). Исправлено: overflow:hidden + display:block на фото (как в рабочей .card-photo) + align-items:flex-start у ленты. Фото — квадрат с обрезкой, карточки равной высоты (проверено на воспроизведении: было 406×116/475px, стало 116×116/185px). Заодно на ПК вкладки «Каталог/Категории» ограничены шириной контента | Владелец прислал скриншот кривой ленты «Популярное», просил найти и починить баги дизайна |
| 2026-07-19 | Каталог, три правки: (1) баг «подкатегории не сужали фильтр» — при выбранной категории отметка подгруппы ничего не меняла (была логика «объединение»). Исправлено: если внутри категории отмечены подгруппы — показываем только их (сужение); вынесено в общий предикат catGroupPredicate. (2) Админ может удалить загруженный отчёт продаж — кнопка в «Ходовых» (только админ, с подтверждением) + функция catalog_delete_sales_period (ОБНОВЛЕНИЕ-18, admin-only, удаляет только продажи периода). (3) Убраны дубли навигации: вкладки «Каталог/Категории» и страница плиток-категорий удалены («Каталог» дублировал уже показанную сетку, категории были и в чипах, и в плитках); категории — только в ленте чипов под поиском. Удалён мёртвый CSS (вкладки, плитки, hero). Всё проверено Playwright, SQL — на Postgres | Владелец: «подкатегории не работают при фильтре»; «пусть админ может удалить период продаж»; «если уже показывает каталог, почему отдельная кнопка каталога — убери дубли, профессионально» |
| 2026-07-21 | Каталог, просьба владельца — удобство фильтра «пришло сегодня» для сотрудника, особенно весовых. Проверено автотестами: совмещение «Весовые»+«Сегодня» уже работало правильно (показывает только весовые с датой поступления = сегодня, дата берётся из файла «Цены поставщиков», столбец «Период»; остальное скрывает — и через чип «Весовые», и через тип). Добавлена **кнопка «🆕 Пришло сегодня»** в верхний ряд чипов (только для вошедшего сотрудника): один тап — все пришедшие сегодня, совмещается с «Весовые»/категорией (весовые за сегодня = 2 тапа), повторный тап снимает. Раньше это было спрятано в окне фильтра. Тесты: todaychip, wtoday | Владелец: «удобную функцию… фильтр сегодня правильно показывал, особенно весовые… когда сотрудник выбирает период и категорию весовые и оставляет сегодняшнее число — показать все весовые, пришедшие сегодня» |
| 2026-07-24 | Каталог, переезд на бесплатное без VPN — **шаг 1 (безопасный, за флагом)**: приложение научилось читать «витрину» покупателя из статического файла `catalog/data/products.json` (+ `groups.json`), а не с платного сервера. Функция `refreshStatic()` включается флагом `STATIC_URL` в `config.js` (по умолчанию '' = выключено, живой сайт не меняется); при сбое/отсутствии файла — тихий откат на загрузку с сервера. В файл идёт строгий белый список витринных полей (id, name, group_id, retail_price, is_weighted, unit, description, photos) — секретное (поставщики, закупка, штрихкоды/коды кассы, поступления, примечания) НЕ выгружается. Скрипт выгрузки `catalog/setup/export-static.mjs` берёт данные публичным anon-ключом и оставляет только витрину. Проверено Playwright (`stage1static`: витрина из файла, сервер за товарами не дёргается) + весь набор регрессов (19 зелёных). Флаг не включаю, пока в репозитории нет реальных данных | Владелец: «уберём VPN тогда?» → выбран путь «всё бесплатно, без сервера/VPN»; оставляем внутренние функции: закупочные цены, «Ходовые товары», вход по ролям |
| 2026-07-24 | Каталог, переезд — выбор способа сервера и **шаг 2 (ядро публикации на GitHub)**. Домашний сервер — тупик; Google Cloud отпал (санкции: российскую/без карты регистрацию не принимает, зарубежной карты у владельца нет). Итог: путь «всё на GitHub, бесплатно, без VPN, без сервера». Обновление каталога — как и раньше, Excel-файлами из 1С; публикация на GitHub **автоматическая** (по желанию владельца — «хочу автоматизацию», без кнопки). Сделано ядро: функции `ghCommit()` (один атомарный коммит на несколько файлов через Git Data API — ref→base commit→tree→commit→patch, ветка со слэшами сохраняется, при 409/422 один повтор), `publishShowcase()` (витрина товаров+категорий в `catalog/data/*.json` одним коммитом). Публичный белый список полей (id,name,group_id,retail_price,is_weighted,unit,description,photos) — секретное (поставщики, коды, штрихкоды, поступления, примечания) не выгружается. Ключ (GitHub token) — только в localStorage устройства (`wm_gh_token`), в код/репозиторий НЕ попадает; адрес репо/ветки — в config.js (не секрет). Тестовый доступ `window.WM_PUBLISH` только на localhost. Проверено Playwright (`ghpublish`: один коммit на 2 файла, секретное вырезано, сортировка) + весь набор регрессов (21 зелёный). Ещё не сделано: UI для ввода ключа, авто-публикация после импорта/правок, шифрование секретного, перенос фото | Владелец: «нету зарубежной карты» (Google отпал); «я буду как и раньше с помощью эксель файлов обновлять каталог?» → да; «хочу чтобы было автоматизация» |
| 2026-07-24 | Каталог, шаг 2 завершён — **окошко ключа + авто-публикация витрины**. Меню админа → «☁️ Публикация на GitHub»: поле для ключа (GitHub token), «Сохранить и проверить» (проверяет доступ к ветке деплоя), «Опубликовать витрину сейчас» (принудительно), «Удалить ключ», встроенная инструкция «где взять ключ». Ключ хранится только в localStorage устройства. **Авто-публикация**: после любой правки каталога (импорт Excel, добавление/правка/удаление товара/категории/фото) витрина сама выкладывается на GitHub — как просил владелец, без кнопок. Реализовано одной точкой в `refresh()` + гейт «только вошедший админ с ключом» + «схлопывание» частых правок (один коммит вместо десятков) + подпись содержимого (нет изменений витрины — нет коммита). Скрипт зеркалирования кода `deploy-mirror.sh` научили сохранять `catalog/data/` (её публикует приложение прямо в деплой-ветку, при зеркалировании кода не затирается); `catalog/data/` добавлена в `.gitignore` рабочей ветки. Бамп SW v29→v30. Проверено Playwright: `ghpublish` (атомарный коммит, секретное вырезано), `ghauto` (гейт админа, схлопывание, подпись, окошко ключа), `stage1static` + весь набор регрессов (22 зелёных) | Владелец: «делай» — автопубликация после Excel-импорта, без кнопок |
| 2026-07-24 | Каталог, **запасной вход в публикацию без сервера**. Обнаружилось, что сервер Timeweb недоступен (у владельца плашка «каталог сохранён 46 ч назад» — сервер лёг ≈2 дня назад, вход-авторизация зависает). Чтобы переезд не зависел от лежащего сервера: добавлен адрес `…/catalog/#publish` (и `#gh`) — открывает окно «Публикация на GitHub» БЕЗ входа на сервер. Публикация всё равно требует GitHub-ключ (без ключа ничего не выложится), а витрина берётся из сохранённой в браузере копии каталога (`state.products`, ~17к товаров в кэше владельца). Так владелец может выложить витрину на GitHub из кэша, не логинясь на мёртвый сервер. Бамп SW v30→v31. Проверено Playwright (`ghhash`: окно открылось без входа, публикация из кэша прошла, секретное вырезано) + регрессы (22) | Владелец не мог войти админом (сервер недоступен); решение — не воскрешать старый сервер, а публиковать из кэша по ключу |
| 2026-07-24 | Каталог, **покупатели переведены на бесплатный GitHub** — включён `STATIC_URL: 'data/'`. Владелец создал классический GitHub-ключ (scope repo), вставил в окно «Публикация на GitHub» и опубликовал витрину из сохранённой в браузере копии (сервер Timeweb лежал) — коммит «Каталог: обновлена витрина» от dexauron, `catalog/data/products.json` ≈4 МБ (~17к товаров) + `groups.json`. Проверено через GitHub API + raw: в файле только витринные поля, секретных (supplier_ids, code, article, barcodes, department, note, arrival_at, закупка) — ноль. После включения флага покупатель без входа читает каталог из файла на GitHub (0 ₽, без сервера, без VPN, работает в России и офлайн); при отсутствии файла — тихий откат на сервер. Бамп SW v31→v32. Регрессы 23 зелёных. Это чинит и аварию: сервер недоступен, а витрина теперь не зависит от него | Владелец: «говорит витрина опубликована» → включаем бесплатный путь для покупателей |
| 2026-07-24 | Каталог, сервер Timeweb **окончательно недоступен** (владелец: «уже всё»). Итог дня: покупатели переведены на бесплатный GitHub (проверено на живом сайте — 17 066 товаров отдаются из файла, 0 ₽, без сервера/VPN). Аккаунты сотрудников/админа сейчас НЕ работают, т.к. вход шёл через сервер — это следующий большой шаг. Решение: сотрудников тоже переносим на бесплатную схему без сервера; закупку и историю продаж восстановим из Excel-файлов владельца (на сервере они не сохранились, но в 1С-выгрузках есть). Фото: из 17 066 товаров с фото 1 268, и они на мёртвом сервере — временно могут не грузиться, перенесём отдельным шагом. **Начат фундамент серверлес-режима**: шифрование секретного (Web Crypto AES-GCM, ключ из пароля через PBKDF2 150k итераций) — `encryptJSON/decryptJSON`; на GitHub будет лежать только шифртекст, разные пароли дадут разный доступ (владелец/сотрудник). Проверено Playwright (`crypto`: круг шифр/расшифр, чужой пароль не открывает, в шифртексте нет открытых значений) | Владелец: «уже всё» (сервер не вернуть) → переносим сотрудников на серверлес, секреты из Excel |
| 2026-07-24 | Каталог, **ядро серверлес-режима** (без сервера). Источник правды владельца — зашифрованный файл `catalog/data/secret-catalog.enc` на GitHub: полный каталог (товары со всеми полями + закупочные цены + продажи + контакты). Функции: `publishFull(password)` — одним атомарным коммитом кладёт открытую витрину (products.json, только публичные поля) + группы + зашифрованный полный каталог; `unlockSecret(password)` — скачивает шифрофайл с raw.githubusercontent, расшифровывает паролем, поднимает полные данные в state и включает режим админа без сервера (`state.serverless`), при неверном пароле — отказ; NO_SECRET если файла ещё нет. Читать шифрофайл может кто угодно (публичный), открыть — только паролем; писать — по GitHub-ключу. Проверено Playwright (`serverless`: один коммит из 3 файлов, витрина чистая, шифрофайл без читаемых полей, вход паролем восстанавливает коды/штрихкоды, чужой пароль не открывает) + весь набор (25 зелёных) | Владелец: «делай» — продолжаю перенос сотрудников на серверлес |
| 2026-07-24 | Каталог, **серверлес-импорт 1С → зашифрованный каталог** (переиспользуя готовые парсеры). Владелец прислал 5 реальных файлов 1С (прайс-лист, цены поставщиков, продажи, остатки, контрагенты). Добавлены серверлес-загрузчики: `svUploadPrices/svUploadRetail/svUploadStock/svUploadSales/svUploadContacts` + диспетчер `svImportRows(rows)` — те же парсеры (`parsePriceReport` и т.д.), но результат сливается в каталог в памяти (сопоставление товара по коду → штрихкоду → названию; id и фото сохраняются; группы/поставщики создаются по имени; закупочные цены — последняя по (товар,поставщик); продажи — по периоду; контакты — телефон к поставщику), затем `publishFull(пароль)`. Проверено на РЕАЛЬНЫХ файлах владельца (`svimport`): 17 056 товаров, 16 773 с розницей, 15 873 со штрихкодами, 23 227 закупочных цен, 5 307 продаж, 643 контакта; витрина публикуется открытой (без секретов), полный каталог зашифрован. Регрессы — 26 зелёных. Осталось: UI (первая настройка/пароль, кнопка импорта в серверлес-режиме, вход по паролю в форме) | Владелец прислал 5 файлов 1С: «хочу чтобы каталог брал информацию из таких файлов, полноценно работающий каталог нужен» |
| 2026-07-24 | Каталог, **серверлес-режим подключён к интерфейсу** — полноценный рабочий каталог без сервера. Вход: форма входа сначала пробует серверлес — пароль скачивает и расшифровывает `secret-catalog.enc` с GitHub (`unlockSecret`); нет файла (строго 404, а не сбой сети) + есть GitHub-ключ → первая настройка (`applyServerless`, этот пароль становится паролём каталога) и сразу открывается окно импорта. Импорт (`smartRun`) в серверлес-режиме гонит файлы через `svUpload*` в память и `publishFull(пароль)` (витрина + зашифрованный полный каталог одним коммитом). Карточка товара в серверлес-режиме показывает закупочные цены из `state.prices` и продажи из `state.sales` (тот же рендер, без сервера). `refresh()` в серверлес-режиме не дёргает мёртвый сервер; выход — забывает пароль и перезагружает витрину. Проверено сквозным Playwright-тестом на РЕАЛЬНОМ прайс-листе (`e2eserverless`): первый вход по паролю → импорт 16 773 товаров с ценами → публикация (витрина чистая, полный зашифрован) → перезаход по паролю грузит каталог → неверный пароль не пускает. Бамп SW v32→v33. Регрессы — 27 зелёных. Осталось как отдельные шаги: правка/добавление отдельного товара в серверлес (пока основной путь — импорт Excel), перенос фото | Владелец: «полноценно работающий каталог нужен», прислал файлы 1С |
| 2026-07-24 | Каталог, пакет по итогам теста владельца на реальных файлах: (1) **фикс импорта цен** — товар из витрины приходит без `supplier_ids`, импорт «Цены поставщиков» на него падал (`reading 'includes'`); добавлена страховка массивов при сопоставлении (svUploadPrices/svUploadStock). (2) **Запоминание устройства** — серверлес-вход (владелец/сотрудник) сохраняется в localStorage (`wm_sv_auth`) и восстанавливается при загрузке (роль сразу из кэша, полный каталог — в фоне), не выходит до явного «Выйти»; Supabase-логика не перебивает восстановленный вход. (3) **Аккаунт сотрудника** — владелец задаёт пароль сотрудника в меню (серверлес-ветка `staffPassForm`), он хранится в его каталоге и публикуется отдельным зашифрованным файлом `secret-staff.enc`; сотрудник входит своим паролём и видит ВСЮ информацию (товары, закупка, продажи/«Ходовые») — полный ПРОСМОТР, но без прав на правку/загрузку (`isAdmin=false`). Флаг `canSales` (видит «Ходовые») выставляется и в серверном входе. (4) **Поиск** — email в подсказки не пускаем (фильтр по «@»), усилён запрет автозаполнения (email в поле — это автозаполнение браузера, не наша настройка). Бамп SW v33→v34. Проверено Playwright: `staff` (полный просмотр, не админ, запоминание, неверный пароль), `e2eserverless` (запоминание входа после обновления), `svfix` (импорт цен на витринный товар) + весь набор регрессов | Владелец: «аккаунт сотрудника 0466»; «пусть сотрудники и админ видят всё, полный доступ к просмотру»; «запоминал устройство и не выходил до выхода»; «убери почту из поиска»; ошибка импорта цен |
| 2026-07-24 | Каталог, баг поиска (владелец: «написал адреналин, а показал товары, где этого слова нет»). Причина: свободный поиск матчил ещё и ИМЯ ПОСТАВЩИКА — у «7Days» поставщик «Адреналин Дистрибуция», балл 60 → товары всплывали без слова в названии (и без подсветки). Исправлено: поставщика в свободном поиске больше не учитываем (искать по поставщику — чипом «🚚 Поставщики»); остались название, код, штрихкод, категория. Проверено Playwright (`searchsup`: «адреналин» → только товары со словом в названии, товары поставщика «Адреналин» не притянуты) + `smart`, `simil` и др. Бамп SW v36 | Владелец прислал скриншот: по «адреналин» лишние товары |
| 2026-07-24 | Каталог, «Популярное» переведено на **продажи** (просьба владельца: «популярные вычислялись по количеству продаж, видно всем»). `buildPopularIds()` суммирует проданное количество на товар (из данных продаж, товар по коду→названию), берёт топ-40; публикуется отдельным файлом `catalog/data/popular.json` (только упорядоченный список id — сами цифры продаж не раскрываются). Покупатель без входа читает popular.json из витрины (refreshStatic), владелец/сотрудник считают вживую из памяти; лента «🔥 Популярное» на главной показывает этот топ (запасной путь — старый счётчик просмотров). Публикуется в общий коммит витрины. Честно объяснено владельцу: «Недавно смотрели» ОТ ВСЕХ пользователей без сервера невозможно (нужно общее хранилище просмотров, а мы ушли от платного сервера) — оно остаётся личным (это устройство), а «Популярное по продажам» закрывает потребность «что популярно у всех». Бамп SW v37. Проверено Playwright (`popsales`) + регрессы | Владелец: «популярные по количеству продаж, видно всем»; «недавно смотрели от всех» |
| 2026-07-24 | Каталог, чистка админ-экранов под бесплатный режим (владелец: «убери недавно смотрели; исправь ошибки на всех экранах, чтобы всё работало»). (1) **«Недавно смотрели» убрано** (renderRecentProducts прячет ленту). (2) **Админ-действия переведены на серверлес** (правка в памяти + publishFull через `svSaveAndPublish`): добавить/изменить/удалить товар (submitForm/deleteProduct), группы (add/rename/delete), «Ходовые» (renderTopPeriods+loadTopProducts считают из state.sales по периоду; удаление отчёта продаж — из памяти + публикация). Раньше эти кнопки стучались в мёртвый сервер и давали ошибки. (3) **Серверные функции скрыты в бесплатном режиме** (чтобы не было кнопок-ошибок): дозаполнить фото, фото на проверке, поставщики (управление), убрать дубли, разведка цен конкурентов, добавить/найти фото в карточке. Фото и управление поставщиками — отдельный будущий шаг (фото → коммит в GitHub). Бамп SW v38. Проверено Playwright (`svadmin`: «Ходовые» из памяти без ошибок; `fav`: недавно-смотрели убрано, избранное живо) + весь набор (28) | Владелец: «убери недавно смотрели»; «исправь ошибки на всех экранах, чтобы всё работало на каждом устройстве» |
| 2026-07-24 | Каталог, «Пришло сегодня» не работало в бесплатном режиме — при серверлес-импорте не проставлялась дата поступления `arrival_at`. Исправлено: `svUploadPrices` ставит `p.arrival_at` = самая свежая дата цены поставщика (столбец «Период» файла «Цены поставщиков»). Теперь фильтр «🆕 Пришло сегодня» находит товары, у которых поступление = сегодня. Поле внутреннее (в витрину не публикуется). Бамп SW v39. Проверено Playwright (`svtoday`: импорт с сегодняшней датой → arrival=сегодня; `todaychip`/`wtoday` — фильтр). Владельцу: нужно ПЕРЕЗАГРУЗИТЬ «Цены поставщиков», чтобы даты проставились; показывает пришедшее ИМЕННО сегодня (если в файле нет сегодняшних дат — пусто, это верно) | Владелец: «Пришло сегодня не работает» |
| 2026-07-24 | Каталог, сквозной аудит (владелец: «найди все ошибки и исправь, пройдись по всему»). Написан аудит-тест `audit`: серверлес-владелец с данными открывает ВСЕ экраны (карточка с ценами/продажами/похожими, поиск, фильтры, «Пришло сегодня», меню, «Ходовые», «Группы», «Публикация», форма товара) и ловит JS-ошибки. Найдена и исправлена **1 реальная ошибка**: `p._codes is not iterable` — поиск падал, если индекс ещё не построен; добавлена страховка `p._codes || []` в `scoreToken`. Проактивно защищены остаточные обращения к мёртвому серверу, срабатывавшие при обычном использовании: трекинг просмотров/поиска (`trackView`/`trackSearch` — отключены при STATIC_URL/серверлес, «Популярное» теперь по продажам) и история розничной цены (`renderRetailHistory` — в серверлес не ведётся). Итог: аудит владельца — 0 ошибок; весь набор регрессов зелёный (30+). Бамп SW v40 | Владелец: «найди все ошибки и исправь пройдись по всему» |
| 2026-07-24 | Каталог, по скриншоту владельца: **убрана строка чипов под поиском** («Все / Поставщики / Ходовые / категории / Весовые / Пришло сегодня»). Причина владельца: всё это (категории и фильтры) настраивается через кнопку фильтра (⚙), строка дублирует. `renderChips` теперь прячет `#groupChips`. Всё нужное осталось в окне фильтра: категории (#filterCats), подгруппы, тип «Весовые», поступление «Сегодня», поставщики; «Ходовые» — в меню админа. **«♥ Только избранное» перенесено в окно фильтра** (чекбокс), чтобы не потерять избранное. Тесты обновлены под новый путь (fav — через чекбокс фильтра; wtoday/subcat — без чипов; todaychip устарел). Бамп SW v41. Проверено (fav, wtoday, subcat, filters2, catcheck, arrival2, audit) | Владелец: «убери [строку чипов], всё работает через выбор фильтров» |
| 2026-07-24 | Каталог, аудит и доработка фильтра (владелец: «не показывает по датам поступления; хочу дерево категорий/подкатегорий со сворачиванием; чтобы сортировка и все фильтры работали, исправь ошибки»). (1) **Дерево категорий → подкатегорий**: `renderFilterCats` переписан — категории со стрелкой ▸ раскрывают свои подгруппы (реальные `state.groups`, отнесённые к категории через `categoryOf`), можно сворачивать (`filterCatOpen`); отметка подкатегории сужает (та же `catGroupPredicate`); отдельная кнопка «Подгруппы…» скрыта. (2) **Сортировка «Популярные»** чинена — сортировала по мёртвому счётчику просмотров (всегда 0), теперь по порядку продаж (`state.popularIds`). (3) **Фильтр по датам поступления** — код корректен (проверено тестом: показывает только попавшие в диапазон); не работал у владельца, т.к. `arrival_at` не был проставлен — нужно перезагрузить «Цены поставщиков» (даты берутся из столбца «Период»). Бамп SW v42. Проверено Playwright (`filteraudit`: дерево, сворачивание, сужение подгруппой, даты поступления, сортировка) + filters2/catcheck/subcat/arrival2/emp/fav обновлены | Владелец: даты поступления, дерево категорий, сортировка, «исправь все ошибки в фильтре» |
| 2026-07-27 | Каталог, **наименование товара показывается ПОЛНОСТЬЮ** (владелец: «хочу, чтобы наименование показывалось полностью, и чтобы это не мешало общей картине — чтобы всё было красиво ровно»). Раньше имя обрезалось на 2 строках с «…» (`-webkit-line-clamp:2`) — длинные названия из 1С было не прочитать. Убрана обрезка; чтобы плитки при этом остались ровными: (1) `min-height:2.6em` у названия — короткие имена не «прыгают»; (2) **цена прижата к низу плитки** (`order:3` + `margin-top:auto`), значки (⚖ и т.п.) переехали НАД ценой (`order:2`) — раньше товар со значком поднимал цену на ~20px, и в ряду цены стояли вразнобой; теперь цены в ряду на одной линии независимо от числа значков и длины имени (высоту ряда сетка выравнивает сама); (3) `.card-tags:empty {display:none}` — без значков нет пустого отступа; (4) перенос длинных слов по слогам (`hyphens:auto` при `lang="ru"`, `overflow-wrap:break-word`) вместо разрыва посреди слова. Бамп SW v43. Проверено Playwright (`fullname`: имена целиком, ничего не скрыто по высоте, цены выровнены в каждом ряду, нет горизонтальной прокрутки — на 390px, 360px и планшете 820px) + регрессы (responsive, filteraudit, catcheck, anon, audit, emp, fav, subcat, popsales, serverless, e2eserverless, staff, svadmin, fill) | Владелец: «наименование показано полностью… чтобы всё было красиво ровно» |
| 2026-07-27 | Каталог переведён на **дизайн-систему Auron Finance** (владелец: «измени дизайн каталога, как в приложении Auron Finance, скопируй дизайн»). Взяты те же токены, что в `app/index.html` и `docs/04-DESIGN-SYSTEM.md`: палитра (тёмная тема основная — фон #000, карточки #1C1C1E, вложенное #2C2C2E; светлая — #F2F2F7/#fff), фирменный индиго `--brand #5E5CE6` вместо зелёного, скругления 20/14/10, «хваталка» окна 36×4, FAB 56×56 с индиго-свечением. **Правило дизайн-системы «бренд-цвет только на интерактивном»**: кнопки, активные чипы, выбранное — индиго; ЦЕНА стала нейтральной (как суммы в Auron), а смысловые цвета вынесены отдельно — `--pos` зелёный (выгодная цена, прибыль, продажи), `--neg` красный (дороже, ошибка), `--warn` оранжевый. Обновлены `theme-color` и `manifest.webmanifest`. Бамп SW v44 | Владелец: «скопируй дизайн Auron Finance» |
| 2026-07-27 | Каталог, **полный аудит** (владелец: «найди все ошибки и баги — в коде, в архитектуре, в дизайне — и исправь; чтобы все кнопки и фильтры работали»). Найдено и исправлено 5 реальных дефектов: (1) **Esc не закрывал окна** — глобального обработчика не было вовсе (дизайн-система требует «Esc — закрыть»); добавлен, закрывает верхнее окно через `closeSheet`, сначала — фото на весь экран. (2) **`color-scheme` не был задан** — из-за этого нативные галочки и поля выбора даты рисовались светлыми на тёмной теме; теперь тема сообщается браузеру. (3) **Низкий контраст**: подписи `#8E8E93` на белом давали 2.9 при норме 4.5 (нечитаемо в торговом зале) — вторичный текст затемнён/осветлён по теме; бренд-индиго на тёмном давал 3.4 в надписях — для текста добавлен осветлённый `--brand-ink`. (4) **Мелкие цели под палец**: стрелка раскрытия подкатегорий была 30×30, строки дерева 33–37px — доведены до 44px. (5) **Служебное слово «owner» показывалось пользователю** в меню — заменено на человеческие «Владелец» / «Сотрудник магазина» с понятной подписью (заодно убраны мёртвые роли «Кассир»/«Аналитик» из бесплатного режима). Проверено: сквозной прогон всех кнопок (`allbtn`: ~200 нажатий на 23 экранах в двух ролях — 0 ошибок), сверка разметки и кода (`ids.py`: мёртвых кнопок нет, битых ссылок на элементы нет), машинный аудит интерфейса (`uiaudit`: контраст и размеры целей в обеих темах — замечаний нет), регрессы (34 теста). Упавшие тесты проверены поимённо — все устарели (ссылаются на убранные строку чипов, старый хаб импорта, роли Supabase, показ остатков, отключённый в бесплатном режиме) либо требуют аргументов; продуктовых багов среди них нет | Владелец: «найди все ошибки и баги и исправь, полный аудит» |
| 2026-07-27 | Каталог, **цены других магазинов — вариант «А»** (владелец выбрал «1 а»): цены записывает вошедший (владелец ИЛИ сотрудник), а **видят и фильтруют все — включая покупателя без входа**. Реализация без сервера: цены лежат ОТКРЫТЫМ файлом `data/competitors.json` (`{stores, prices}`) — это чужие ценники из зала, не наша тайна, поэтому не шифруются и доступны всем; они же дублируются в зашифрованные снимки владельца и сотрудника. Запись цены публикуется **отдельным маленьким коммитом** (`publishCompetitors`) — поэтому сотруднику НЕ нужен пароль владельца, достаточно ключа публикации на устройстве. В карточке товара: наша цена, цены магазинов с датой записи и разницей («у них дешевле/дороже на N ₽»), **фильтр по магазину и по датам записи** (как просил владелец — «в каких магазинах и когда была записана цена, по каждому товару»); фильтр сбрасывается при открытии другого товара. Цена за один день по одному магазину перезаписывается, прошлые даты остаются — видно, как цена менялась. Проверено `comp2` (сотрудник вносит → публикуется открытый файл → покупатель без входа видит и фильтрует; вносить не может) | Владелец: «1 а» (вариант А) |
| 2026-07-27 | Каталог, **новый экран «Цены других магазинов»** (владелец: «2 — для тех, которые ты посчитаешь нужными»). Выбран как самый полезный: собранные цены раньше было видно только внутри карточки одного товара, то есть общей картины не было. Экран в меню (для всех вошедших): сводка по магазинам — сколько цен записано и когда последняя; нажатие на магазин открывает все его цены с названием товара, датой, нашей ценой и разницей. Добавлена русская склонялка «1 цена / 2 цены / 5 цен». Бамп SW v45. Проверено `compscreen` | Владелец: «2 для тех которые ты посчитаешь что нужны» |
| 2026-07-27 | Каталог, **сортировка «Новее» не работала у покупателя без входа** (владелец: «фильтр сортировка не работает»). Проверены ВСЕ варианты сортировки в двух режимах — у владельца работали все, у покупателя ломалась ровно одна: «Новее». Причина: сортировка идёт по дате поступления, а даты в витрину не публиковались (были помечены как внутреннее поле) — сортировать было нечем, порядок оставался алфавитным. Исправлено: `arrival_at` добавлена в `PUBLIC_FIELDS`; поставщик и закупочная цена по-прежнему НЕ публикуются, уходит только дата. Плюс запасная дата `created_at` (когда товар появился в каталоге) — чтобы «Новее» работала и для товаров, ни разу не попадавших в файл цен. Обе даты режутся до ГГГГ-ММ-ДД: витрину качает каждый покупатель, лишнее время на 17 тыс. товаров заметно. Бамп SW v46. Проверено `sortcheck` (5 сортировок × 2 режима + подсветка выбранной + сохранение порядка после «Показать») и `sortfallback` (запасная дата). **Владельцу: чтобы заработало, нужно один раз нажать «Опубликовать»** — витрина перестроится с датами | Владелец: «фильтр сортировка не работает» |
| 2026-07-27 | Каталог, **умный поиск: убран мусор из выдачи** (владелец: «доработай поисковик, чтобы не показывал лишние товары»). Найдены три причины мусора, все исправлены. (1) **Совпадение внутри слова**: искали любой кусок строки, поэтому «сок» находил «ВыСОКобелковый творог», а «рис» — «ИРИС» и «СюРПриз». Теперь ищем по НАЧАЛУ СЛОВ (знаки — дефисы, точки, скобки — считаются разделителями, поэтому «дог» по-прежнему находит «хот-дог»); кусок внутри слова остался слабым сигналом и только для запросов от 5 букв. (2) **Транслитерация теряла буквы**: «водяной» → `vodanoi` (я→a), из-за чего «вода» ложно совпадала с «Игрушка Водяной». Теперь транслит применяется ТОЛЬКО когда языки букв разные (искали латиницей — название русское, или наоборот); два русских слова сравниваются напрямую. (3) **Нечёткий поиск был слишком мягким на коротких словах** — ограничен запросами от 4 букв (сам порог похожести оставлен мягким, иначе перестаёт находиться «хатдок» → «хот-дог»). Плюс два улучшения качества: **отсечение слабого хвоста** — если нашлось хорошее совпадение, всё, что ниже 60% от лучшего, не показываем (по «молоко» больше не вываливается вся группа «Молочные продукты»); **целое слово важнее куска** — по «вода» сначала «Вода 0,5л», потом «Водолей». Проверено на выдуманных ловушках (`smartsearch`, 11 случаев) и на 6000 РЕАЛЬНЫХ названий из каталога владельца; поиск по коду кассы, штрихкоду, словам в любом порядке и опечаткам («sneakers» → Snickers) сохранён. Бамп SW v47 | Владелец: «поисковик должен быть умным и не показывать лишние товары» |
| 2026-07-31 | **Сайт лежал (404 на каталоге) — ветки затирали друг друга при публикации.** Причина: появилась третья ветка `claude/cool-archimedes-qv458l` (страница сканера) со СВОИМ `deploy.yml`, который собирал сайт только из `app/` и публиковал с `force_orphan` (замена содержимого целиком) — каталог с сайта исчезал. Обратное тоже было верно: наш workflow не знал про `scan.html` и стирал его. Кто отработал последним — того часть и оставалась. **Исправлено системно** (владелец: «пусть ничего друг другу не будут мешать, и в будущем тоже»): единый `deploy.yml`, побайтово одинаковый во ВСЕХ публикующих ветках (`main`, app-ветка, ветка каталога, ветка сканера), собирает сайт из трёх источников — приложение + каталог + `scan.html`; добавлены `concurrency` (сборки в очередь, чтобы две публикации не наступали друг другу на пятки) и проверка перед выкладкой: нет `app/index.html` или `app/catalog/index.html` → публикация ОТМЕНЯЕТСЯ, а не выкладывает половину сайта. В шапке файла — предупреждение, что копии обязаны быть одинаковыми. Проверено: gh-pages содержит все три части, workflow во всех ветках с одной контрольной суммой | Владелец: 404 на сайте; «пусть не мешают друг другу» |
| 2026-07-31 | Каталог: **подобрана правка другой сессии, сделанная прямо в деплой-ветке** (`174ac81`, «битое фото → заглушка 📦»): при недоступной картинке показывается та же заглушка 📦, что у товаров без фото, вместо «сломанной картинки» браузера. Перенесена в рабочую ветку черри-пиком — иначе очередное зеркалирование `catalog/` затёрло бы её и откатило кэш v48 → v47. Теперь код каталога в обеих ветках совпадает. Бамп SW v49 | Разъезд веток при параллельной работе |
| 2026-07-31 | Каталог понимает **справочник «Штрих коды» из 1С** (владелец прислал файл на 19 934 строки: «Штрих код» · «Единица» · «Номенклатура»). Раньше такой файл распознавался, но грузиться мог только «в довесок» к файлу цен — отдельно кнопка загрузки не появлялась, а в бесплатном режиме тип вообще отклонялся. Сделано: (1) **распознавание починено** — мешала служебная строка выгрузки «Выводить количество подчинённых записей: Да»: слово «количество» заставляло каталог думать, что это отчёт с количествами. Теперь служебные строки не учитываются, плюс два надёжных признака: 1С сама пишет «Имя объекта: Штрих коды», и состав колонок (есть «Штрих код» и «Номенклатура», нет цен/количеств/сумм). (2) **Свой разбор** `parseBarcodesReport` + загрузка `svUploadBarcodes`: товар ищется по коду/названию, коды ДОБАВЛЯЮТСЯ к уже имеющимся (не затирают), у каждого запоминается своя единица (`p.barcode_units`). (3) **У одной номенклатуры — несколько кодов**: на штуку, на упаковку, на блок. Количество в упаковке зашито в единице («упак (24)») — оно разбирается и показывается по-человечески: «упаковка — 24 шт», «фасовка 2,5 кг», «штука». В карточке штучный код идёт первым, упаковки — после, по возрастанию. (4) **Поиск по ЛЮБОМУ из штрихкодов** работает (все коды попадают в поисковый индекс). Штрихкоды и единицы — внутренние данные, в витрину покупателя НЕ публикуются. Бамп SW v50. Проверено на настоящем файле владельца (`barcodes`): 17 016 товаров получили коды, 2 138 — несколько, поиск находит по каждому из 8 кодов примера | Владелец: «хочу, чтобы каталог понимал такой файл… несколько штрихкодов с единицей измерения… поиск по всем штрихкодам»; «штрихкоды упаковок и блоков с количеством штук в упаковке» |
| 2026-07-31 | Каталог понимает **справочник «Единицы измерения» из 1С**. Владелец прислал файл дважды: сначала БЕЗ кодов номенклатуры (в нём 20 302 строки, но привязать единицы к товарам было не из чего — сказал об этом прямо), затем с колонкой «Номенклатура.Код» — и теперь привязка есть. Сделано: (1) **Распознавание** по строке 1С «Имя объекта: Единицы измерения». Проверяется ПЕРВЫМ: из-за слов «Розничная цена единицы» и «Количество в упаковке» в шапке файл раньше принимался за отчёт об ОСТАТКАХ и грузился бы не туда. (2) **Словарь единиц** `state.unitCoef`: «упак (24)» → 24, «кг (2,5)» → 2,5 (99 единиц). Он же — источник количества в упаковке для показа штрихкодов; раньше число бралось только из скобок названия. (3) **Фасовки товара** `p.pack_units` — привязка по коду номенклатуры; в карточке строка «Фасовки»: «штука · упаковка — 21 шт» (показывается, когда фасовок больше одной). (4) **Нормализация кода**: Excel выгружает код с разделителем тысяч («1,001» = 1001) — 16 721 строка из 20 301; коды приводятся к обычному виду, иначе не находился ни один товар. Фасовки и единицы — внутренние данные, в витрину не публикуются. Бамп SW v51. Проверено на обоих настоящих файлах владельца (`units`, `units2`): 99 единиц в словаре, 6000 товаров получили фасовки, у 1185 их несколько | Владелец: «справочник единиц измерения, хочу чтобы каталог понимал и обновлял данные»; «добавил коды номенклатур» |
| 2026-07-31 | Каталог, **найдена ошибка в отсеве строк-итогов**: в JavaScript `\b` (граница слова) не работает с кириллицей, поэтому проверка `/^\s*(итого|всего)\b/i` НЕ отсеивала строку «Итого» — она попадала в данные как обычная запись (в справочнике единиц «Итого» с коэффициентом 55 777 стало бы «единицей измерения»). Убран `\b` в трёх разборщиках: прайс-лист, справочник штрихкодов, справочник единиц | Найдено при разборе справочника единиц |
| 2026-07-31 | **Обновления каталога не доезжали до людей** (владелец прислал скриншот с живого сайта: справочник единиц → «Не нашёл колонку "Номенклатура" в отчёте "Остатки"», справочник штрихкодов → «не понял, что это за файл» — ровно поведение СТАРОГО кода, хотя на gh-pages лежал новый). Причина: (1) открытая страница продолжала работать на старом коде — новая версия service worker вставала, но перезагрузки не было; (2) стратегия «сначала сеть» упиралась в HTTP-кэш GitHub Pages (файлы отдаются с запасом на минуты), поэтому `fetch` возвращал старый `app.js`, и SW клал его же в свой кэш. Исправлено: оболочка (страница, скрипты, стили, манифест) запрашивается МИМО кэша браузера (`cache: 'reload'`); при возврате к вкладке проверяется обновление (`reg.update()`); когда новая версия перехватывает управление — страница перезагружается один раз сама. Перезагрузка только при СМЕНЕ управления: при первом заходе контроллер появляется впервые, там перезагружать нечего. Бамп SW v52. Проверено настоящим тестом `swupdate` (service worker включён, файл на сервере подменяется — страница показывает новую версию); тест сначала «проходил» вхолостую из-за неверного шаблона подстановки — исправлено, теперь он действительно ловит регресс | Владелец: скриншот с ошибками импорта на живом сайте |
| 2026-08-06 | **Сайт застрял на старой версии — виновата публикация GitHub Pages, а не код.** Владелец дважды прислал скриншот с ошибками импорта справочников; после хард-рефреша ошибки остались. Проверка по сети показала: в ветке `gh-pages` лежит v52 с новым кодом, а сайт отдаёт **v49** — то есть GitHub Pages не публикует то, что мы туда кладём. В логах Pages: `deployment_queued` → `deployment_in_progress` → `##[error]Timeout reached, aborting!`. Четыре падения подряд, в том числе при одиночном чистом запуске без параллельных пушей; инцидентов на githubstatus.com нет — застряла публикация конкретно этого репозитория (сторона GitHub, правами сессии не чинится: rerun и workflow_dispatch → 403). **Что исправлено с нашей стороны:** (1) `concurrency` переведён с очереди на `cancel-in-progress: true` — очередь была вредной: каждая публикация пишет в gh-pages, каждая запись запускает сборку Pages, при частых пушах (публикация витрины из приложения + правки кода) очередь разрасталась и сборки срывались по таймауту. (2) **Сайт похудел с 35 МБ до 6,1 МБ**: `secret-catalog.enc` и `secret-staff.enc` (по 14,5 МБ) публиковались на сайт впустую — приложение читает их НЕ с сайта, а напрямую из репозитория через `raw.githubusercontent.com` (`rawUrl`/`fetchRaw`). Витрина (`products.json`, `groups.json`, `popular.json`, `competitors.json`) осталась — её читают именно с сайта. Обе правки разложены во все четыре публикующие ветки (контрольная сумма workflow одинакова). Публикацию должен разблокировать владелец: Settings → Pages → переключить источник на другую ветку, сохранить, вернуть `gh-pages`, сохранить | Владелец: «сделал как ты сказал и всё говорит это» |
| 2026-08-06 | **Публикация каталога падала с «Failed to fetch»** (владелец: остатки загрузились — 10 041 запись, а публикация сорвалась). Причина — размер запроса: `publishFull` отправлял ВСЁ содержимое одним POST в GitHub (`/git/trees` с полем `content` у каждого файла) — **37,8 МБ**, из них два зашифрованных снимка по 16,63 МБ. Такой запрос браузер не дотягивает, и это обрыв на уровне сети, а не отказ GitHub (потому и не HTTP-код, а «Failed to fetch»). Владельцу на вопрос «можно убрать ограничение?» отвечено прямо: ограничение не наше, снять нельзя — но можно перестать в него упираться. Сделано: (1) **сжатие перед шифрованием** (gzip через `CompressionStream`, флаг `z` в конверте) — каталог это десятки мегабайт текста, жмётся в разы; старые файлы без сжатия читаются по-прежнему (поля `z` у них нет), есть понятная ошибка, если браузер не умеет распаковывать; (2) **каждый файл заливается отдельным запросом** (`/git/blobs`), а в дерево кладутся ссылки на них вместо содержимого. Итог на каталоге 17 000 товаров: было 37,8 МБ одним запросом — стало **4,5 МБ шестью запросами, самый большой 3,64 МБ**; зашифрованные снимки 16,63 МБ → **0,43 МБ**. Бамп SW v53. Проверено `gzsize` (сжатие + совместимость со старыми файлами) и `pubsize` (файлы уходят по отдельности, в дереве только ссылки, максимальный запрос < 8 МБ) | Владелец: «Каталог собран, но публикация не удалась: Failed to fetch»; «ограничение на размер можно убрать?» |
| 2026-08-06 | Каталог **работает вообще без базы** — нужно для нового магазина «с нуля» (владелец спросил, как объяснить настройку другому человеку). Раньше при пустых полях старого сервера приложение упиралось в баннер «нужна настройка» и НЕ показывало бесплатную витрину — то есть у нового человека каталог просто не открылся бы, а инструкция врала бы уже на втором шаге. Исправлено: подключение к базе стало необязательным (`hasDb`), баннер настройки показывается, только если не задано НИ подключение к базе, НИ бесплатная витрина; клиент базы создаётся лишь при наличии настроек; в `refresh()` добавлена ветка «базы нет» — раньше при отсутствии интернета код проваливался к серверным запросам и обращался к несуществующему клиенту. Бамп SW v54. **Отдельно про проверку:** первая версия теста «без базы» была НЕДОСТОВЕРНОЙ — настройки подставлялись через `addInitScript`, а `js/config.js` затем перезаписывал их настоящими, и тест на самом деле гонял конфиг Way Market (обнаружено по `sb=true` в отладке). Тест переписан: подменяется сам файл `js/config.js`. Теперь честно проверяются оба случая — с интернетом и без (`nodb`), плюс отдельная проверка, что у нашего магазина с настоящими настройками ничего не сломалось (`waymarket`) | Владелец: «как объяснить человеку, чтобы он настроил такой же каталог» |
| 2026-08-06 | Каталог, **переделана карточка товара** (владелец: «доработаем дизайн карточки, уберём лишнее, как у других»). Показаны три варианта макетами на настоящих данных; выбран «Чистый список» (как в Ozon/Яндекс Лавке) — он убирает повторы и шум, но ничего не прячет: прятать данные у сотрудника, стоящего у полки, рискованнее длинной карточки. Убрано ДУБЛИРОВАНИЕ (одно и то же говорилось трижды): тег «📏 Продаётся: шт» (теперь только для необычных единиц — кг, упак), строка «Фасовки» (те же «штука»/«упаковка — 48 шт» уже подписаны у штрихкодов; строка осталась только если штрихкодов нет вовсе). Оформление: цена стала главной — крупно (34px) и без рамки, вместо одинаковой с кодом индиго-плашки; поля превратились в плоский список с тонкими разделителями вместо стопки одинаковых прямоугольников; наценка — одна строка вместо цветного блока; продажи — короткая строка вместо блока с тремя крупными числами; «Поступление» переименовано в «Последнее поступление» (в карточке рядом стояли две разные даты поступления и это читалось как ошибка). По просьбе владельца **«Код кассы» переименован в «Код товара»** (в карточке и в форме). Удалены осиротевшие стили (.field-main, .sales-box/nums/cell, .markup-box и др.), чтобы не конфликтовали. Бамп SW v55. Проверено `card` (14 проверок: нет повторов, цена крупнее полей, у полей нет рамок, покупатель без входа не видит коды/наценку/продажи) | Владелец: «дизайн карточки, убрать лишнее»; «измени код кассы на код товара» |
| 2026-08-06 | Каталог, **найден дефект: пустая дата превращалась в «undefined.undefined.ll»**. Импорт цен допускает `price_date: null` (в файле могло не быть даты), а `fmtDate` слепо резал строку — в карточке у поставщика появлялось «⚠ цена от undefined.undefined.defined». Исправлено в корне: `fmtDate` проверяет формат и возвращает пустую строку, а строка цены при отсутствии даты пишет «дата неизвестна» вместо мусора. Найдено случайно при проверке новой карточки | Найдено при переделке карточки |
| 2026-08-06 | Каталог, **шаг 2 по фото: поиск по штрихкоду в открытых базах заработал без сервера** (владелец: «делай шаг 2»). Механизм уже был написан (Open Food Facts + Beauty/Products/PetFood, подбор по штрихкоду и по названию с проверкой похожести), но упирался в Supabase Storage: найденное фото скачивалось, сжималось и заливалось в своё хранилище. Хранилища нет — и я сначала говорил, что шаг 2 зависит от шага 1. **При проверке оказалось, что не зависит**: достаточно сохранять ССЫЛКУ на фото из открытой базы (лицензия открытая, их CDN публичный), а скачать чужую картинку браузер всё равно не даёт (CORS). Сделано: в бесплатном режиме `attachFoundPhoto` просто запоминает URL; фоновый автопоиск разрешён без сервера; кнопка «Найти фото» в карточке возвращена. **Публикация — одним коммитом в конце**, а не после каждого фото (иначе тысячи коммитов и упор в лимиты GitHub). Бамп SW v56 | Владелец: «делай шаг 2» |
| 2026-08-06 | Каталог, **найден дефект в поиске фото: сбой открытой базы помечал товары как «фото нет» НАВСЕГДА**. Обнаружено случайно — во время работы Open Food Facts лежал (HTTP 502 «Unscheduled downtime»), и стало видно, что «база не ответила» и «фото у неё нет» в коде неразличимы: оба давали `null`, после чего товар попадал в список проверенных и больше никогда не перепроверялся. На реальном каталоге один сбой сервиса тихо похоронил бы тысячи товаров. Исправлено: поиск возвращает `{ url, reached }`; товар помечается проверенным ТОЛЬКО если база реально ответила (200 или 404). Плюс: если база не отвечает 3 товара подряд — поиск останавливается и честно пишет «Открытая база фото сейчас недоступна, попробуй позже, проверенное не потеряется», вместо того чтобы впустую перебирать 17 тысяч позиций. Проверено `photofind` (8 проверок в двух режимах: база работает — фото сохранены ссылкой, публикация ровно одним коммитом, «без фото» помечены; база лежит — не помечено ничего, показано понятное сообщение, перебор остановлен) | Найдено при выполнении шага 2 |
| 2026-08-13 | Каталог, **приходная цена считается и за штуку, и за упаковку по каждому поставщику** (владелец: «хочу, чтобы приходную цену сразу рассчитывал за упаковку по каждому поставщику — цену за шт и за упаковку»). На вопрос «за что указана цена в прайсе» владелец ответил: **по-разному, смотреть колонку „Ед.“**, и попросил показывать **обе цены в строке поставщика**. При разборе выяснилось, что это не только удобство, а **дефект в расчётах**: единицу строки прайса импорт читал, но к цене не привязывал, поэтому цена, указанная за упаковку, сравнивалась с розничной ценой за штуку. На живом примере: розница 55 ₽, у одного поставщика 43,33 ₽ за штуку, у другого 1 920 ₽ за упаковку 48 шт (то есть 40 ₽ за штуку — дешевле). Каталог показывал «✓ выгоднее» ПЕРВОМУ (43,33 < 1 920 как число) и считал наценку от 43,33 ₽. Сделано: единица строки сохраняется вместе с ценой (`unit`); любая цена приводится к базовой единице через справочник единиц товара (`pack_units`) или число в скобках названия («упак (48)» → 48); в строке поставщика сверху крупно **цена за штуку**, под ней **цена за упаковку** («1 920 ₽ за упаковку · 48 шт»); если цена в прайсе за штуку — упаковка досчитывается сама (43,33 × 48 = 2 079,84 ₽). За штуку теперь считаются и наценка, и «✓ выгоднее», и сортировка поставщиков, и стрелки ↑↓ (раньше переход поставщика со штук на упаковки читался как скачок цены), и история цены в карточке поставщика. Бамп SW v57. Проверено `packprice` (11 проверок на двух поставщиках — за штуку и за упаковку); отдельно убедился, что тест ПАДАЕТ на старом коде: там «выгоднее» доставалось дорогому поставщику | Владелец: «приходную цену за упаковку по каждому поставщику — за шт и за упаковку» |
| 2026-08-13 | Каталог, **две доработки карточки** (владелец спросил, что ещё можно улучшить; из предложенного выбрал эти две). **1. Остаток и «сколько заказать».** При разборе нашлось, что остатки из 1С (10 041 позиция) каталог загружает и запоминает — но **нигде не показывает**: данные лежали мёртвым грузом. Теперь в карточке блок «Остаток» с датой выгрузки (остаток устаревает за сутки, без даты ему нельзя верить), а вместе с продажами — подсказка: «Хватит на 2,4 дня — пора заказывать» и «Заказать 2 упаковки (96 шт) — хватит на 22 дня». Заказ считается **в упаковках**, а не в штуках, — потому что заказывают упаковками; опирается на количество в упаковке из вчерашней работы. Порог «пора заказывать» — меньше 7 дней, закупаемся на 14 дней. Если продажи не загружены — показываем только остаток и ничего не выдумываем; нулевой остаток пишем словом «Закончился»; покупатель без входа остаток не видит. **2. Сравнение с конкурентами одной строкой.** Раньше цены чужих магазинов лежали столбиком и сравнивать приходилось глазами. Теперь сверху вывод: «⚠ Дешевле в магазине „Пятёрочка“ — 52 ₽, это на 3 ₽ меньше нашей» или «✓ У нас дешевле всех», с пометкой, с каким числом магазинов сравнили и когда сделана запись. Важная деталь: по каждому магазину берётся **самая свежая** запись — прошлогодняя цена в вывод не попадает (в списке ниже история сохраняется как была). Бамп SW v58. Проверено `stockcomp` (18 проверок: 4 товара и 2 роли — с продажами и без, с остатками и без, владелец и покупатель) | Владелец: «какие ещё доработки, улучшения для карточек мы можем сделать?» |
| 2026-08-13 | Каталог, **расчёт заказа переписан по правилам управления запасами** + **калькулятор цены поставщика** (владелец: «посмотри, как правильно делается рекомендация сколько заказать, чтобы вычисления были правильными» и «хочу универсальный калькулятор, чтобы сравнить разницу с текущими поставщиками и с новым, который предлагает выгодную цену»). **Расчёт заказа.** Вчерашняя версия была прикидкой: «осталось меньше 7 дней — пора заказывать, взять на 14 дней». Это неверно, потому что не учитывает главное — сколько дней идёт доставка. Сверился со стандартной схемой (reorder point / safety stock / order-up-to level): точка заказа ROP = средний дневной спрос × (срок доставки + страховой запас); уровень пополнения S = спрос × (доставка + периодичность заказа + запас); объём заказа Q = S − остаток, округлённый ВВЕРХ до упаковок. Заказывать надо не «когда мало», а когда остатка хватит ровно до приезда следующей поставки: при доставке за 1 день порог в 7 дней замораживает деньги, при доставке за 10 дней — товар кончится до приезда. Сроки каталог знать не может — добавлен экран **«Правила заказа»** (доставка / периодичность / страховой запас, по умолчанию 3 / 7 / 3 дня) с живым примером, значения хранятся в каталоге иобщие для магазина. В карточке расчёт можно раскрыть («Откуда это число») и проверить по шагам. Честно названо то, чего расчёт НЕ знает: товар в пути (учёта заказов нет) и упущенные продажи (если товара не было на полке, «продано» занижено, а спрос выше) — плюс предупреждение, если отчёт продаж старше 45 дней. **Калькулятор цены** в карточке (кнопка под списком поставщиков): вводишь цену нового поставщика, отмечаешь «за штуку / за упаковку», при желании скидку в % — каталог приводит предложение к цене за штуку и сразу показывает разницу с лучшей текущей ценой и с каждым поставщиком, наценку при нашей рознице (с предупреждением, если продаём в убыток) и выгоду в рублях за месяц по нашим продажам. Бамп SW v59. Проверено `ordercalc` (26 проверок: точка заказа и её изменение от срока доставки, разбивка расчёта, экран правил с живым пересчётом и сохранением, калькулятор в 5 сценариях — за упаковку, со скидкой, за штуку, в убыток, без количества в упаковке) | Владелец: «как правильно считается сколько заказать»; «универсальный калькулятор сравнить разницу с поставщиками» |
| 2026-08-13 | Каталог, **калькулятор сравнивает только со свежими ценами, старые уходят в историю** (владелец: «пусть калькулятор не делает сравнение цены у тех поставщиков, которые больше месяца прошло; можно чтобы показывал историю с датой поступления»). Замечание по делу: цена без поступления больше 30 дней — это уже не цена, а воспоминание, поставщик её мог давно поменять, и сравнивать с ней новое предложение значит обманывать себя. В карточке товара такое правило уже действовало («✓ выгоднее» не даётся устаревшей цене), а в калькуляторе — нет. Теперь «лучшая цена», процент разницы и выгода за месяц считаются ТОЛЬКО по ценам за последние 30 дней. Старые цены не выброшены: они показаны отдельным списком «Не сравниваем — поступлений давно не было» с датой каждой, и добавлена раскрывающаяся история всех цен и поступлений по всем поставщикам (дата · поставщик · цена за штуку, от свежих к старым). У свежих поставщиков в строке теперь тоже видна дата поступления. Если свежих цен нет вовсе — калькулятор прямо говорит, что сравнивать не с чем, и всё равно считает цену за штуку, упаковку и наценку. Бамп SW v60. Проверка в `ordercalc` усилена третьим поставщиком с самой НИЗКОЙ ценой (30 ₽) от 10.05: если бы устаревшая цена участвовала в сравнении, вывод перевернулся бы с «дешевле» на «дороже» — тест это ловит | Владелец: «пусть калькулятор не сравнивает цены старше месяца; покажи историю с датой поступления» |
| 2026-08-13 | Каталог, **аудит: найдено и исправлено 5 дефектов, четыре из них в разборе файлов 1С** (владелец: «сделай аудит и найди ошибки и исправь»). Проверка шла тремя слоями: статический разбор кода (дубли id, обращения к несуществующим элементам, классы без стилей), прогон в браузере всех экранов и кнопок в трёх ролях на пяти «трудных» товарах (весовой, без цены, закончился, без штрихкода, упаковка без количества), и отдельный тест разбора файлов на «грязных» данных. Браузерный прогон сбоев не нашёл — все находки оказались в разборе файлов, куда мышкой не добраться. **1. Числа с тремя знаками после запятой понимались в тысячу раз больше** — самый опасный: 1С выгружает количества и коэффициенты как «1,000» (это 1), «2,500» (2,5 кг), «48,000» (48 шт), а денежный разборщик считал такую запятую разделителем тысяч. Остаток 2,5 кг превращался в 2500 кг, упаковка из 48 штук — в 48 000, и весь расчёт заказа и цены за штуку врал в тысячу раз. Не всплывало раньше только потому, что в xlsx такие ячейки обычно числовые и разбор строки не запускается — но любой пересохранённый файл или CSV это ломает. Заведён отдельный разборщик количеств: запятая всегда десятичная (русский формат), разделитель тысяч — пробел; для цен старое правило оставлено («1,234» — это 1234 ₽). **2. Строка «Итого» в прайсе становилась товаром** — в трёх других разборщиках защита была, в разборе прайса её не было. **3. Код с разделителем тысяч не приводился к единому виду в прайсе** — «1 463» из прайса и «1463» из остатков считались разными товарами и раздваивали номенклатуру; теперь все разборщики чистят код одинаково (`normCode` убирает и обычный, и неразрывный пробел). **4. Справочник единиц опознавался только по служебной строке «Имя объекта: Единицы измерения»** — тот же файл без неё каталог не понимал вовсе; добавлено опознание по составу колонок («Коэффициент» + колонка единиц). **5. Устаревшая цена в карточке поставщика показывалась на зелёном фоне «всё хорошо»** — класс `.supview-price-old` был в разметке, но стилей к нему не существовало. Бамп SW v61. Проверено `parsers` (20 проверок разбора на грязных данных) и `audit` (все экраны, кнопки, сортировки, поиск, калькулятор на граничных вводах, проверка что покупатель не видит внутреннего); прежние тесты прошли | Владелец: «сделай аудит и найди ошибки и исправь» |
| 2026-08-13 | Каталог, **покупателю без пароля показываем код товара и ленту «Новое в магазине»** (владелец: «покажи людям ещё код товара, добавь что нового в магазине»). **Код товара** добавлен в открытый файл витрины и показан в карточке до проверки входа: по нему покупателю проще объяснить кассиру, что он берёт, и найти товар поиском. Остальное внутреннее закрыто как было — артикул, штрихкоды, поставщики, закупочные цены, наценка, продажи, остаток. **«Новое в магазине»** — лента на главной над «Популярным», видна всем. «Новое» считается по дате ПОЯВЛЕНИЯ товара в каталоге (`created_at`), а не по дате последнего завоза (`arrival_at`): иначе в новинки каждую неделю попадали бы хлеб и молоко, которые просто регулярно привозят. Срок — 30 дней, показываем до 12 товаров, от самых свежих. Отдельно предусмотрен случай ПЕРВОЙ загрузки каталога: тогда дата появления у всех товаров одна и лента показала бы весь ассортимент — поэтому если «новых» больше пятой части каталога, лента молчит. Бамп SW v62. Проверено `guestnew` (12 проверок в трёх сценариях: обычная работа, первая загрузка каталога, новинок нет); правила в `card` и `audit` обновлены под новое решение по коду товара. **Осталось нерешённым:** цены конкурентов по-прежнему видны покупателю без пароля, включая строку «Дешевле в магазине „Магнит“» — вынесено владельцу, решения пока нет | Владелец: «покажи людям ещё код товара, добавь что нового в магазине» |
| 2026-08-13 | Каталог, **переустройство навигации по образцу продуктовых приложений + наличие товара + тесты перенесены в репозиторий** (владелец: «сделай как у топовых продуктовых магазинов или складов, доработай каталог, обнови дизайн и логику, архитектуру»). Сразу оговорено: копировать конкретную сеть — её логотип, шрифты, фирменные цвета — нельзя, это чужая собственность; взято то, что у всех топовых каталогов общее. **1. Нижняя панель разделов** (Каталог · Категории · Избранное · Ещё) — так устроено любое продуктовое приложение: разделы под большим пальцем, а не в шапке. Раньше единственным входом в категории была кнопка фильтра, и с главной каталог просто не «просматривался». На «Избранном» — счётчик отмеченных. **2. Экран категорий плитками** — иконка, название, число товаров; тап открывает «Каталог» с этим фильтром. Категории (13 + «Прочее») в коде были давно, но жили только внутри фильтра. **3. Наличие товара словами** — «Есть в магазине» / «Заканчивается» / «Нет в наличии». В открытую витрину кладём НЕ число остатка (внутренние данные), а одно слово `stock_state`; «заканчивается» считается по продажам (хватит меньше чем на 2 дня), а если продаж нет — по простому порогу. Остатков не загружали — молчим, врать про наличие хуже, чем не сказать. На плитках показываем только «Мало» и «Нет»: море зелёных «Есть» было бы шумом. **4. Тесты переехали в `catalog/tests/`** с общей обвязкой, запускалкой `run.js` и README — временная папка обнулялась при перезапуске контейнера уже трижды, набор проверок терялся целиком. **Найдено при проверке:** новый обход экранов сразу поймал ошибку, внесённую в этой же работе — обработчик кнопки «Войти» вешался ДО присваивания функции и запомнил пустую заглушку, кнопка молчала. Бамп SW v63. Проверено `navigation` (21 проверка) и `screens` (обход всех карточек, разделов, пунктов меню, сортировок, поиска и приватности покупателя); обе прогнаны на старом коде и падают там | Владелец: «сделай как у топовых магазинов, обнови дизайн, логику, архитектуру» |
| 2026-08-13 | Каталог, **цены конкурентов закрыты от покупателя, старые экраны загрузки удалены** (владелец: «пусть цены конкурентов не будут видны покупателю, удали старые экраны загрузок»). **Цены чужих магазинов.** Прятать блок в приложении оказалось мало: репозиторий публичный, а цены лежали в ОТКРЫТОМ файле `data/competitors.json` на сайте — его мог скачать кто угодно по прямой ссылке. При разборе выяснилось, что этот файл читают ТОЛЬКО незалогиненные посетители: вошедший сотрудник берёт цены из своего зашифрованного каталога. То есть после закрытия блока файл стало некому читать — и он удалён целиком: не публикуется, не качается, не существует. Записанная цена теперь едет в зашифрованный каталог (`publishFull`), а не в открытый файл. Побочно: сохранить цену магазина теперь может только владелец — у сотрудника нет пароля для публикации; раньше это формально мог сделать и сотрудник, но лишь при наличии ключа GitHub, которого у сотрудников и так нет. **Старые экраны загрузки** (отдельно прайс, остатки, продажи, контакты, фото) удалены — их заменил единый «Импорт данных», открыть их было нельзя уже давно. Вместе с разметкой убраны их разборщики файлов и обработчики; функции загрузки, которые зовёт умный импорт, оставлены и почищены от обращений к исчезнувшим кнопкам и полям (вместо кнопки — пустышка, период продаж без полей берётся из файла). Минус 87 строк разметки и 5 мёртвых функций; проверено, что код больше не обращается ни к одному исчезнувшему элементу. Бамп SW v64. Проверено `import` (восстановлен потерянный тест разбора файлов + проверка, что умный импорт цел и старых экранов нет) и `screens` (покупатель не видит цен магазинов, сотрудник видит); обе прогнаны на старом коде и падают там. **Найдено в тестовой обвязке:** повторная установка того же адреса `#p=…` не считается переходом, карточка не перерисовывалась — проверка смотрела на старое содержимое; исправлено сбросом адреса | Владелец: «цены конкурентов не видны покупателю; удали старые экраны загрузок» |
| 2026-08-13 | Каталог, **исправлен порядок обработки файлов при загрузке нескольких сразу** (владелец прислал экран загрузки: «была ещё такая ошибка, не все распознал» — в отчёте «кодов без товара: 228» и «не нашлось товаров: 55»). Причина найдена в коде: справочники привязываются к УЖЕ существующим товарам, а словарь единиц обрабатывался ПЕРВЫМ — раньше прайса, розницы и остатков. Товары, появлявшиеся в этой же загрузке, справочник искал в каталоге, где их ещё не было, и оставлял без фасовок. Обоснование прежнего порядка («чтобы штрихкоды сразу знали количества в упаковке») оказалось неверным: штрихкоды хранят единицу строкой как есть, а расшифровывается она уже при показе карточки, — словарь им при загрузке не нужен. Новый порядок: прайс → розница → остатки → единицы → штрихкоды → фото → продажи → контакты. Отдельно: **число «не нашлось» само по себе бесполезно** — теперь в строке файла показываются примеры кодов, а полный список выгружается кнопкой «Скачать список того, что не нашлось в каталоге», чтобы владелец сверился с 1С (чаще всего это снятые с продажи позиции). Бамп SW v65. Проверено `import-order`. **Первая версия теста была НЕДОСТОВЕРНОЙ**: порядок был переписан внутри самого теста, и проверка подтверждала сама себя — на старом коде проходила. Порядок вынесен в приложении в именованную константу и отдаётся тесту; теперь на старом коде проверка честно падает | Владелец: (экран загрузки) «была ещё такая ошибка не все распознал» |
| 2026-08-13 | **Каталог — инструмент СОТРУДНИКОВ, а не витрина для покупателей** (владелец: «этот каталог не для покупателей, а для сотрудников магазина, учитывай это и запомни»). Это было записано в `docs/08-CATALOG.md` с самого начала, но в работах последних дней я рассуждал «что показать покупателю» и на этом ошибся. Записано жирно в двух местах памяти: врезка в начале `08-CATALOG.md` и раздел приватности в скиле `catalog-dev` (там же исправлена формулировка «покупатель без входа» → «сотрудник без входа»: сама фраза подталкивала к ошибке). **Исправлено нарушение утверждённого правила:** признак наличия товара («Есть в магазине» / «Заканчивается» / «Нет в наличии») я двумя днями ранее положил в ОТКРЫТЫЙ файл витрины, рассудив «покупателю полезно». Но правило гласит: без входа каталог не показывает ни цен, ни остатков — ссылка публичная, открыть её может кто угодно, а «заканчивается» это тот же остаток, просто округлённый. Теперь наличие считается на устройстве вошедшего из живого числа и в открытый файл не попадает вовсе; без входа не показывается ни на плитке, ни в карточке. Коды товара и штрихкоды по-прежнему видны сразу — ради них каталог и делался. Бамп SW v66. Добавлена проверка «в открытой витрине нет внутренних полей», которая смотрит не на интерфейс, а на САМ публикуемый файл: скрыть блок в приложении мало, достаточно положить лишнее поле — и данные утекут по прямой ссылке. Обе проверки прогнаны на коде до исправления и падают там. **Отдельно:** контейнер перезапустился и откатил рабочую копию на 13 коммитов назад; всё было цело на сервере, восстановлено `git reset --hard origin` | Владелец: «каталог не для покупателей, а для сотрудников — запомни» |
| 2026-08-13 | Каталог, **быстрый путь к коду товара** (владелец: «сделай полный анализ каталога и скажи, что можешь сделать для сотрудников» → выбрал этот пункт). Разбор вёлся по одной мерке: помогает ли это человеку, который стоит в зале с телефоном. Главная задача из `08-CATALOG.md` — сотрудник зала сел за кассу и должен узнать код товара — решалась за пять действий, а сам код был подан как мелкая серая метка среди прочих тегов. **Найдено при разборе:** (1) плотный вид ПРЯТАЛ код — `.grid.compact .card-tags { display: none }`, то есть сотрудник, уплотняя список, терял ровно то, за чем пришёл; (2) фильтры «Без фото / Без цены / Без ШК» — это гигиена каталога, работа админа, а показывались всем, включая невошедших. Сделано: код вынесен из кучи тегов в отдельный крупный моноширинный элемент, виден во всех режимах; **тап по коду копирует его и НЕ открывает карточку** — сотруднику за кассой нужен код, а не описание; добавлен третий режим показа **«список»** (кнопка режимов теперь идёт по кругу плитки → плотные → список): без фото на экран влезает вдвое больше товаров (8 против 4 на 390×900), код крупный и всегда на одном месте справа; метка «без ШК» переехала на плитку — такие товары пробивают по коду; админские фильтры скрыты от зала. Бамп SW v67. Проверено `code-first` (16 проверок), на старом коде падает. **Найдена собственная ошибка в стилях:** у базового `.card` стоит `flex-direction: column`, и в строке списка код уезжал ПОД название — строка вырастала со 85 до 117 px, список терял смысл. Поймано замером «сколько товаров влезает на экран», а не глазами: сама по себе строка выглядела прилично | Владелец: «полный анализ каталога, что можешь сделать для сотрудников» |
| 2026-08-13 | Каталог, **чистка мусора и аудит** (владелец: «сделай чистку от мусора в коде и в дизайне, убери лишнее, найди баги и исправь»). Удалено: два блока НЕДОСТИЖИМОГО кода — `renderChips` (75 строк) и `renderRecentProducts` (23 строки) целиком лежали за `return`, обе ленты владелец убрал ещё раньше, а код остался; вместе с ними их обработчики нажатий, элементы разметки (`groupChips`, `subChips`, `recentStrip`), функция `chipHtml` и второй тестовый вход `window.__catalogTest`, которым никто не пользовался. Из стилей убрано 26 мёртвых правил (`.sup-card`, `.scan-btn`, `.filter-btn`, `.tag-code` и другие — от давно переделанных экранов) и дубль `.grid.compact .card-tags`, который я сам добавил накануне. Итог: app.js 6760 → 6609 строк, styles.css 1179 → 1133, разметка чище. Проверено формально: обращений к несуществующим элементам нет, классов CSS без применения нет, функций без вызова нет. **Найден и исправлен настоящий баг:** умный импорт для файла с фото передавал загрузчику СЫРЫЕ строки файла, а тот ждёт готовое соответствие «товар → ссылка» — строки к товарам никто не привязывал (`matchPhotoRows` осиротела). В базовом режиме это записало бы мусор вместо ссылок; в бесплатном режиме тип «фото» просто пропускался, поэтому дефект не всплывал | Владелец: «чистка от мусора, убери лишнее, найди баги» |
| 2026-08-13 | Каталог, **каждое устройство помнит свои настройки + экран «Это устройство»** (владелец: «хочу, чтобы каталог запомнил каждое устройство и сохранял настройки, которые делает каждое устройство»). Большинство настроек и так хранилось на телефоне, но **две терялись**: режим показа «список» сохранялся и НЕ восстанавливался (в проверке при загрузке допускались только «плитки» и «плотные» — мой недосмотр накануне), и не запоминался открытый раздел нижней панели. Исправлено. Добавлен экран **«⚙️ Это устройство»**: видно, что именно телефон помнит (вход, вид списка, раздел, тема, избранное, фильтры, сохранённый каталог для работы без связи, ключ публикации), можно дать устройству имя («Касса 1», «Телефон Ани») и сбросить настройки одной кнопкой. Сброс намеренно НЕ трогает сохранённый каталог и ключ публикации: иначе сотрудник остался бы без данных офлайн, а владелец потерял бы доступ к публикации из-за случайного нажатия. Заодно исправлен тупик: «Ещё» у невошедшего сразу требовал пароль — теперь открывает настройки устройства, а кнопка входа стоит там же. Настройки хранятся только на самом устройстве, на сервер ничего не уходит и соседнему телефону не мешает. Бамп SW v69. Проверено `device` (19 проверок на ДВУХ устройствах: настройки переживают перезагрузку, второе устройство начинает со своих, имя одного не появляется у другого, сброс чистит настройки и не трогает каталог с ключом) | Владелец: «каталог запомнил каждое устройство и сохранял его настройки» |
| 2026-08-13 | Каталог, **два уровня категорий и раскладка поиска по категориям** (владелец: «пусть каталог показывает по категориям при поиске, по группам которые распределены — чтобы поиск товара был удобный, понятный и приятный»). Групп из 1С больше двухсот — простым списком их не просмотреть, поэтому они давно раскладываются по 13 понятным категориям, но раскладка нигде не показывалась. Сделано: **экран «Категории» стал двухуровневым** — плитка категории показывает, сколько в ней товаров И сколько групп; тап раскрывает список настоящих групп 1С с количествами, плюс кнопка «Показать все» на категорию целиком и возврат «‹ Все категории». Товар находится в два тапа вместо поиска вслепую. **Результаты поиска раскладываются по категориям**: заголовки «🥛 МОЛОЧНОЕ 1», «🍬 СЛАДОСТИ 1» вместо плоской простыни карточек. Заголовки появляются ТОЛЬКО при поиске (при листании каталога целиком они мешают), только если категорий больше одной (иначе заголовок ничего не делит) и не в режиме списка (он и так плотный, там нужен код). Бамп SW v70. Проверено `navigation` (расширен: второй уровень, группы внутри категории, возврат, раскладка поиска, отсутствие заголовков без поиска). **Своя ошибка по ходу:** правка добавила ИСПОЛЬЗОВАНИЕ переменных `grouped`/`catOf`/`catIconOf`, а их объявление не записалось из-за сбоя скрипта — сетка перестала рисоваться совсем. Поймано полным прогоном тестов сразу после правки: 11 замечаний «карточка не открылась», «сетка опустела» | Владелец: «показывать по категориям при поиске, по группам которые распределены» |
| 2026-08-13 | Каталог, **эмодзи убраны, оформление приведено к стилю iOS** (владелец: «убери эмодзи, сделай дизайн в стиле iOS»). **Почему это правильно:** эмодзи рисуются шрифтом системы — на Android, iPhone и Windows они разного вида, размера, веса и цвета, и в единый интерфейс не складываются. Заведён один набор контурных значков в духе системных иконок iOS: 40 значков интерфейса + 13 значков категорий, одна толщина линии (1,7), скруглённые концы, цвет наследуется от текста, размер задаётся классом. В разметке значки помечены `<i data-ic="имя">` и подменяются на рисунки при запуске; в коде — функция `ic()`. Там, где значок ничего не добавлял (кнопки меню, заголовки экранов), он просто убран — в iOS такие строки подписываются словами. Всего заменено или убрано 157 мест. **Оформление:** скругления 20/14/10 → 14/10/8 (20 давали «пузырьковый» вид, в системных приложениях так не делают); шрифтовой стек с SF Pro впереди и сглаживанием; шапка и нижняя панель — полупрозрачные с размытием, как «материал» iOS, с запасным вариантом для браузеров без поддержки; разделители в пол-пикселя вместо рамок вокруг каждой строки (в iOS рамка идёт вокруг ГРУППЫ строк, а строки разделяет линия) — так переделан список групп внутри категории; второстепенные кнопки стали спокойнее; табличные цифры в ценах и кодах, чтобы числа не «плясали». Палитру не трогал: она и так системная — те же серые и индиго, что в iOS. Бамп SW v71. Добавлена проверка `no-emoji` — читает исходники и падает, если эмодзи вернутся; заодно следит, что у каждой категории есть рисунок. **По ходу:** массовая замена значков регулярным выражением сломала синтаксис (кавычки в шаблонных строках) — откатил и переделал точечно, по одной строке с проверкой; плитки категорий печатали «null», потому что значок брался из поля напрямую | Владелец: «убери эмодзи, сделай дизайн в стиле iOS» |
| 2026-08-13 | Каталог, **сам раскладывает товар по категориям — по названию, а не по группе 1С** (владелец: «не всегда товар правильно занесён в нужную группу; хочу, чтобы каталог понимал по названию либо по штрихкоду через интернет и правильно распределял и старые товары, и новые»). Раньше категория бралась ТОЛЬКО из названия группы: группа обобщённая («Товары») или неверная — товар оказывался не на своей полке. Новый порядок: **1)** ручная правка или найденное в интернете (поле `category`), **2)** САМО НАЗВАНИЕ товара, **3)** название группы, **4)** «Прочее». Правила по названию привязаны к НАЧАЛУ строки — в выгрузках 1С название почти всегда начинается с самого товара («Молоко Простоквашино», «Шампунь Head & Shoulders»); в середине «крем-сода» ловилась бы как косметика. Тринадцать наборов правил, «Детское» проверяется первым (подгузники подходят и под гигиену). Проверено на 29 настоящих названиях, сваленных в одну мусорную группу: **все 29 разложены верно**, неузнанное честно уходит в «Прочее». Результат кэшируется в служебном поле, иначе тринадцать правил на 17 000 товаров считались бы при каждой отрисовке. Категория уходит и в открытую витрину — раскладка одинаковая до входа и после. **Второй слой — интернет:** для товаров, оставшихся в «Прочем» и имеющих штрихкод, кнопка «Разложить по категориям» спрашивает открытую базу Open Food Facts (`categories_tags`) и переводит её английские и французские категории в наши; в меню видно, сколько товаров ещё не разложено. Сбой сети и «база не знает» различаются, три неответа подряд — остановка. Бамп SW v72. **Найдена та же ловушка с границей слова, что и в разборе 1С:** в JS кириллица не считается словесным символом, поэтому «сок\\b» НЕ срабатывало на «Сок Добрый» — «Сок» и «Сыр» уходили в «Прочее». Заменено на «сок(?![а-яё])»; в комментарии оставлено предупреждение | Владелец: «пусть каталог сам распределяет товар по категориям, понимая по названию или штрихкоду» |
| 2026-08-13 | **Каталог перешёл на одну архитектуру: сервера больше нет** (владелец на вопрос «сервер Timeweb ещё используется?» ответил «сервер больше не нужен»). До этого каталог жил в ДВУХ режимах сразу — база Supabase на Timeweb и бесплатная публикация на GitHub, — и почти всё было сделано дважды: два импорта, две записи цен, два входа, два способа хранить фото. Разные ветки ломались по-разному: та ошибка с фото из Excel, что нашлась при чистке, жила только в серверной ветке и потому не всплывала. Удалено: 31 функция, существовавшая только ради базы (`impUpload`, `stockUpload`, `salesUpload`, `contactsUpload`, `photoExcelApply`, `fullLoadProducts`, `deltaSyncProducts`, `applySession`, `loadContacts`, `loadCompetitors`, вся очередь предложенных фото, управление поставщиками и контактами, загрузка фото в хранилище), серверные хвосты в двух десятках функций с двумя ветками, библиотека `supabase.min.js`, папка `setup/` целиком (схема и миграции — 2 854 строки SQL, 268 КБ), лишние настройки в `config.js` (адрес базы, ключи, служебные почты). Итог: **app.js 6 953 → 5 798 строк (−17%)**, разметка 739 → 696, обращений к базе 65 → 0, проверок «в каком мы режиме» 43 → 32 (остались как признак «вошёл»). Что потеряно вместе с сервером и об этом сказано владельцу: правка контактов поставщика вручную (контакты по-прежнему приходят из 1С), очередь предложенных покупателем фото, съёмка фото на камеру с сохранением в хранилище, история розничной цены. Авточистка дублей перевешена с серверного входа на вход владельца. Все восемь наборов проверок проходят после каждого шага — правка шла кластерами, а не одним махом | Владелец: «давай обновим архитектуру?» → «сервер больше не нужен» |
| 2026-08-13 | Каталог, **app.js разложен на 15 модулей** (владелец: «раздели app.js на модули»). Было 5 798 строк в одном файле, найти что-либо тяжело, а править рискованно — я сам за сессию дважды ломал то, чего не трогал. Использованы штатные модули браузера (`<script type="module">`), сборщика нет: деплой остался прежним. Разделение шло по уже размеченным разделам файла: `store` (состояние и кэш), `core` (помощники), `icons`, `catalog` (категории и поиск), `render`, `card`, `publish`, `imports`, `photos`, `admin`, `competitors`, `device`, `scanner`, `data`, `app` (связывание и запуск). Самый крупный модуль — 940 строк вместо 5 798. **Главная трудность:** модуль не может присваивать импортированной переменной, а 20 переменных менялись из разных мест (`currentProduct`, `secretPw`, `cardSales`, `compFilter`…). Все они сведены в один объект `ui` из `store.js` — полю объекта присваивать можно. **Три поломки, найденные и исправленные по ходу:** (1) генератор срезал строку `})();` по тексту — и заодно убил закрывашку маленького IIFE ВНУТРИ `bindEvents`; теперь обёртка снимается по месту, а не по содержимому; (2) `const CAT_NAMES` вычислялся при загрузке модуля и падал на «Cannot access CATEGORIES before initialization» — из-за кольцевых импортов список категорий ещё не готов; сделан ленивым; (3) тестовый доступ `window.WM_PUBLISH` собирался на верхнем уровне `imports.js` и падал на `norm` по той же причине — перенесён в точку входа, где всё уже загружено. Бамп SW v75. Добавлена проверка `modules`: нет лишних импортов, все импорты разрешаются, нет экспорта «в никуда», ни один модуль не больше 1200 строк, офлайн-кэш знает про все файлы. Она же сразу нашла 21 лишний импорт после переноса тестового доступа | Владелец: «раздели app.js на модули» |
| 2026-08-13 | **Память о каталоге приведена в соответствие с кодом.** Инструкция разработчика (`.claude/skills/catalog-dev/`) всё ещё описывала сервер: «бэкенд Supabase», ключи в настройках, миграции в `catalog/setup/` (папки уже нет), моки базы в тестах, ориентиры «в app.js» (файла уже нет). Такая память опаснее её отсутствия — по ней следующая правка пошла бы в никуда. Переписаны разделы «что это за приложение» (работа без сервера, зашифрованные файлы, токен только на устройстве владельца), «приватность» (единственная настоящая граница — что попадает в открытую витрину, серверной защиты больше нет), «публикация данных» вместо «миграций базы», «тесты» (они теперь в `catalog/tests/`, проверять данные, а не картинку), «ориентиры» по модулям. Удалены два устаревших файла-справочника. То же поправлено в `CLAUDE.md` | Порядок в документах — часть работы, а не довесок |
| 2026-08-13 | **Ревизия репозитория: убрано всё, что осталось от прошлых попыток** (владелец: «не нужно, удали, сделай ревизию»). Удалено 37 файлов, ~22 000 строк и ~1,5 МБ: (1) **эпоха Excel** — таблицы `WAY_MARKET_v8/v9/ideal/demo`, `ФИНАНСОВЫЙ_КОНТРОЛЬ`, `Auron_шаблон`, макросы `.bas` и одиннадцать скриптов-сборщиков этих таблиц (`build_*.py`, `_patch_*.py`, `fill_year_wm9.py`, `test_wm9.py`, `tools/make_template.py`); (2) **три заброшенных бэкенда** — `webapp/` (Google Apps Script, 1 652 строки), `server/` (Node + Express, 1 359 строк), `cloudflare/` (прокси к облачному Supabase — не нужен, база теперь своя), `supabase/` (схемы облачной базы, заменены на `infra/migrations`, где колонки совпадают с `api.js`). **Оставлено с причиной:** `infra/` — схема действующей базы приложения Auron; `prototype/index.html` — на него как на эталон иконок ссылается утверждённый `04-DESIGN-SYSTEM.md`; `scripts/process_sales.py` — расчёт себестоимости FIFO, пригодится модулю розницы. Ничего не пропало безвозвратно: удалённое остаётся в истории git. **Найдено, но не тронуто (чужая ветка, приложение Auron):** на сайте лежит `app_bundle.html` (132 КБ) — старая самостоятельная копия приложения, на неё никто не ссылается, но она открывается и содержит адрес базы; `sw.js` приложения заранее скачивает `supabase.min.js` и `chart.min.js` (~400 КБ), которых нет в разметке; копия `app/` в ветке каталога отстала от рабочей ветки приложения на три файла | Владелец: «не нужно, удали, сделай ревизию» |
| 2026-08-13 | **Розничная цена остаётся открытой** (владелец на вопрос «цена с полки лежит в открытом файле — закрыть или оставить?» ответил «оставить открытой»). Причина владельца: этот ценник и так висит в зале, тайны в нём нет, а сотруднику без входа он полезен сразу. Правило приватности уточнено, чтобы код и документ не противоречили: без входа закрыты **закупочные цены, остатки, продажи, поставщики и их контакты, цены чужих магазинов**; открыты названия, коды, штрихкоды и розничная цена. Поправлены врезка в `08-CATALOG.md` и раздел приватности в инструкции разработчика | Владелец: «оставить открытой» |
| 2026-08-13 | **Каталог публикуется по частям** (владелец выбрал это первым шагом обновления архитектуры). Было: любая мелочь — новая цена конкурента, одно фото, правка названия — переписывала ВЕСЬ каталог: витрину 4,7 МБ и два зашифрованных файла по 17,4 МБ, итого ≈39 МБ и несколько минут; оборвалась связь на середине — данные обновлены наполовину. Стало: каталог нарезан на куски со своей подписью содержимого, и на GitHub уезжают только изменившиеся. Витрина — 16 кусков `data/p/00.json…` плюс открытая опись `data/index.json`; закрытая часть — зашифрованная **опись** (в ней мелочь: группы, поставщики, контакты, правила заказа) плюс куски товаров (24), закупочных цен (12) и продаж (6) в `data/sec/` и `data/sec-staff/`. Проверено на каталоге из 400 товаров: первая публикация — 105 файлов, после правки ОДНОГО товара — **6 файлов вместо 105**, причём цены и продажи не переписывались вовсе. **Три решения, без которых экономии бы не было:** (1) номер куска — устойчивый хэш от id товара, а не позиция в списке, иначе один новый товар сдвинул бы границы всех кусков; (2) «соль» шифрования берётся из прошлой описи и меняется только при смене пароля — случайная соль на каждую публикацию заставляла бы перешифровывать всё; (3) в адресе куска стоит его подпись, поэтому service worker кэширует куски навсегда (кэш `wm-data-v1` переживает обновление приложения) — вход сотрудника после мелкой правки докачивает один кусок, а не весь каталог. Попутно: файлы уезжают пачками по четыре (было по одному), видно ход публикации («Публикую… 3 из 6»), «Опубликовать сейчас» больше не плодит пустой коммит, а «изменилось ли» сверяется с описью на самом GitHub, а не с памятью устройства (на новом телефоне это зря перезаливало витрину). Старый цельный файл по-прежнему читается — переход происходит сам при первой публикации. Деплой не выкладывает закрытые куски на сайт (`data/sec`, `data/sec-staff`). Бамп SW v76. Новая проверка `publish-parts` — с мини-GitHub в памяти; прогнана на старом коде и валится там, как и должна | Владелец: «давай обсудим, как обновить архитектуру» → выбрал «публикация по частям» |
| 2026-08-13 | **Откат рабочей папки больше не приводит к потерям** (владелец: «постоянная проблема — что-то откатилось, что-то потерялось, убери эту проблему»). Причина: контейнер, в котором идёт работа, иногда возвращает папку к старому состоянию — за одну сессию трижды. Запушенное всегда оставалось целым, пропадало только несохранённое, но замечал я это вручную и не всегда сразу. Сделаны два хука: `session-start.sh` в начале каждой сессии сверяет папку с GitHub и, если она отстала и несохранённых правок нет, молча восстанавливает (проверено на трёх случаях: откат без правок — восстановил; откат с правками — не тронул и предупредил; незапушенный коммит — предупредил); `check-pushed.sh` в конце ответа один раз не даёт закончить работу, которая ещё не на GitHub. Новые файлы, не добавленные в git, восстановлению не мешают — reset их не трогает. Правило «сделано = запушено» записано в `CLAUDE.md`. **Плюс разбор ошибок:** заведён `docs/ОШИБКИ.md` — семь разделов с реальными случаями этой сессии и правилом из каждого (потеря незапушенного; тест, сравнивавший сам себя; замена по всему файлу, сломавшая кавычки; резка кода по совпадению текста; лишнее поле в открытой витрине как утечка; кириллица и `\b`, ссылка на ещё не назначенную функцию, вёрстка «на глаз»; устаревшая инструкция после смены архитектуры; забытая четвёртая ветка при правке деплоя). Файл добавлен в таблицу обязательного чтения в `CLAUDE.md` | Владелец: «ты должен учиться на своих ошибках, сделать анализ и усовершенствоваться» |
| 2026-08-13 | **Закрытый каталог теперь один на всех, ключ — в «конвертиках» под каждым паролем** (следующий шаг обновления архитектуры). Было: один и тот же каталог шифровался ДВАЖДЫ — файл владельца и почти такой же файл сотрудника; разница между ними сводилась к одному полю. Это удваивало и хранение (два раза по 17,4 МБ), и работу при каждой публикации, а третья роль стоила бы ещё столько же. Стало: каталог один, зашифрован случайным «ключом каталога», а сам ключ лежит рядом в `keys.json` маленькими конвертиками — по одному на пароль (349 байт на оба). Проверено на 400 товарах: первая публикация **63 файла вместо 105**, мелкая правка — **4 файла вместо 6**, конвертики при этом не переписываются. Побочные выгоды: роль определяется тем, ЧЕЙ конвертик открылся (вход стал одной проверкой вместо двух попыток подряд), новая роль стоит полкилобайта, а не 17 МБ. **Важное решение по безопасности:** смена пароля сотрудника меняет ключ каталога и перешифровывает всё (42 куска). Это не расточительство — прежний сотрудник мог сохранить старый ключ на своём устройстве, и без смены ключа «смена пароля» была бы видимостью защиты; проверка это и подтверждает: старый пароль после смены не открывает каталог, новый открывает. Проверено также, что в открытом `keys.json` нет ни паролей, ни данных. Каталог старого формата (два цельных файла) по-прежнему читается и удаляется при первой же публикации. Бамп SW v78 | Владелец: «продолжай» (после выбора «публикация по частям» первым шагом) |
| 2026-08-13 | **Движок отделён от магазина: каталог стал универсальным** (продолжение обновления архитектуры; владелец: «сделать универсальным»). Раньше «Way Market» был вшит в разметку, стили, служебные файлы и в набор из 13 продуктовых разделов — сделать каталог для другого магазина значило править код в разных местах. Теперь ВСЁ, чем один магазин отличается от другого, лежит в одном файле `catalog/js/config.js`: название, подпись, логотип, цвет, набор разделов и список включённых возможностей. Новый модуль `brand.js` применяет это к странице: заголовок вкладки, шапка, цвет (тёмный фирменный цвет автоматически осветляется для текста, иначе на тёмной теме надписи были бы нечитаемы), а выключенные возможности просто исчезают из меню. Логотипа нет — рисуется кружок с первой буквой названия, чтобы чужой каталог не выглядел недоделанным. **Разделы:** `'grocery'` — наши 13 продуктовых (раскладка по названию товара), `'none'` — разделами становятся группы 1С как есть; экран «Категории» и дерево фильтров теперь строятся из того, что реально получилось, а не из зашитого списка — иначе у непродуктового магазина экран был бы пустым. Написана инструкция `catalog/КАК-СДЕЛАТЬ-СВОЙ-КАТАЛОГ.md` — семь шагов от «сделай копию» до «дай пароль сотрудникам», с честным предупреждением про ключ и про то, что пароль каталога восстановить некому. Новая проверка `store-config` запускает ОДИН И ТОТ ЖЕ движок дважды: как Way Market и как выдуманный «Стройдвор» (другое имя, цвет, без логотипа, без конкурентов и продаж, разделы из групп) — и убеждается, что оба работают, а в модулях не осталось зашитого названия. Бамп SW v79 | Владелец: «продолжай» |
| 2026-08-13 | **Быстрый первый вход: товары показываются по мере загрузки** (последний пункт обновления архитектуры). Было: приложение ждало ВСЕ куски витрины и только потом рисовало первый товар — на медленной связи это пустой экран на несколько секунд. Стало: каждый пришедший кусок сразу попадает на экран. Замерено проверкой (1 600 товаров, часть кусков приходит через 3 секунды): первые товары на экране **через 0,17 секунды вместо 3,2** — проверка прогнана на старом коде и валится там именно на этом. Поисковый индекс пересобирается не чаще раза в полсекунды: на 17 тысячах товаров полная пересборка на каждый кусок заметно тормозила бы телефон. **Попутно найдено и исправлено моё же дублирование:** я добавил чтение сохранённой копии в `refresh()`, не заметив, что при запуске она уже читается (`init`) — каталог грузился из памяти телефона дважды. Убрано; проверка «повторный заход не ждёт сеть» оставлена как сторож, чтобы показ по мере загрузки не сломал прежнее поведение. Ещё замечание к самой проверке: сначала она ловила появление «.card» и показывала 200 мс даже на старом коде — под этим селектором рисуется серый скелет загрузки, а не товар; теперь проверка ждёт именно товары. Бамп SW v80 | Владелец: «продолжай» |
| 2026-08-13 | **Экраны переведены на системный вид iOS; «Популярное» убрано; вкладка «Ещё» заменена на «Фильтры»** (владелец прислал снимок экрана: «убери показ популярные, обнови нижние вкладки, вместо вкладки ещё добавь настройки фильтра, экран фильтров сделай удобнее приятнее в стиле iOS, в принципе все экраны, виджеты, кнопки»). **Убрано:** лента «Популярное» на главной (сама раскладка «ходовых» осталась — по ней работает сортировка). **Нижняя панель:** вместо «Ещё» — «Фильтры» со значком ползунков: до фильтров теперь дотягивается большой палец, а меню и настройки устройства переехали на кнопку человечка в шапке (в окно входа добавлена строка «Настройки этого устройства», иначе сотрудник без входа потерял бы к ним доступ). **Экран фильтров переписан целиком:** шапка как в системных окнах («Сбросить» слева, название по центру, «Готово» справа), сгруппированные списки со скруглёнными краями и разделителями от текста, настоящий переключатель iOS (зелёный, как в системе — он там зелёный независимо от цвета приложения), сортировка — строками с галочкой вместо тесных кнопок, цена и даты — строками «От/До», кнопка «Показать N» всегда внизу. **Тем же набором частей** (`.ios-group`, `.ios-row`, `.ios-nav`, `.ios-switch`) перестроены меню администратора (три группы вместо стопки из 14 одинаковых кнопок, «Выйти» красным) и экран «Это устройство». **Карточка товара:** коды собраны в системный список, цена вынесена над ним, значение кода — крупно и белым, потому что ради кода карточку и открывают. Мелочи: круглая стрелка «назад» больше не рисуется поверх собственной шапки окна; раскрывающая стрелка в списке категорий нарисована фигурой, а не знаком «▸» из шрифта (на разных телефонах он разного размера). Проверки навигации дополнены: последняя вкладка — «Фильтры», она открывает окно фильтров с живым счётчиком, ленты «Популярное» на главной нет. Все 12 наборов проходят; каждый экран снят и просмотрен в тёмной и светлой теме. Бамп SW v81 | Владелец: «убери показ популярные… сделай удобнее приятнее в стиле iOS» |
| 2026-08-13 | **Полный аудит каталога: найдено и исправлено семь вещей** (владелец: «найди все ошибки и исправь, сделай полный аудит»). **(1) Экран «Поставщики» был мёртвым целиком** — кнопки рисовались, но ни переименование, ни удаление, ни контакты не работали: правка жила на сервере, а сервер убрали ещё в августе, экран же остался. Написан заново: строка открывает поставщика, там название, телефон, кто общается, примечание и удаление (с честным предупреждением, сколько товаров за ним числится); всё сохраняется на устройстве владельца и уезжает обычной публикацией. Заодно вернулась правка контактов поставщиков — то, что было потеряно вместе с сервером. **(2) Подписи строк меню ломали разметку:** код писал в строку через `textContent`, а внутри строки лежит `<span>` с названием — он стирался, и строка теряла вид. Появился помощник `setRowText`. **(3) Нижняя панель фильтров вылезала за край экрана** на 18 px с каждой стороны: от старой разметки остались отрицательные отступы, которые «раздвигали» панель до краёв окна, а панель с тех пор переехала. **(4) Три вида кнопок были мельче пальца** — в том числе копирование кода товара (23×30 px), то есть самое частое действие в зале; теперь область нажатия не меньше 44 px, вид не изменился. **(5) Расчёт заказа при неизвестных продажах** выдавал «пора заказывать» и «заказать 0» одновременно; теперь без данных о продажах совет не даётся вовсе. **(6) Пароль каталога владельца сменить было нечем** — при утечке оставалось только смириться. Добавлено «Пароль каталога (ваш)» с полной перешифровкой: прежний файл ключей навсегда остаётся в истории репозитория, поэтому без нового ключа смена пароля была бы обманом. Проверено: старый пароль после смены не открывает каталог, новый открывает. **(7) Устаревшие утверждения в документе каталога** («База данных — Supabase», «админ-доступ — вход Supabase») заменены на то, как есть сейчас. **Две новые постоянные проверки:** `audit` (кнопка без обработчика, ссылка на исчезнувший элемент, забытый элемент, эмодзи в разметке) и `layout` (на 320 и 390 px ничего не вылезает за край, кнопки не мельче пальца). Проверено также руками: приватность по трём ролям (без входа не видно ни закупки, ни остатков, ни поставщиков; сотрудник не видит правку каталога), арифметика заказа и калькулятора на четырёх наборах чисел, поиск (опечатки, часть слова, код с пробелом, штрихкод, латиница ↔ кириллица), поведение при сбоях (нет сети, битая витрина, пустой каталог, неверный пароль) — везде понятные сообщения и ноль сбоев JS. Все 14 наборов проверок проходят. Бамп SW v84 | Владелец: «найди все ошибки и исправь, сделай полный аудит» |
| 2026-08-21 | **Почта в поиске, ложное «Показать ещё» и уборка памяти** (владелец прислал снимки с живого сайта: «убери проблему, чтобы в поисковик не вставлялась почта — у меня в каталоге почты нет», «исправь ошибки при загрузке файлов», «чтобы каталог не ломался, не зависал, не собирал лишний мусор, освобождал память»). **(1) Почта в поиске** — это автозаполнение браузера: он узнавал поле по имени и подставлял сохранённый адрес. Имя поля теперь случайное при каждом запуске (узнать нечего), добавлены признаки «не для автозаполнения» для менеджеров паролей. А если адрес всё же попал в поиск, каталог отвечает по-человечески: «Это адрес почты — похоже, браузер подставил его сам», вместо бесполезного «ничего не нашлось». **(2) Найдена настоящая ошибка на том же снимке:** экран одновременно говорил «Ничего не нашлось» и предлагал «Показать ещё (осталось 17795)». Причина: при пустом результате показ прерывался раньше, чем убиралась кнопка от прошлого показа. Теперь при пустом результате сетка чистится и кнопка снимается. **(3) Загрузка файлов:** разобранные строки семи файлов (десятки тысяч) висели в памяти до перезагрузки страницы — теперь освобождаются сразу после загрузки. Формулировки в отчёте переписаны: «кодов без товара: 164» читалось как ошибка, хотя это позиции, которых нет в прайсе (обычно снятые с продажи) — так и написано, список по-прежнему можно скачать. **(4) Уборка памяти (`tidyMemory`)** — вызывается при входе и после каждой загрузки: история цены хранится по 8 последних записей на поставщика и не старше двух лет (самая свежая остаётся всегда, иначе товар потерял бы цену), отчёты продаж — последние 12 периодов, счётчик просмотров — 300 товаров. На проверке: 1200 строк истории → 480, при этом ни один товар не остался без цены. **(5) Кэш фото ограничен** 600 картинками (было без предела — сотни мегабайт на телефоне), лишние вытесняются старыми вперёд. **(6) Отметки «фото искали, не нашли»** хранились вечно и без даты: список рос вместе с каталогом, а товар, у которого фото появилось в открытой базе позже, больше не проверялся. Теперь с датой и забываются через 90 дней. Новая постоянная проверка `memory` следит за всем этим. Все 15 наборов проходят. Бамп SW v85 | Владелец: «убери проблему с потерей памяти, чтобы каталог не ломался, не зависал, не собирал лишний мусор» |
| 2026-08-21 | **Поиск по коду ускорен в 14 раз — каталог больше не «зависает» у кассы** (продолжение той же просьбы владельца: «чтобы не ломался и не зависал»). Замер на настоящем объёме (17 500 товаров): поиск по коду «1234» занимал **318 мс**, по штрихкоду — 218 мс, и это на компьютере; телефон в 3–5 раз медленнее, а поиск идёт на каждую нажатую цифру — то есть при наборе кода у кассы каталог фактически замирал на секунду после каждого нажатия. Причина: запрос из цифр прогонялся через полную оценку всех товаров с транслитом и разбором опечаток. Теперь цифровой запрос идёт коротким путём по кодам и штрихкодам: **22 мс вместо 318** при том же результате. Выдача не изменилась — оценки взяты те же самые (код: точно 120, начало 95, внутри 70; без знаков 118/92/68; название — через общий `matchPre`), и это проверено сравнением выдачи старого и нового пути на пяти видах запросов (коды, часть кода, штрихкод, цифры в названии, короткий код) — списки совпадают полностью. Нечёткий поиск для цифр отключён сознательно: код — вещь точная, «похожий» код опаснее честного «ничего не нашлось». Замеры остальных мест на 17 500 товарах: пересборка поискового индекса 334 мс, полная перерисовка 50 мс, раскладка по категориям 29 мс, память 87 МБ — это в норме. В проверку `memory` добавлен сторож скорости: поиск по коду должен укладываться в 60 мс | Владелец: «чтобы каталог не ломался, не зависал» |
| 2026-08-21 | **Разбор настоящих файлов 1С владельца: три улучшения импорта** (владелец прислал все семь своих выгрузок и снимок экрана: «исправь все ошибки, найди их, пусть всё будет правильно»). Прогнал файлы через настоящий разбор каталога в браузере: собирается 17 848 товаров, из них с кодом 17 750, со штрихкодом 17 362, с розничной ценой 17 822, с остатком 10 481; 24 494 строки цен поставщиков, 5 080 строк продаж, 673 поставщика — **сбоев нет, разбор верный**. То, что владелец принимал за ошибки, проверено поимённо: «нет в каталоге: 191» из справочника единиц — коды, которых действительно нет в прайсе (проверено поиском по коду и по названию — ни одного ложного), а справочник единиц в его выгрузке вообще не содержит названий, только «Номенклатура.Код», так что сопоставить их иначе нечем. **(1) Сопоставление по набору слов.** В 1С один товар пишут по-разному: в прайсе «Чабан Сметана 25% стакан 200г», в справочнике штрихкодов «Сметана Чабан 25% стакан 200гр» — штрихкод не привязывался. Теперь при неудаче сравнивается НАБОР слов с единой записью единиц (200гр = 200г, 1л, 500мл, 44шт). Строго: «Трусики 38шт» и «Трусики 44шт» не склеиваются, а если набор слов подходит сразу двум товарам — не используется вовсе. На настоящих данных: одним ненайденным штрихкодом меньше и на один лишний товар меньше. **(2) Продажи теперь отчитываются.** Раньше строки, не нашедшие товар, пропадали молча — «Ходовые товары» могли врать. Теперь в отчёте видно, сколько строк не совпало (на файле владельца — ноль). **(3) Дубли в самой 1С.** Обнаружились 47 пар товаров с одинаковым набором слов и РАЗНЫМИ кодами («7Days Круассаны Мини» ↔ «7Days Мини Круассаны»). Сами не сливаем — у каждого свой код, оба пробиваются на кассе, — но после загрузки каталог показывает: «Похоже на дубли в самой 1С: 47 пар», список скачивается. Чистить лучше у источника. **(4) Разборщик Excel больше не тянется из интернета** (был чужой CDN): своя копия в `catalog/vendor/xlsx.min.js`, в офлайн-кэше — проверено прогоном настоящих файлов с НАМЕРЕННО закрытым интернетом: всё загрузилось. Новые проверки закрывают все три случая, включая тот самый «200гр против 200г» из выгрузки владельца. Бамп SW v87 | Владелец: «исправь все ошибки, найди их, пусть всё будет правильно» |
| 2026-08-21 | **Три вещи для сотрудника зала** (владелец выбрал их из десяти предложенных). **(1) Проверка ценника крупно:** сканируешь штрихкод — и прямо в окне камеры крупно название, цена и код, читается с вытянутой руки. Карточку не открываем: при сверке ценников подряд она каждый раз перекрывала бы камеру; рядом кнопка «Открыть товар», если нужны подробности. **(2) Сканирование подряд:** камера больше не закрывается после каждого товара — просканировал, увидел цену, сразу следующий. Один и тот же код в течение 2,5 секунд повторно не считается (иначе кадр за кадром распознаётся одно и то же), при находке телефон коротко вибрирует. Режим выбирается переключателем в окне сканера («Открыть товар» / «Проверить ценник») и запоминается на телефоне. **(3) «Мои частые товары»:** телефон и так считал, что этот сотрудник открывает чаще всего, но нигде не показывал — теперь это лента на главной, с кодом прямо на плитке; хот-доги, выпечка и весовое открываются в одно касание. Список у каждого телефона свой и наружу не уходит, в ленту попадают товары, открытые не меньше двух раз. Попутно убран последний эмодзи в интерфейсе (заголовок «Новое в магазине») — заменён значком. Бамп SW v88 | Владелец: «дай 10 вариантов, чем облегчить работу сотрудникам зала» → выбрал первые три |
| 2026-08-22 | **Заказы поставщикам, сравнение товаров и защита от сбоев** (владелец: «сотрудники зала делают заказ у поставщиков — хочу оформлять заказ в каталоге: у кого, когда заказали, когда придёт, кто заказал, сумма; в виде календаря по дням недели, чтобы видеть, какие дни загружены и какие суммы»; «чтобы можно было сразу сравнить несколько товаров по ценам поставщиков»; «чтобы каталог не ломался и не зависал»). **(1) Заказы.** Новый экран: неделя столбиками — высота столбика равна сумме поставок за день, поэтому перегруженный день виден до того, как прочитаешь хоть одну цифру; под ней итог недели и список заказов, у каждого своя сумма, поставщик, кто заказал, когда заказали и когда придёт. Заказ можно отметить пришедшим или удалить; тап по дню сразу создаёт заказ на этот день. **Где живут заказы (сервера нет):** у владельца заказ уходит в каталог и виден всем после публикации, у сотрудника — лежит на его телефоне с пометкой «не отправлен» и одной кнопкой передаётся владельцу текстом в мессенджер. Так сотрудник не остаётся без инструмента, а записи не теряются. Проверка нашла настоящую недоработку по ходу: заказ, сохранённый на другую неделю, исчезал с глаз — теперь календарь сам переходит на неделю сохранённого заказа. **(2) Сравнение.** В карточке появилась кнопка «К сравнению», внизу главного экрана — полоска «Сравнить (N)». В таблице рядом: закупка (самая выгодная, приведённая к одной штуке), у кого она, розница, наценка и остаток; лучшая закупка среди отобранных подсвечена зелёным. До восьми товаров — больше на телефоне не читается. **(3) Чтобы не ломался.** Одна необработанная ошибка раньше обрывала всё, что шло за ней: экран замирал, и человек видел зависший каталог. Теперь такие случаи ловятся глобально: человеку — понятное сообщение (не чаще раза в 10 секунд), приложению — жизнь дальше, а последние 20 сбоев записываются на устройстве и видны в «Это устройство» → «Сбои приложения», чтобы владелец мог их назвать. Обновление каталога обёрнуто той же защитой. Три новые проверки (`orders`, `compare` и правки в остальных) — всего 17 наборов, все проходят. Бамп SW v90 | Владелец: «оформить заказ в каталоге… сравнить несколько товаров… чтобы не ломался и не зависал» |
| 2026-08-22 | **Автосохранение работы: откат больше ничего не уносит** (владелец: «сделай так что-бы не откатывалось»). Сам откат папки контейнера отменить нельзя — его делает среда, в которой идёт работа. Поэтому сделано так, чтобы терять было нечего: хук `.claude/hooks/autosave.sh` после **каждой** правки файла снимает слепок рабочей папки и кладёт его отдельным коммитом в ветку-копилку `claude/autosave-<ветка>` на GitHub. Снимок делается через отдельный индекс (`GIT_INDEX_FILE` во временной папке), поэтому рабочая ветка, индекс и история остаются чистыми; троттлинг 15 секунд и пуш в фоне, чтобы не тормозить работу. Хук начала сессии теперь не только возвращает откатившуюся папку из GitHub, но и достаёт из копилки работу, не попавшую в коммиты; он не трогает папку, если в ней есть свои несохранённые правки, и если копилка старее последнего коммита (иначе затёр бы свежее старым). Проверено имитацией аварии: незакоммиченный файл → слепок → `git reset --hard` на старый коммит и удаление файла → хук вернул и папку, и файл. **Своя ошибка по ходу:** первая проверка вернула папку, но не файл — откат вернул старую версию самого хука, и дальше выполнялась она; добавлен перезапуск скрипта после восстановления. Записано в `docs/ОШИБКИ.md` (правила 9 и 10) | Владелец: «сделай так что-бы не откатывалось» |
| 2026-08-22 | **Доводка автосохранения после проверок на имитации аварии** (продолжение записи выше). Две недоработки, которые нашлись только на живой проверке: восстановление из копилки оставляло файлы **подготовленными к коммиту** (`git checkout <ветка> -- .` пишет и в индекс) — из-за этого в один коммит попал служебный файл проверки; и снимок делался только при несохранённых правках, поэтому после коммита в копилке оставался «хвост», который в следующей сессии вернулся бы как якобы потерянная работа. Исправлено: индекс после восстановления возвращается к HEAD, снимок делается и на чистой папке, если копилка отстала от последнего коммита. Проверено заново: авария (откат на коммит назад + пропажа незакоммиченного файла) → хук вернул и коммит, и файл, индекс чистый, повторный запуск ничего лишнего не предлагает. `docs/ОШИБКИ.md` — правило 11 | Владелец: «сделай так что-бы не откатывалось» |
| 2026-08-22 | **«Закончилось на полке» — список пополнения** (владелец: «продолжай разработку»; идея из десяти предложенных ранее). Пустую полку сотрудник зала видит раньше всех, но записать это было некуда — запоминали в голове, до заказа доживало не всё. Сделано: кнопка «Закончилось на полке» в карточке товара, **третий режим сканера** («Товар» / «Ценник» / «Закончилось») — можно обойти зал с камерой, не закрывая её, и **экран списка**, разложенного по поставщикам. Из группы поставщика — кнопка «Заказать»: открывается форма заказа с подставленным поставщиком и перечнем того, что закончилось, а после сохранения эти позиции помечаются «заказано», чтобы их не заказали дважды. Список живёт на телефоне (сервера нет, писать в каталог может только владелец) и одной кнопкой уходит владельцу текстом — так же, как заказы. **Против мусора и зависаний:** название и код запоминаются прямо в строке (список не рассыплется, если товар удалят), не больше 300 строк, заказанное старше двух недель исчезает само. Счётчик ожидающих позиций виден в меню. Заказы и список пополнения связаны обратным вызовом, а не встречными импортами. Бамп кэша v91, новая проверка `tests/restock.js` (23 пункта), все 18 наборов проходят | Владелец: «продолжай разработку» |
| 2026-08-22 | **Пересчёт остатков (инвентаризация) в каталоге** (владелец: «продолжай разработку»; идея из десяти предложенных ранее). Считали на бумаге, потом сверяли глазами. Теперь сотрудник идёт по залу с телефоном: **каждое сканирование — плюс одна штука** (камера не закрывается), товар без штрихкода добавляется по коду или названию (весовое, выпечка), количество правится кнопками «−/+» или вводится руками. **Расхождение с остатком из 1С видно прямо в строке** (недостача красным, излишек оранжевым, «сходится» зелёным), наверху — счётчик расхождений. **Остатки каталога пересчёт не переписывает**: они приходят из 1С, и подменять учёт приложение не должно — это сверка, готовый список уходит владельцу текстом (расхождения первыми). Лист пересчёта живёт на телефоне (`wm_count_v1`), переживает перезапуск приложения, не больше 1000 строк; в меню видно, что пересчёт начат и сколько посчитано. Бамп кэша v92, новая проверка `tests/count.js` (16 пунктов); проверка вёрстки расширена — теперь она открывает и **новые окна с настоящим содержимым** (раньше проверяла их пустыми), все 19 наборов проходят | Владелец: «продолжай разработку» |
| 2026-08-22 | **Пересчёт остатков убран, ярлыки приложения оставлены** (владелец: «инвентаризацию не нужно»). Экран пересчёта был сделан и проверен, но владельцу он не нужен — удалён целиком: модуль, окно, пункт меню, стили, проверка и ярлык. Осталось то, что рядом появилось и работу не усложняет: **быстрые действия с ярлыка приложения** — долгое нажатие на значок на главном экране телефона открывает «Сканер» или «Закончилось» (адрес `?do=…` разбирается один раз и убирается, чтобы обновление страницы не повторяло действие, а без входа рабочий список сначала спрашивает пароль). Бамп кэша v93, проверка `tests/shortcuts.js`, все 19 наборов проходят. **Правило на будущее:** не возвращать инвентаризацию без отдельной просьбы | Владелец: «инвентаризацию не нужно» |
| 2026-08-22 | **Вкладка «Работа» и полный календарь заказов** (владелец: «добавь нужные кнопки экраны календарь заказов»). Заказы, «закончилось на полке» и сравнение жили в меню под кнопкой с человечком — там их ищут в последнюю очередь. Сделано: **пятая вкладка нижней панели «Работа»** (значок-фургон) — экран дел смены со состоянием прямо в строках: сколько поставок на этой неделе и на какую сумму, сколько просрочено (красным), сколько позиций ждёт заказа, сколько товаров отобрано к сравнению, и вход в сканер; на самой вкладке значок «сколько дел». Без входа экран объясняет, для кого он, и ведёт на вход. **Календарь заказов дополнен:** переключатель «Неделя / Месяц» (месяц — сетка дней с суммами, итог за месяц, тап по дню открывает его неделю), кнопка **«Сегодня»**, и **просроченные поставки** — день прихода прошёл, а «пришёл» никто не отметил: они помечены красным в строке и собраны отдельным блоком, но не дублируются, если и так видны в показанной неделе. Значок вкладки обновляется через `ui`, а не встречным импортом (сетка и рабочие списки не должны знать друг о друге). Бамп кэша v94, новая проверка `tests/work.js`, проверка заказов расширена (месяц, «Сегодня», просрочка), навигации — на пять вкладок; все 20 наборов проходят | Владелец: «добавь нужные кнопки экраны календарь заказов» |
| 2026-08-22 | **Плашка «что ждёт сегодня» на главном экране** (владелец: «продолжай»). В каталог заходят десятки раз за смену, и первое, что человек должен видеть, — приезжает ли сегодня поставка и не висит ли непринятый заказ. Наверху главного экрана появилась плашка-кнопка: «Сегодня N поставок · сумма», рядом красным «просрочено N»; тап открывает заказы. Показывается только вошедшим и только когда есть о чём сказать — пустая плашка не занимает место. Считается из тех же данных, что и вкладка «Работа», обновляется вместе с ней. Бамп кэша v95, проверка `tests/work.js` расширена (плашка, её сумма, переход, исчезновение когда дел нет) | Владелец: «продолжай» |
| 2026-08-22 | **Позиции в заказе: что именно заказали** (владелец: «продолжай»). В заказе была только сумма — при приёмке сверять было не с чем, а перечень из списка «закончилось» уходил строкой в примечание и обрезался. Теперь у заказа есть **список позиций** с количеством: по коду или штрихкоду подставляется товар из каталога, чего в каталоге нет — записывается как написали (заказывают и то, чего в базе ещё нет). Позиции **необязательны**: заказ по телефону «как обычно» так и остаётся одной суммой. Из списка «закончилось на полке» позиции подставляются в заказ сами, лишнее убирается крестиком. В списке заказов видно «N позиций», в сообщении владельцу перечень идёт отдельной строкой. Бамп кэша v96, проверки заказов и списка пополнения расширены; все 20 наборов проходят. **Своя ошибка по ходу:** присвоение позиций поставил выше объявления `const o` — форма заказа перестала открываться совсем; поймано проверками сразу после правки | Владелец: «продолжай» |
| 2026-08-26 | **Аудит скорости и модернизация под Android** (владелец: «на Android очень зависает и неудобно… хочу, чтобы и на iPhone, и на Android быстро отвечал и быстро обновлялся; сделай полный аудит»). Замерено на процессоре в 6 раз медленнее обычного (бюджетный Android), каталог 17 000 товаров. **Найдено четыре причины.** (1) При загрузке для каждого товара считались полтора десятка производных строк для поиска — одна задача почти на 3 секунды, в которую телефон не отвечал. (2) Поиск словом перебирал все 17 000 товаров с разбором опечаток — около 6 секунд на каждую букву. (3) Офлайн-копия работала «сначала сеть»: каждый заход ждал ответа сервера по двум десяткам файлов, хотя всё уже лежало на телефоне; вдобавок при первом заходе всем скачивался разборщик Excel (310 КБ сжатых), нужный только владельцу. (4) «Матовое стекло» (backdrop-filter) под шапкой и окнами пересчитывалось каждый кадр — отсюда рывки при прокрутке и дёрганое открытие окон. **Сделано:** ленивые производные строки + указатель «начало слова → товары», собираемый порциями в свободную минуту; разбор опечаток отдельным проходом с дешёвой отбраковкой; общий Intl.Collator вместо localeCompare и пропуск лишней сортировки; запись кэша и разбор файлов 1С вынесены с горячего пути (файлы 1С — в отдельный поток, `js/xlsx-worker.js`); офлайн-копия отдаёт оболочку сразу и обновляется в фоне, причём перезагрузка на новую версию не срабатывает, пока человек печатает или держит открытое окно; экономный режим (телефон с 4 ГБ и меньше) выключает размытие; подсказка «поставить на главный экран». **Итог:** первые товары 1666→441 мс, весь каталог 6915→1060 мс, поиск словом ~6000→439 мс, открытие карточки 228→93 мс, самая длинная заминка 3788→394 мс. Выдача поиска закреплена `tests/search-quality.js` и не изменилась; добавлены `tests/speed.js`, `tests/offline.js`, `tests/import-worker.js`; всего 24 набора, все проходят | Владелец: «сделай полный аудит, найди все ошибки, модернизация» |
| 2026-08-27 | **Понятные отказы публикации и деньги с разделителями** (владелец прислал скрин с ошибкой и попросил «чтобы везде суммы разделялись»). **(1) Отказ GitHub.** Владелец увидел «Сохранено, но опубликовать не удалось: GitHub GET /repos/… → 401 Bad credentials» — это код ответа сервера, а не объяснение. Причина у него житейская: ключ доступа просрочился. Теперь причина переводится на человеческий (`ghReason` в publish.js): просроченный ключ, не найдена ветка, «слишком часто», сбой GitHub; вверху экрана появляется плашка «Сохранено на телефоне, но не опубликовано… Нажми, чтобы вставить новый ключ», нажатие открывает окно публикации. Технические подробности уходят в журнал ошибок. Записанное при этом не теряется — оно на телефоне. **(2) Деньги.** Суммы пишутся с разделителями разрядов везде, включая **поля ввода**: сумма заказа, цена конкурента, калькулятор, фильтр цены. Число разбивается по ходу набора (`attachMoneyInput`), курсор остаётся на месте (считаем цифры слева от него, а не символы), сохраняется настоящее число (`moneyNum`), а поля переведены с `type=number` на текстовые — в числовое поле пробел просто не помещается. Количества в отчётах тоже разбиты: «12 000 шт». Новые проверки `tests/publish-errors.js` и `tests/money.js`, бамп кэша v99, все 26 наборов проходят | Владелец: «исправь ошибку… сделай, чтобы везде суммы разделялись» |
