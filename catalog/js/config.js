// Настройки каталога. Сервера нет: витрина лежит файлом на сайте, полный
// каталог — в зашифрованном файле в репозитории. Секретов здесь НЕТ.
window.CATALOG_CONFIG = {
  // Откуда покупатель и невошедший сотрудник берут витрину.
  STATIC_URL: 'data/',

  // Куда публикуется каталог. Это не секрет — просто адрес репозитория.
  // Сам ключ (GitHub token) хранится только на устройстве владельца
  // (localStorage) и в репозиторий никогда не попадает.
  GITHUB_OWNER: 'dexauron',
  GITHUB_REPO: 'Auron',
  GITHUB_BRANCH: 'claude/store-product-catalog-60wer4',
  DATA_PATH: 'catalog/data',
};
