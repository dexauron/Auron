/* ============================================================================
   ПРИМЕР ЗА ГОД: чтобы увидеть программу заполненной, не вводя ничего руками.

   Владелец попросил «примерно заполни данные за год во всех окнах». Пустая
   программа ничего не говорит о себе: непонятно, что считается, как выглядят
   отчёты, что вообще можно спрашивать. Здесь генерируется год работы обычного
   круглосуточного продуктового: смены, расходы, зарплата, долги, товар.

   ДВА ПРАВИЛА, КОТОРЫЕ ЗДЕСЬ НАРУШАТЬ НЕЛЬЗЯ:

   1. КАЖДАЯ ЗАПИСЬ ПОМЕЧЕНА `demo: true`. Убрать пример — значит удалить
      ровно эти записи и не тронуть ни одной настоящей. Без метки пришлось
      бы чистить по дате или по «похожести», а это способ однажды стереть
      владельцу его работу.

   2. ПЕРЕД ЗАПОЛНЕНИЕМ ПРОГРАММА СПРАШИВАЕТ, а если записи уже есть —
      предупреждает отдельно. Учёт не то место, где что-то появляется само.

   Числа взяты правдоподобные для магазина у дома: выручка 60–130 тысяч в
   смену, четыре кассира, два денежных ящика, аренда, коммуналка, зарплата.
   ========================================================================== */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else root.WMDemo = factory();
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';

  var МЕТКА = 'demo';
  var КАССИРЫ = ['Аня', 'Марат', 'Зарема', 'Иман'];
  var ПОСТАВЩИКИ = ['Молокозавод', 'Хлебозавод', 'Мясокомбинат', 'Овощебаза',
    'Напитки Юг', 'Бакалея Опт', 'Кондитер'];
  var СТАТЬИ = ['Аренда', 'Коммунальные', 'Интернет и связь', 'Охрана',
    'Вывоз мусора', 'Обед', 'Расходники', 'Ремонт и обслуживание', 'Реклама'];
  var ГРУППЫ = ['Молочное', 'Хлеб', 'Мясо и птица', 'Овощи и фрукты',
    'Напитки', 'Бакалея', 'Кондитерка', 'Бытовая химия', 'Сигареты'];
  var ТОВАРЫ = [
    ['Молоко 3.2% 1 л', 'Молочное'], ['Кефир 1% 0,9 л', 'Молочное'],
    ['Сметана 20% 300 г', 'Молочное'], ['Творог 9% 200 г', 'Молочное'],
    ['Сыр Российский', 'Молочное'], ['Масло сливочное 180 г', 'Молочное'],
    ['Хлеб Бородинский', 'Хлеб'], ['Батон нарезной', 'Хлеб'],
    ['Лаваш тонкий', 'Хлеб'], ['Булочка с маком', 'Хлеб'],
    ['Курица охлаждённая', 'Мясо и птица'], ['Фарш домашний', 'Мясо и птица'],
    ['Колбаса варёная', 'Мясо и птица'], ['Сосиски молочные', 'Мясо и птица'],
    ['Картофель', 'Овощи и фрукты'], ['Лук репчатый', 'Овощи и фрукты'],
    ['Помидоры', 'Овощи и фрукты'], ['Яблоки', 'Овощи и фрукты'],
    ['Бананы', 'Овощи и фрукты'], ['Огурцы', 'Овощи и фрукты'],
    ['Вода 5 л', 'Напитки'], ['Сок апельсиновый 1 л', 'Напитки'],
    ['Кола 1,5 л', 'Напитки'], ['Чай чёрный 100 пак', 'Напитки'],
    ['Кофе растворимый 95 г', 'Напитки'], ['Энергетик 0,45', 'Напитки'],
    ['Сахар 1 кг', 'Бакалея'], ['Мука 2 кг', 'Бакалея'],
    ['Рис длиннозёрный 900 г', 'Бакалея'], ['Гречка 800 г', 'Бакалея'],
    ['Макароны 450 г', 'Бакалея'], ['Масло подсолнечное 1 л', 'Бакалея'],
    ['Соль 1 кг', 'Бакалея'], ['Тушёнка говяжья', 'Бакалея'],
    ['Печенье овсяное', 'Кондитерка'], ['Шоколад молочный', 'Кондитерка'],
    ['Конфеты весовые', 'Кондитерка'], ['Вафли', 'Кондитерка'],
    ['Порошок стиральный 3 кг', 'Бытовая химия'], ['Мыло туалетное', 'Бытовая химия'],
    ['Губки для посуды', 'Бытовая химия'], ['Пакеты фасовочные', 'Бытовая химия'],
    ['Сигареты А', 'Сигареты'], ['Сигареты Б', 'Сигареты'], ['Сигареты В', 'Сигареты']
  ];

  /* Свой генератор случайных чисел. Нужен, чтобы пример был одинаковым:
     владелец показал экран, а через час открыл — и там те же числа. */
  var семя = 424242;
  function сл() { семя = (семя * 1103515245 + 12345) & 0x7fffffff; return семя / 0x7fffffff; }
  function цел(a, b) { return a + Math.floor(сл() * (b - a + 1)); }
  function из(список) { return список[цел(0, список.length - 1)]; }
  function руб(x) { return Math.round(x * 100) / 100; }

  /* Слить два объекта в новый. Object.assign в этом коде нигде не
     используется — программа обязана открываться и в старом браузере. */
  function слить(a, b) {
    var out = {}, k;
    for (k in a) if (Object.prototype.hasOwnProperty.call(a, k)) out[k] = a[k];
    for (k in b) if (Object.prototype.hasOwnProperty.call(b, k)) out[k] = b[k];
    return out;
  }

  function датаОт(база, сдвиг) {
    var t = new Date(база + 'T00:00:00Z');
    t.setUTCDate(t.getUTCDate() + сдвиг);
    return t.toISOString().slice(0, 10);
  }

  /* Сколько записей в базе: своих (пример) или настоящих */
  function счёт(S, свои) {
    var n = 0;
    (S.COLLECTIONS || []).forEach(function (c) {
      if (c === 'log' || c === 'trash' || c === 'accounts') return;
      (S.state[c] || []).forEach(function (r) {
        if (!r) return;
        if (свои ? r[МЕТКА] : !r[МЕТКА]) n++;
      });
    });
    return n;
  }

  /* --- ЗАПОЛНИТЬ ------------------------------------------------------- */
  function заполнить(S, E, D, дней) {
    дней = дней || 365;
    семя = 424242;
    var конец = E.today();
    var начало = датаОт(конец, -(дней - 1));

    S.ensureAccounts();
    var счета = S.state.accounts || [];
    function поВиду(k) {
      for (var i = 0; i < счета.length; i++) {
        if (счета[i].kind === k && !счета[i].archived) return счета[i].id;
      }
      return счета[0] ? счета[0].id : '';
    }
    var сейф = поВиду('cash'), банк = поВиду('bank');

    var кассы = E.txt(S.settings.tills)
      ? E.txt(S.settings.tills).split(',').map(function (x) { return x.trim(); }).filter(Boolean)
      : ['Касса 1', 'Касса 2'];
    var смены = E.txt(S.settings.finShifts)
      ? E.txt(S.settings.finShifts).split(',').map(function (x) { return x.trim(); }).filter(Boolean)
      : ['День', 'Ночь'];

    /* Складываем в свои списки и отдаём базе ОДНИМ разом.

       Сначала я звал S.add() на каждую запись. На 2 800 записях это 17
       секунд: add() сохраняет всю базу и пишет строку в журнал правок —
       каждый раз. Владелец решил бы, что программа зависла, а журнал
       «Что менялось» забился бы примером до потери смысла.

       addMany() сохраняет один раз и в журнал не пишет. */
    var копим = {};
    function добавить(coll, rec) {
      rec[МЕТКА] = true;
      if (!копим[coll]) копим[coll] = [];
      копим[coll].push(rec);
      return rec;
    }
    function отдать() {
      var всего = 0;
      for (var c in копим) {
        if (!Object.prototype.hasOwnProperty.call(копим, c)) continue;
        всего += копим[c].length;
        S.addMany(c, копим[c]);
      }
      return всего;
    }

    /* --- Люди ---------------------------------------------------------- */
    var должности = ['Продавец-кассир', 'Продавец зала', 'Старший смены', 'Уборщица'];
    КАССИРЫ.forEach(function (имя, i) {
      добавить('staff', { name: имя, position: должности[i % должности.length],
        rate: [200, 220, 210, 190][i], phone: '+7 928 ' + цел(100, 999) + '-' + цел(10, 99) + '-' + цел(10, 99),
        hired: датаОт(начало, -цел(30, 400)) });
    });
    добавить('staff', { name: 'Лиза', position: 'Уборщица', rate: 150,
      hired: датаОт(начало, -цел(100, 500)), fired: датаОт(начало, цел(30, 120)) });

    /* --- Год работы ---------------------------------------------------- */
    var размен = {};
    кассы.forEach(function (к) { размен[к] = 5000; });

    for (var д = 0; д < дней; д++) {
      var дт = датаОт(начало, д);
      var мес = дт.slice(5, 7), число = +дт.slice(8, 10);
      // выручка выше к выходным и к концу месяца — как в жизни
      var дн = new Date(дт + 'T00:00:00Z').getUTCDay();
      var сезон = (дн === 5 || дн === 6) ? 1.18 : (дн === 1 ? 0.92 : 1);
      var деньЗП = (число >= 5 && число <= 12) ? 1.1 : 1;

      for (var ки = 0; ки < кассы.length; ки++) {
        for (var си = 0; си < смены.length; си++) {
          var касса = кассы[ки], смена = смены[си];
          var было = размен[касса];
          var база = (ки === 0 ? цел(45000, 95000) : цел(25000, 55000));
          var zНал = руб(база * сезон * деньЗП);
          var zБезнал = руб(zНал * (0.7 + сл() * 0.9));
          var карта = руб(zБезнал * (0.45 + сл() * 0.2));
          var qr = руб(zБезнал * (0.3 + сл() * 0.2));
          var телефон = руб(zБезнал - карта - qr);
          var возврат = сл() < 0.1 ? цел(50, 2000) : 0;

          var список = [], выплаты = 0;
          var строк = сл() < 0.5 ? цел(1, 2) : 0;
          for (var i = 0; i < строк; i++) {
            var сум = цел(500, 25000);
            выплаты += сум;
            список.push({ name: из(ПОСТАВЩИКИ) + ', поставка', sum: сум });
          }
          var внесено = сл() < 0.07 ? цел(2000, 10000) : 0;
          var оставил = сл() < 0.12 ? цел(0, 8000) : было;
          var должны = руб(было + zНал - возврат + внесено - выплаты - оставил);
          var расх = 0;
          if (сл() < 0.2) расх = цел(-500, 500);
          if (сл() < 0.015) расх = цел(-9000, 9000);

          добавить('dds', { type: E.T_SHIFT, date: дт, till: касса, shift: смена,
            cashier: из(КАССИРЫ), openCash: было, zCash: zНал, zCashless: zБезнал,
            zCard: карта, zQr: qr, zNfc: телефон,
            returnsCash: возврат, deposits: внесено,
            payouts: выплаты, payoutList: список,
            kept: оставил, received: руб(должны + расх),
            checks: цел(90, 420), voided: сл() < 0.15 ? цел(1, 4) : 0,
            toAccount: сейф, cashlessAccount: банк });
          размен[касса] = оставил;
        }
      }

      // Итоги дня: закуп и долги поставщикам
      if (сл() < 0.6) {
        добавить('dds', { type: E.T_DAY, date: дт,
          goodsCash: сл() < 0.6 ? цел(5000, 60000) : 0,
          debtTaken: сл() < 0.5 ? цел(10000, 120000) : 0,
          debtPaid: сл() < 0.55 ? цел(10000, 90000) : 0 });
      }
      // Расходы
      if (сл() < 0.4) {
        добавить('dds', { type: E.T_OUT, date: дт, category: из(СТАТЬИ),
          method: 'Наличные', account: сейф, amount: цел(500, 45000) });
      }
      if (число === 5) {
        добавить('dds', { type: E.T_OUT, date: дт, category: 'Аренда',
          method: 'Перевод', account: банк, amount: 110000 });
      }
      if (число === 12) {
        добавить('dds', { type: E.T_OUT, date: дт, category: 'Коммунальные',
          method: 'Перевод', account: банк, amount: цел(20000, 48000) });
      }
      // Владелец берёт себе / вносит своё
      if (сл() < 0.06) {
        добавить('dds', { type: E.T_DRAW, date: дт, method: 'Наличные',
          account: сейф, amount: цел(10000, 80000), note: 'на личные нужды' });
      }
      if (сл() < 0.03) {
        добавить('dds', { type: E.T_IN, date: дт, category: 'Внёс владелец',
          method: 'Наличные', account: сейф, amount: цел(50000, 300000),
          note: 'не хватало на закуп' });
      }
      if (сл() < 0.03) {
        добавить('dds', { type: E.T_IN, date: дт, category: 'Возврат от поставщика',
          method: 'Наличные', account: сейф, amount: цел(2000, 30000),
          note: из(ПОСТАВЩИКИ) });
      }
      // Сдача наличных в банк
      if (сл() < 0.09) {
        добавить('dds', { type: E.T_MOVE, date: дт, category: 'Сдача в банк',
          account: сейф, toAccount: банк, amount: цел(50000, 400000) });
      }
      // Табель: кто работал в эту смену
      if (д % 1 === 0) {
        for (var т = 0; т < смены.length; т++) {
          добавить('timesheet', { date: дт, employee: из(КАССИРЫ), shift: смены[т],
            hoursDay: смены[т] === смены[0] ? 12 : 0,
            hoursNight: смены[т] === смены[0] ? 0 : 12,
            bonus: сл() < 0.05 ? цел(500, 3000) : 0,
            fine: сл() < 0.03 ? цел(200, 2000) : 0 });
        }
      }
      // Зарплата: аванс и расчёт
      if (число === +E.num(S.settings.advanceDay || 25)) {
        КАССИРЫ.forEach(function (имя) {
          добавить('payouts', { date: дт, employee: имя, kind: 'Аванс',
            amount: цел(8000, 16000), method: 'Наличные', account: сейф });
        });
      }
      if (число === +E.num(S.settings.salaryDay || 10)) {
        КАССИРЫ.forEach(function (имя) {
          добавить('payouts', { date: дт, employee: имя, kind: 'Расчёт',
            amount: цел(12000, 28000), method: 'Наличные', account: сейф });
        });
      }
      // Долги покупателей — тетрадка у кассы
      if (сл() < 0.08) {
        var взято = цел(300, 6000);
        добавить('debtors', { date: дт, name: из(['Сосед Ваха', 'Тётя Зина', 'Рамзан',
          'Мадина', 'Электрик Саша', 'Водитель Ислам']),
          sum: взято, paid: сл() < 0.6 ? взято : (сл() < 0.5 ? цел(0, взято) : 0),
          note: 'до зарплаты' });
      }
      // Пересчёт денег по купюрам
      if (сл() < 0.04) {
        добавить('cashcount', { date: дт, till: из(кассы), cashier: из(КАССИРЫ),
          n5000: цел(0, 12), n2000: цел(0, 8), n1000: цел(0, 20), n500: цел(0, 15),
          n200: цел(0, 10), n100: цел(0, 25), n50: цел(0, 10), n10: цел(0, 30) });
      }
    }

    /* --- План выплат поставщикам на ближайшее ------------------------- */
    for (var п = 0; п < 9; п++) {
      var когда = датаОт(конец, цел(-12, 25));
      добавить('plans', { due: когда, supplier: из(ПОСТАВЩИКИ), category: 'Оплата ТП',
        amount: цел(15000, 180000),
        status: когда < конец ? (сл() < 0.6 ? 'Оплачена' : 'Запланирована') : 'Запланирована' });
    }

    /* --- Конверты: на что копим ---------------------------------------
       ПОЛЕ НАЗЫВАЕТСЯ plan, А НЕ goal. Программа считает конверты по plan
       (сколько откладывать в месяц); пример писал goal, и экран
       «Накопления» показывал нули во всех трёх плитках при заполненной
       базе. Это третий такой случай в примере за один вечер — поэтому
       появилась проверка «пример заполняет экраны, а не притворяется».

       И сами деньги в конверты кто-то должен класть: без записей
       «Перемещение» с номером конверта в нём лежит ноль, сколько ни
       планируй. */
    var конверты = [
      { id: 'demo-fund-nalogi', name: 'Налоги за квартал', plan: 60000,
        note: 'откладываем каждый месяц, платим раз в квартал' },
      { id: 'demo-fund-arenda', name: 'Аренда на месяц вперёд', plan: 110000,
        note: 'чтобы аренда не била по обороту' },
      { id: 'demo-fund-remont', name: 'Ремонт холодильника', plan: 20000,
        note: 'копим на замену компрессора' }
    ];
    конверты.forEach(function (f) {
      добавить('funds', { id: f.id, name: f.name, plan: f.plan,
        account: поВиду('cash'), note: f.note });
    });

    /* Кладём в конверты по месяцам — не всегда полностью, как в жизни:
       иначе «надо отложить» всегда показывало бы ноль и экран не объяснял
       бы, зачем он нужен. */
    for (var вз = 0; вз < Math.min(12, Math.round(дней / 30)); вз++) {
      var деньВзноса = датаОт(конец, -(вз * 30 + цел(1, 8)));
      конверты.forEach(function (f) {
        if (сл() < 0.2) return;                       // месяц пропустили — бывает
        добавить('dds', { type: 'Перемещение', date: деньВзноса,
          from: поВиду('cash'), to: поВиду('cash'), fund: f.id,
          amount: руб(f.plan * (0.6 + сл() * 0.4)),
          note: 'В конверт «' + f.name + '»' });
      });
    }

    var добавлено = отдать();
    S.save();
    return добавлено;
  }

  /* --- ТОВАРНАЯ АНАЛИТИКА: живёт в памяти, как и настоящие выгрузки 1С -- */
  function товары(D, E, дней) {
    дней = дней || 365;
    семя = 909090;
    var конец = E.today();
    var месяцев = Math.min(12, Math.max(1, Math.round(дней / 30)));

    /* У каждого товара свой характер продаж, и это не украшение.

       Сначала я брал количество случайным каждый месяц — и XYZ-анализ
       показал, что ВСЕ сорок пять товаров рваные (группа Z). В жизни так не
       бывает: хлеб и молоко берут ровно каждый день, а вот шампанское —
       рывками. Пример, в котором весь товар одинаково непредсказуем, врёт
       об анализе, ради которого этот экран и сделан.

       Поэтому у товара есть базовый уровень и разброс вокруг него:
       ровный (±8%) — это X, средний (±22%) — Y, рваный (±60%) — Z. */
    var цены = ТОВАРЫ.map(function (t, i) {
      var закуп = цел(25, 900);
      var характер = i % 5 < 2 ? 0.08 : (i % 5 < 4 ? 0.22 : 0.6);
      return { name: t[0], group: t[1], buy: закуп,
        sell: Math.round(закуп * (1.18 + сл() * 0.55)),
        база: i % 3 === 0 ? цел(300, 800) : цел(20, 190),
        разброс: характер };
    });

    D.sales = [];
    for (var м = месяцев - 1; м >= 0; м--) {
      /* Полные календарные месяцы: выгрузку из 1С делают за месяц целиком,
         а не «с первого по восемнадцатое». Куски мешали бы сравнивать. */
      var t2 = new Date(конец.slice(0, 8) + '01T00:00:00Z');
      t2.setUTCMonth(t2.getUTCMonth() - м);
      var от = t2.toISOString().slice(0, 10);
      t2.setUTCMonth(t2.getUTCMonth() + 1);
      t2.setUTCDate(0);
      var до = t2.toISOString().slice(0, 10);
      if (до > конец) до = конец;
      var период = { periodKey: от + '..' + до, from: от, to: до, date: до };
      цены.forEach(function (ц) {
        var откл = 1 + (сл() * 2 - 1) * ц.разброс;
        var кол = Math.max(1, Math.round(ц.база * откл));
        var выручка = руб(кол * ц.sell);
        var себест = руб(кол * ц.buy);
        D.sales.push(слить({ name: ц.name, key: E.norm(ц.name), group: ц.group,
          qty: кол, revenue: выручка, cogs: себест, profit: руб(выручка - себест),
          sellPrice: ц.sell, buyPrice: ц.buy, abc: '' }, период));
      });
    }

    var сейчас = { periodKey: 'остатки', from: конец, to: конец, date: конец };
    D.stock = цены.map(function (ц) {
      var кол = цел(0, 240);
      return { name: ц.name, key: E.norm(ц.name), group: ц.group, unit: 'шт',
        article: '', code: '', barcode: '460' + цел(1000000, 9999999),
        qty: кол, buyPrice: ц.buy, buySum: руб(кол * ц.buy),
        retailPrice: ц.sell, retailSum: руб(кол * ц.sell) };
    });
    D.stockTaken = конец;

    /* ЦЕНЫ ПОСТАВЩИКОВ — ПО НЕСКОЛЬКУ ПРЕДЛОЖЕНИЙ НА ТОВАР.
       Экран «Где дешевле» сравнивает предложения РАЗНЫХ поставщиков на один
       товар. Одна строка на товар сравнивать не с чем: экран честно
       показывал бы нули, и владелец решил бы, что программа сломана.

       Имена полей — те же, что отдаёт разбор выгрузки 1С: price и supplier.
       Пример, заполненный другими именами, показывает пустые экраны — так и
       было: 91 нуль из 92 на «Где дешевле». */
    D.prices = [];
    цены.forEach(function (ц) {
      var сколько = цел(2, 4);                      // 2-4 поставщика на товар
      var кто = ПОСТАВЩИКИ.slice().sort(function () { return сл() - 0.5; }).slice(0, сколько);
      кто.forEach(function (п, i) {
        /* Первый — самый дешёвый, дальше дороже: тогда на экране видна
           разница, ради которой его и открывают. */
        var надбавка = 1 + i * (0.03 + сл() * 0.12);
        D.prices.push({ name: ц.name, key: E.norm(ц.name), group: ц.group,
          supplier: п, price: руб(ц.buy * надбавка),
          barcode: '460' + цел(1000000, 9999999), unit: 'шт', date: конец, article: '' });
      });
    });
    D.pricesTaken = конец;

    /* key обязателен: по нему программа ищет телефон поставщика
       (contactsIndex). Без него телефоны не находились нигде — ни на
       «Где дешевле», ни у поставщиков. */
    D.contacts = ПОСТАВЩИКИ.map(function (п) {
      return { name: п, key: E.norm(п), firm: п,
        phone: '+7 928 ' + цел(100, 999) + '-' + цел(10, 99) + '-' + цел(10, 99),
        email: '', note: '' };
    });

    D.writeoffs = [];
    цены.forEach(function (ц) {
      if (сл() > 0.25) return;
      var кол = цел(1, 20);
      D.writeoffs.push({ name: ц.name, key: E.norm(ц.name),
        reason: из(['Истёк срок', 'Бой', 'Порча', 'Недостача']),
        qty: кол, cost: руб(кол * ц.buy), retail: руб(кол * ц.sell),
        date: датаОт(конец, -цел(1, 60)) });
    });

    D.dead = [];
    цены.forEach(function (ц, i) {
      if (i % 7 !== 0) return;
      var кол = цел(10, 90);
      D.dead.push(слить({ name: ц.name, key: E.norm(ц.name), group: ц.group,
        qty: кол, cost: руб(кол * ц.buy), sold: цел(0, 4),
        days: цел(60, 300) }, сейчас));
    });

    /* Возвраты покупателей: этот экран оставался пустым, и заметил это не я,
       а сплошная проверка всех сорока шести экранов. */
    D.returns = [];
    цены.forEach(function (ц) {
      if (сл() > 0.18) return;
      var кол = цел(1, 6);
      /* cost и retail — имена из разбора 1С, и экран «Возвраты» читает
         именно их. Пример писал revenue/cogs, и все суммы на экране были
         нулями при пяти видимых строках. */
      D.returns.push(слить({ name: ц.name, key: E.norm(ц.name), group: ц.group,
        qty: кол, cost: руб(кол * ц.buy), retail: руб(кол * ц.sell),
        reason: из(['Не подошёл', 'Брак', 'Истёк срок', 'Передумал']) }, сейчас));
    });
    D.returnsPeriod = { from: датаОт(конец, -30), to: конец };

    D.files = [{ name: 'Пример за год (без файла)', kind: 'sales',
      rows: D.sales.length, note: 'данные для показа, выгрузка не загружалась' }];
    return D.sales.length;
  }

  /* --- УБРАТЬ ПРИМЕР --------------------------------------------------- */
  function убрать(S) {
    var убрано = 0;
    (S.COLLECTIONS || []).forEach(function (c) {
      var список = S.state[c];
      if (Object.prototype.toString.call(список) !== '[object Array]') return;
      var было = список.length;
      S.state[c] = список.filter(function (r) { return !(r && r[МЕТКА]); });
      убрано += было - S.state[c].length;
    });
    S.save();
    return убрано;
  }

  return { заполнить: заполнить, товары: товары, убрать: убрать,
    счёт: счёт, МЕТКА: МЕТКА };
});
